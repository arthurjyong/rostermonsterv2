# Roadmap (Milestone Delivery Order)

This document defines **milestone-level delivery order** for Roster Monster v2. It is intentionally not a day-to-day execution tracker; active checkpoint/task truth lives in `docs/delivery_plan.md`.

## Milestone sequence

### 1) Operator-ready request sheet generation — *Completed 2026-04-21*
Deliver an operator-usable ICU/HD request sheet shell backed by closed generation contract boundaries. Closed via M1 C1..C4 (contract closure + implementation) on operator delivery. See `docs/delivery_plan.md` §11; D-0019.

### 1.1) Operator-facing launcher *(addendum to Milestone 1)* — *Completed 2026-04-22*
Narrow operator-facing launcher so named monthly-rotation pilot operators can invoke empty ICU/HD request-sheet generation without running Apps Script by hand. Closed via M1.1 C1 on hands-on validation. M1 itself stays Completed (not reopened) per the addendum-milestone convention (D-0021); launcher architecture in D-0022; auto-share scope in D-0023.

### 2) Minimal local compute pipeline — *Completed 2026-04-29*
Deterministic local parse → normalize → rule / scoring / solve execution path using closed contracts. Closed across nine checkpoints (M2 C1..C9): rule engine + scorer + solver + selector contracts (C1, C2); their Python implementations (C3, C4, C5); operator-tuneable scoring config surface end-to-end (C6, C7); v3 spacingPenalty geometric-decay curve (C8); production Apps Script snapshot extractor + Python CLI ingestion path (C9). D-0024..D-0043.

### 3) Safe result/output and writeback — *Completed 2026-04-30*
Safe result surfaces and sheet writeback behavior for operator consumption. Closed at M3 C1 (writeback library + launcher route + Python CLI extension); M3 C2 (live-operator demo) dropped per D-0048 — round-trip already proven on the dev-copy. D-0044..D-0047.

### 4) Cloud end-to-end pipeline + dual-track preservation — *Completed 2026-05-01*
Cloud-deployed end-to-end pipeline so operators can drive a one-click roster-generation flow without local Python tooling, while preserving the local CLI as a maintainer-side dev-velocity surface for solver-strategy experiments. Reframed from `Parallel operational search and orchestration` per D-0049; closed at M4 C1 as the only checkpoint (Cloud Run service + Apps Script library reorganization + bound shim's `Roster Monster → Solve Roster` menu). D-0048..D-0054. The original M4 framing (parallel orchestration) and original M5 framing (observability) parked as **FW-0027** + **FW-0028** — re-promotable to milestones when concrete drivers surface.

### 5) Operator-side analysis & multi-roster delivery — *Completed 2026-05-07*
Operator-side analysis tooling delivered — Python analyzer engine + `AnalyzerOutput` + Apps Script analyzer renderer + launcher Web App upload route, all as sibling consumers of the wrapper envelope (writeback contract untouched, no new selector retention mode). Reframed the post-M4 priority by sequencing operator-side analysis tooling AHEAD of solver-side score-aware search; M5 C4 live operator validation confirmed the analyzer's role as calibration framework — the comparison tab surfaced a load-bearing scoring-formulation insight (`pointBalanceGlobal` weight design) the operator would not have seen from `totalScore` alone, exactly the M5 thesis working as designed. Closed across M5 C1 (analyzer engine + analysis contract; D-0056..D-0058), M5 C2 (renderer + launcher route + cross-page nav; D-0060..D-0063; cloud deployment `@15`), and M5 C4 (live operator validation; D-0065). M5 C3 dropped per D-0063 — upload-portal scope was wholly absorbed into C2's launcher Web App route, same discipline as M3 C2 dropped per D-0048. Long-term scoring-formulation rework parked as FW-0033 (systematic weight elicitation / tuning), explicitly NOT rolled into M6 scope. D-0055..D-0065. Forward-going Task 1/2/3 cadence vocabulary (D-0064) also landed alongside the closure.

### 6) Solver-side score-aware search (LAHC) — *Completed 2026-05-09*
Delivered Late Acceptance Hill Climbing (LAHC) as the first alternative solver search strategy alongside today's `SEEDED_RANDOM_BLIND` per `docs/solver_contract.md` §11.1, addressing FW-0003's empirical score plateau on `SEEDED_RANDOM_BLIND`. Strategy-internal-to-solver framing — extends `docs/solver_contract.md` with a "Search strategies" section + named strategy enum + LAHC algorithm spec; two strategy-aware fields cross the solver boundary at v2: `strategyId` as a required input per §9 (callers MUST pass it explicitly) and the wrapper envelope's `solverStrategy` enumerant as an output recording which strategy ran (so the M5 analyzer + ops trail can see what ran). Maintainer-only operator-tunable surface (Python module constants for cloud defaults; CLI flag overrides for local tuning; no operator-facing UI changes). Closed 2026-05-09 per **D-0069** across M6 C1 (closed 2026-05-07 per D-0067 — solver-strategy contract extension + LAHC algorithm spec; `docs/solver_contract.md` §11.1 expanded + new §12A; solver contract bumped `contractVersion: 1` → `2` because §9 promotes `strategyId` to a required boundary input — input-shape change per §2), M6 C2 (closed 2026-05-08 — LAHC implemented end-to-end in `python/rostermonster/solver/` per §12A spec, no contract changes), M6 C3 (closed 2026-05-08 — envelope additive bump in `docs/selector_contract.md` §16.5 + CLI flags + integration tests; no `contractVersion` bumps; **D-0068** narrows C3 to local-first; cloud-mode LAHC integration carved off to **FW-0035**), and M6 C4 (closed 2026-05-09 — closure scope shifted from a live operator-cycle comparison-tab cross-reference to dev-copy empirical characterization because the dominance signal was already overwhelming on the May 2026 ICU/HD dev-copy fixture: paired t(4)=11.10, p≪0.001, mean Δ +53.85 score points across 5 seeds; cloud benchmarking surfaced a different load-bearing concern — single-thread bottleneck, captured as **FW-0038** parallel-solver M7 candidate; PR #134 PR-A added `LahcParams.swapProbability` + `--lahc-swap-probability` CLI flag + `apps_script/launcher/src/DeleteSheetsById.gs`; resolved value threads through audit surfaces — `SearchDiagnostics.lahcSwapProbability`, `LahcParamsRecord.swapProbability`, `runEnvelope.solverStrategyConfig.lahcParams.swapProbability` per Codex P2 round-1 fix). Sweet-spot defaults `L=10` / `idleThreshold=2000` / `swapProbability=0.5` / `K=10–20` (per FW-0036 + FW-0037 dev-copy evidence) captured as override knobs but **not module-pinned** pending multi-fixture validation. Active milestone moves to *none* deliberately — M7 (parallel solver) held in study before activation per FW-0038. Multi-run side-by-side renderer enhancement parked as FW-0034. Cloud Deep Solve + email-notification + cloud-side FULL retention promotion (FW-0030) + scoring-formulation rework (FW-0033) explicitly carved off to FW or future milestones. M6 closure trail: D-0066 → D-0067 → D-0068 → D-0069.

### 7) Parallel solver (Cloud Batch + single-VM dense pack, async UX) — *Completed 2026-05-13*
Delivered parallel K-trajectory LAHC execution via Cloud Batch single-VM dense pack with async UX, escaping the GIL-bound single-thread bottleneck identified during M6 C4 cloud benchmarking. Closed 2026-05-13 per **D-0072** across M7 C1 (closed 2026-05-10 per D-0070 — asia-southeast1 GCP quota approvals CPUS=300 / INSTANCES=350 / C3_CPUS=108 binding), M7 C2 (closed 2026-05-11 — full Cloud Batch parallel-LAHC chain delivered across 7 PRs #138 → #144 with the §12A.4 byte-identity exit criterion proven 5/5 audit tests; local-CLI ↔ Cloud-Batch produce byte-identical writebackEnvelope at the same `(masterSeed, K)` including the load-bearing negative-seed `_UINT64_MASK` case), M7 C3 (closed 2026-05-12 per D-0071 + Codex P1.7 single-VM amendment — docs-only checkpoint: PR #147 locked D-0071 + 17 rounds of Codex P1/P2 fixes / ~38 individual amendments across 8 docs), M7 C4 (closed 2026-05-13 per D-0072 — async UX implementation across T2A.1..T2E + post-merge follow-ups #160/#161/#162/#163 merged 2026-05-12; the code-implementation track closed 2026-05-12 but the C4 checkpoint itself stayed Active pending T3 — the operator-facing dry run — which landed via M7 C5's live validation on 2026-05-13), and M7 C5 (closed 2026-05-13 per D-0072 — live operator validation on real ICU + HD cycles confirmed the four happy-path operator-facing exit criteria: toast → email-with-AnalyzerOutput-attachment → writeback + 5 roster tabs → analyzer comparison tab, the last closing the D-0069-deferred operator-cycle cross-reference pathway as a natural side-effect of M7 C4's analyzer plumbing in the inline finalize step; the two stretch criteria from §7 C5 — concurrent-rejection on operator double-click and parser-rejection still surfacing synchronously — have unit-test coverage from M7 C4 T2D PR #157 but were not exhaustively validated on the live cycle). **Deployment shape pivoted twice during M7 C3:** (1) D-0070's original framing was 13-VM dense pack (`c3-highcpu-8` × 13, `multiprocessing.Pool(8)` × 13 = K=104) inside a sync request with Cloud Run timeout 250s; (2) D-0071's first framing kept the 13-VM dense pack but moved UX to async with a 2nd task group running the finalizer after the workers via `SUCCEEDED_OR_FAILED` dependency; (3) **Codex P1.7 amendment** caught that Cloud Batch v1 only supports ONE task group (`submitJob` rejects multi-task-group jobs), forcing a SECOND pivot to **single-VM dense pack** — one `c3-highcpu-88` VM, `multiprocessing.Pool(88)` × 1 task = K=88; the finalize step (aggregate + score + select + analyzer + POST callback) runs INLINE in the same Python process after `Pool.close() + .join()`. The sync→async pivot was driven by M7 C2 perftest evidence (real production roster at K_approved=104 returned 5/13 tasks unfinished at the 240s sync deadline, partial K'=64); the single-VM pivot was driven by Cloud Batch v1's single-task-group restriction. K reduces 104 → 88 (statistically equivalent — both 4-9× past best-of-K plateau at K=10-20 per FW-0036; quality impact negligible). 10-min operator-facing cap enforced via the finalize step's first-action self-check at 510s elapsed since `RM_SUBMIT_TIMESTAMP_MS` (reserves ~90s for finalize-step work so operator-facing total wall stays ≤ 600s); task `maxRunDuration: 660s` is a Batch-level safety net intentionally above the 600s self-check ceiling; `maxRetryCount: 0` (single attempt — retry × 660s would blow the 10-min cap). FW-0037 elbow tuple locked at the M7 production config: `L=50` / `idleThreshold=3500` / `swapProbability=0.5`. K=88 reflects the largest c3-highcpu-X size that fits the current C3_CPUS=108 quota — future quota bump to C3_CPUS ≥176 unlocks `c3-highcpu-176` for K=176 (parameterized as FW-0040 dial; stays parked at closure — K=88 vs K=176 is a quality wash per FW-0036's plateau evidence). **FW-0027 (parallel operational search and orchestration), FW-0030 (cloud-side FULL retention support), FW-0035 (cloud-mode LAHC integration), and FW-0038 (parallel solver) all DELIVERED via M7** — FW-0027 absorbed into M7 C3+C4 per D-0071 (async UX supersedes "parallel operational orchestration" framing); FW-0030 delivered as side-effect because in-memory K-candidate aggregation inside the single Cloud Batch task IS the FULL retention (no `candidates_full.json` serialization needed; analyzer runs inline before the callback POST); FW-0035 delivered at M7 C4 (T2C+T2D — bound shim sends `solverStrategy: 'LAHC'` + `operatorEmail`; Cloud Run dispatches into async path on `solverStrategy=LAHC`); FW-0038 itself was M7's implementation target. **FW-0039 (silent-operator-outcome watchdog)** and **FW-0040 (K=88 → K=176 dial)** stay parked post-M7 with their existing revisit triggers. M7 closure trail: D-0070 → D-0071 → D-0072. **Active milestone moves to *none* deliberately** post-M7, mirroring the post-M4 (2026-05-01) and post-M6 (2026-05-09) cadence — a deliberate study window between milestones before the next activation.

## Intentional later work (not near-term)
The roadmap intentionally defers some work until concrete drivers surface:
- ~~Parallel operational search and orchestration (FW-0027)~~ — **DELIVERED via M7 C3+C4 per D-0072.** Async UX (Cloud Run thin front door + Cloud Batch single-task with inline finalize + B-prime callback to launcher) supersedes the original "parallel operational orchestration" framing. Status flipped from "absorbed into M7" → "delivered via M7" at M7 closure 2026-05-13.
- Observability and benchmark hardening (FW-0028) — re-promotable when long-term reliability or benchmark-comparison drivers surface. M5's analysis tooling overlaps the operator-facing slice but does not subsume the broader observability surface. Candidate next-work thread in the post-M7 study window.
- ~~Cloud-side FULL retention support (FW-0030)~~ — **DELIVERED via M7 C3+C4 per D-0072.** In-memory K-candidate aggregation inside the single Cloud Batch task IS the FULL retention; analyzer runs inline before the callback POST (no `candidates_full.json` serialization needed). Status flipped from "absorbed into M7" → "delivered via M7" at M7 closure 2026-05-13.
- ~~Cloud-mode LAHC integration (FW-0035)~~ — **DELIVERED via M7 C3+C4 per D-0072.** Cloud LAHC ships via the bound shim's `solverStrategy: 'LAHC'` request body field; Cloud Run dispatches into the async Cloud Batch path on `solverStrategy=LAHC`. Status flipped from "absorbed into M7" → "delivered via M7" at M7 closure 2026-05-13.
- Systematic weight elicitation / tuning (FW-0033) — re-promotable when cycle-over-cycle weight churn or multi-operator priority disagreement surfaces concrete demand. M5 C4 verdict surfaced this thread but the maintainer's sequencing call was "search next, formulation rework later"; explicitly NOT rolled into M6 or M7 scope, stays parked through M7 closure. Candidate next-work thread in the post-M7 study window.
- Silent-operator-outcome watchdog (FW-0039) — accepted-gap-for-v1 per D-0071 sub-decision 17 + Codex P1 round-2 fix; revisit triggers stay armed (operator pool growth beyond ~3-5, actual finalizer-crash OR callback-unreachable incident leaving an operator stranded > 1 hour, FW-0028 observability promotion, Google Workspace migration unlocking Gmail-API-via-SA at finalizer-side).
- Single-VM K upgrade K=88 → K=176 (FW-0040) — parameterized dial per Codex P1.7 amendment; revisit triggers stay armed (operator demand for more roster variants, FW-0036 plateau evidence shifting past K=20, cloud-mode LAHC broadening). K=88 vs K=176 is a quality wash per FW-0036's plateau evidence so the quota-bump investment waits for concrete demand.
- Broad generalization to additional departments before ICU/HD-first learning is closed.
- Pilot rollout to non-maintainer operators (broader pilot is a future milestone or operational rollout step, not currently scheduled).
