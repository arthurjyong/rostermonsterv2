# Delivery Plan

## 1. Purpose of this document
This is the **living execution guide** for Roster Monster v2.

It defines what is actively being delivered now (active milestone, active checkpoint, and supporting tasks) and what is intentionally next.

It does **not** replace:
- architecture truth in `docs/blueprint.md`
- milestone-level sequencing in `docs/roadmap.md`
- normative technical boundaries in contract docs
- issue-level implementation tracking

## 2. Planning vocabulary
- **Product**: the full end-to-end capability being built.
- **Milestone**: a major delivery state that advances product readiness.
- **Checkpoint**: a bounded, reviewable step inside a milestone.
- **Task**: a concrete work item used to close a checkpoint. Typical decomposition inside a checkpoint follows the **Task 1 (docs) → Task 2 (code, with optional Task 2A / 2B / 2C sub-letters when work has logically distinct chunks) → Task 3 (closure)** cadence per D-0064. Not a contract — a docs-only checkpoint may be just Task 1; an unusual checkpoint may have a different shape. Historical "Phase 1/2/3" labels in §11 closure entries (M2..M5 C2) are preserved as-is.

Working rule: this repo should normally maintain **one active milestone** and **one active checkpoint** at a time.

## 3. Product definition
Roster Monster v2 builds a reusable roster-allocation core with department-specific template control, while preserving Google Sheets as the operational front end. ICU/HD is the first concrete implementation target.

## 4. Product-level delivery principles
- Preserve Google Sheets as the operational front end.
- Keep architecture-first and contract-first boundaries where needed.
- Prioritize work that directly unblocks real operator workflow.
- Avoid reopening settled contracts unless there is a strong consistency reason.
- Avoid spending effort on items that do not support the active checkpoint.

## 5. Milestone map

### M1 — Operator-ready request sheet generation
- **Goal:** Deliver an operator-usable ICU/HD request sheet shell, backed by closed generation contract boundaries.
- **Status:** **Completed** *(closed 2026-04-21 on operator delivery; D-0019)*
- **Checkpoints:** C1 sheet-generation MVP boundary (closed 2026-04-17), C2 template artifact ↔ generation alignment (closed 2026-04-18), C3 acceptance/handoff readiness (closed 2026-04-18), C4 implement operator-ready sheet generation (closed 2026-04-21).

### M1.1 — Operator-facing launcher *(addendum to M1)*
- **Goal:** Provide a narrow operator-facing launcher so named monthly-rotation pilot operators can invoke empty ICU/HD request-sheet generation without running Apps Script by hand.
- **Status:** **Completed** *(closed 2026-04-22 on hands-on validation; M1 itself stays Completed, not reopened)*
- **Checkpoints:** C1 implement operator launcher web app (closed 2026-04-22).
- **Addendum framing:** M1.1 is derivative of M1's operator-facing surface, not a compute-line milestone. Addendum numbering convention (`M<parent>.<n>`) recorded in D-0021.

### M2 — Minimal local compute pipeline
- **Goal:** Stand up deterministic local parse/normalize/solve flow against closed contracts.
- **Status:** **Completed** *(closed 2026-04-29 on M2 C9 closure; full sheet → snapshot → Python pipeline runs end-to-end on real ICU/HD May 2026 data)*
- **Checkpoints:** C1 rule/scorer/solver contract closure (closed 2026-04-25); C2 selector contract closure (closed 2026-04-26); C3 parser/normalizer implementation (closed 2026-04-27); C4 minimal rule/scorer/solver integration (closed 2026-04-27); C5 local run artifact basics (closed 2026-04-28); C6 parser overlay for scoring config (closed 2026-04-28); C7 Scorer Config tab — sheet-generator half (closed 2026-04-28); C8 spacingPenalty geometric-decay shape (closed 2026-04-28); C9 snapshot extraction Apps Script — D-0036 (closed 2026-04-29). All nine closed.

### M3 — Safe result/output and writeback
- **Goal:** Define and implement safe output surfaces and writeback behavior.
- **Status:** **Completed** *(closed 2026-04-30 on M3 C1 closure; M3 C2 dropped per D-0048 — round-trip already proven, live-operator demo no longer the right next investment per D-0049)*
- **Checkpoints:** C1 writeback library + launcher route + Python CLI extension (closed 2026-04-30).

### M4 — Cloud end-to-end pipeline + dual-track preservation
- **Goal:** Deliver a cloud-deployed end-to-end pipeline so operators can drive a one-click roster-generation flow without local Python tooling, while preserving the existing local CLI as a maintainer-side dev-velocity surface for solver-strategy experiments.
- **Why it matters:** Pre-pilot political/visibility goal — boss-clickable proof of concept. Dual-track preservation matters because solver-strategy work (the next priority) needs the fast local-CLI iteration loop.
- **Status:** **Completed** *(closed 2026-05-01 on M4 C1 closure as the only delivered checkpoint; solver-strategy optimization promotes to its own forthcoming milestone rather than M4 C2 — different in character)*
- **Checkpoints:** C1 cloud end-to-end pipeline (minimum demo) (closed 2026-05-01).

### M5 — Operator-side analysis & multi-roster delivery
- **Goal:** Give operators the tools to see what's actually in a roster and compare alternatives, without changing the solver. Three-piece architecture (Python analyzer engine + `AnalyzerOutput` + Apps Script analyzer renderer) as a sibling consumer of the wrapper envelope, additive to the existing pipeline.
- **Why it matters:** Without analysis tooling, "did a future score-aware strategy improve quality" is unmeasurable from the operator's seat — today's only operator-facing signal is `totalScore` and the writeback tab. Analysis tooling is also the operator-side workaround for the weighted-sum scoring formulation pain (operator picks among K candidates with full component breakdowns rather than trusting a single scalar). Sequencing analysis BEFORE solver-side score-aware search (LAHC etc., parked in M6) means the calibration framework exists when the search-strategy work needs to be evaluated.
- **Status:** **Completed** *(closed 2026-05-07 on M5 C4 live operator validation verdict; D-0065 — analyzer's role as calibration framework validated; the comparison tab successfully surfaced a load-bearing scoring-formulation insight the operator would not have seen from `totalScore` alone)*
- **Checkpoints:** C1 Python analyzer engine + analysis contract draft (closed 2026-05-05; PR #110 contract draft + PR #111 implementation; D-0056..D-0058); C2 Apps Script analyzer renderer (closed 2026-05-06; PRs #114 contract draft + #115 implementation + #116 launcher nav header; D-0060..D-0063; cloud deployment `@15` — C3 upload-portal scope was wholly absorbed into C2's launcher Web App route per D-0063, so C3 is dropped from the plan); C4 live operator validation (closed 2026-05-07 on M5 closure PR; D-0065).

### M7 — Parallel solver (Cloud Batch + intra-request K-fanout)
- **Goal:** Parallelize K-trajectory LAHC execution so a sync Quick Solve at operator-meaningful K completes within the 6-min `UrlFetchApp` ceiling, escaping the GIL-bound single-thread bottleneck identified during M6 C4 cloud benchmarking. LAHC's K-trajectory independence per `docs/solver_contract.md` §12A.2 is the contract-level invariant that makes parallelism structurally permitted.
- **Why it matters:** Cloud SRB-K=32 baseline measured ~270s warm / ~283s cold per `docs/cloud_compute_contract.md` §8.4 — the GIL-bound single-thread cap means hardware bumps yield diminishing returns (4-vCPU bump only +10% per the M6 C4 benchmark). The LAHC cloud projection at K=10 already overshoots the 5-min Cloud Run timeout (~13 min before parallelism). Parallelism is the only path to operator-meaningful cloud LAHC at sync UX. Goal: K-trajectory LAHC at the FW-0037 elbow tuple (`L=50`, `idleThreshold=3500`, `swapProbability=0.5`) running ~110-145s wall time end-to-end on Cloud Batch within Cloud Run Service timeout 250s (lowered from default 300s per D-0070 sub-decision 5 to leave Apps Script wall budget for post-fetch writeback + renderAnalysis).
- **Status:** **Active** *(activated 2026-05-10 per D-0070)*
- **Checkpoints:** C1 specs locked + GCP quota request filed (active); C2 Cloud Batch implementation (container image + job spec + Cloud Run orchestrator wiring + determinism re-audit per §12A.4); C3 sync UX + bound shim wiring + analyzer integration + cloud_compute §9/§10 amendments (Cloud Run Service timeout LOWERED to 250s per D-0070 sub-decision 5; `Roster Monster → Solve Roster` menu integrates with new path; cloud_compute_contract §9 amended to add `solverStrategy` + `lahcParams` to `optionalConfig` request input; §10 amended to add OK-only `analyzerOutput` to response; sync result + writeback + renderAnalysis fits inside the 360s Apps Script script-execution wall-clock — exact-fit budget at tail: 10s extract + 250s cloud + 30s writeback + 60s renderAnalysis + 10s variance); C4 live operator validation + closure (the comparison-tab pathway D-0069 skipped on M6 C4; real ICU/HD cycle SRB vs LAHC via parallel path through the M5 analyzer).
- **Dependencies:** M6 closed (LAHC algorithm + tuning evidence pinned); GCP Compute Engine API enabled (done 2026-05-10); GCP regional quota approval for asia-southeast1 (`CPUS`: 100→2,520; `INSTANCES`: 24→350; `C3_CPUS`: 24→2,520) — prerequisite for C1 closure, not for C1 entry.
- **Exit criteria:** All four checkpoints closed; Cloud Batch path passes determinism re-audit per §12A.4 under multi-process execution; sync Quick Solve from bound shim returns within the Apps Script script-execution wall-clock (360s shared between extract + Cloud Run fetch + writeback) on real operator cycle; LAHC quality matches the M6 local-CLI baseline at the same `(masterSeed, K)` (byte-identical winners). M7 closure does NOT require K=2,500 specifically — if quota is approved at smaller K (300, 1,000), the architecture runs at that K and still hits the goal because best-of-K plateau is at K=10-20 per FW-0036 dev-copy evidence; quota size determines how much headroom over plateau, not whether plateau is reached.

### M6 — Solver-side score-aware search (LAHC)
- **Goal:** Deliver Late Acceptance Hill Climbing (LAHC) as the first alternative solver search strategy alongside today's `SEEDED_RANDOM_BLIND` per `docs/solver_contract.md` §11.1, addressing FW-0003's empirical score plateau on `SEEDED_RANDOM_BLIND`.
- **Why it matters:** Today's solver explores assignment space without score awareness; quality plateaus past ~100-200 candidates per FW-0003. LAHC's history-list accept criterion (move accepted iff score ≥ score from `L` iterations ago) escapes local optima while preserving determinism under fixed seed per D-0053. Strategy choice is recorded in the wrapper envelope so the M5 analyzer (per D-0066 sub-decision 7) makes LAHC's quality improvement (or lack thereof) visible — operator renders each strategy's `AnalyzerOutput` separately into the source spreadsheet (two comparison tabs land per D-0062 always-new-tab discipline) and manually cross-references between them. Single-tab side-by-side renderer enhancement is parked as FW-0034 (NOT in M6 scope).
- **Status:** **Completed** *(closed 2026-05-09 on M6 C4 verdict; D-0069 — LAHC empirically validated as superior to `SEEDED_RANDOM_BLIND` on the May 2026 ICU/HD dev-copy fixture: paired t(4)=11.10, p≪0.001, mean Δ +53.85 score points at comparable wall-time budgets; sweet-spot defaults established subject to multi-fixture validation per FW-0036/FW-0037: `L=10`, `idleThreshold=2000`, `swapProbability=0.5`, `K=10–20`)*
- **Checkpoints:** C1 solver-strategy contract extension + LAHC design (closed 2026-05-07 per D-0067); C2 LAHC core impl in Python (closed 2026-05-08); C3 envelope / CLI integration (closed 2026-05-08; local-first per D-0068; cloud-mode LAHC integration carved off to FW-0035); C4 live operator validation via M5 analyzer — closure scope shifted to dev-copy empirical characterization (closed 2026-05-09 per D-0069; the dominance signal was already overwhelming so the originally-framed operator-cycle comparison-tab cross-reference would have been ceremonial). C4 closure also surfaced **FW-0038** (parallel solver — M7 candidate framing) on the back of the cloud single-thread bottleneck identified during the cloud benchmark.
- **Dependencies:** M5 closed (analyzer + renderer end-to-end so LAHC quality is measurable from the operator's seat); current `SEEDED_RANDOM_BLIND` implementation in `python/rostermonster/solver/` (M2 C4 closure).
- **Exit criteria:** All four checkpoints closed; LAHC implementation passes unit + integration tests; LAHC's behavior is measurable + interpretable against `SEEDED_RANDOM_BLIND` via the M6 stack (analyzer's comparison tab, FULL-retention sidecars, and per-trajectory `SearchDiagnostics`). M6 closure does NOT require LAHC to strictly dominate `SEEDED_RANDOM_BLIND` — surfacing the comparison and learning what the data shows is the load-bearing exit criterion (per the M5-as-calibration-framework discipline). The originally-framed "live operator-cycle comparison-tab cross-reference" was the *expected* validation pathway at M6 activation; per D-0069 the criterion was satisfied via dev-copy empirical characterization (paired t-test + Wilcoxon + TOST equivalence + bootstrap stability across L / idleThreshold / move-mix sweeps) rather than the operator-cycle pathway, because the dev-copy signal was already overwhelming and a different load-bearing concern (cloud single-thread bottleneck → FW-0038) surfaced during the operator-availability window. **Verdict 2026-05-09 (D-0069):** all four checkpoints closed; tests green at every closure; the empirical signal on the dev-copy fixture is unambiguous so M6 closes here rather than waiting on the operator's next monthly cycle. Module-level defaults NOT pinned this milestone — empirical sweet-spot values held back as `--lahc-*` overrides until multi-fixture validation per FW-0036/FW-0037.

## 6. Current active milestone
- **Active milestone:** **none** *(M7 closed 2026-05-13 per D-0072 — live async UX validated end-to-end on both real ICU and HD operator cycles)*

M7 closure trail: **D-0070 → D-0071 → D-0072**. M7 closed 2026-05-13 per D-0072 with verdict: full async UX validated end-to-end on real live ICU + HD operator cycles. All four operator-facing exit criteria from §7 C5 passed — (a) bound shim's SUBMITTED-state toast fired post-click, (b) `[RosterMonsterV2] Success: <tab>` email arrived with the AnalyzerOutput JSON attachment intact, (c) writeback tab + 5 roster tabs rendered correctly, (d) **the analyzer comparison tab rendered**, closing the D-0069-deferred operator-cycle cross-reference pathway as a natural side-effect of M7 C4's inline-finalize-step analyzer plumbing. **FW-0027 (parallel operational search and orchestration), FW-0030 (cloud-side FULL retention support), FW-0035 (cloud-mode LAHC integration), and FW-0038 (parallel solver) all DELIVERED via M7** per D-0072 — status flips on each. FW-0039 (silent-operator-outcome watchdog) and FW-0040 (K=88 → K=176 single-VM dial) stay parked with their existing revisit triggers. Active-milestone-empty post-M7 mirrors the post-M4 (2026-05-01) / post-M6 (2026-05-09) cadence — deliberate study window before the next activation; no successor pre-decided. Candidate next-work threads include FW-0033 (scoring-weight elicitation), FW-0036/FW-0037 multi-fixture promotion (LAHC K-default + L-tuning protocol module-pinning waits for 2+ additional post-M7 cycles), FW-0028 (observability hardening), FW-0031/FW-0032 (analyzer extensions), or operational broadening beyond ICU/HD-first scope.

## 7. Checkpoint plan for the active milestone

No active milestone post-M7 closure (2026-05-13 per D-0072). Historical M7 checkpoint plan (C1–C5) is preserved in §11 closure entries. The next-milestone study window is deliberately empty per the post-M4 / post-M6 cadence; candidate next-work threads live in `docs/future_work.md` (FW-0028 / FW-0031 / FW-0032 / FW-0033 / FW-0036 / FW-0037 multi-fixture promotion / FW-0039 / FW-0040 + operational broadening).

<details>
<summary>Historical M7 C1–C5 plan (collapsed — preserved verbatim for audit; closure narratives in §11)</summary>

**Note:** M7 C3's scope was pivoted from "Sync UX implementation" to "Async architecture decision (D-0071)" on 2026-05-11 per the M7 C2 perftest evidence (sync 240s deadline brittle on real production rosters at K=104; FW-0027 condition (b) firing). The original D-0070 sync-UX plan (preserved below for the C1 / C2 entries which closed under that framing) is OBSOLETED by D-0071 for C3+C4+C5. The current C3/C4/C5 plan reflects the async pivot.

- **C1 — Specs locked + GCP quota request filed.** *Closed 2026-05-10; PR #136 activation + this closure PR.* Direction-setting docs PR (PR #136) pinned architecture (Cloud Batch + dense pack via `c3-highcpu-8` + `multiprocessing.Pool(8)` + on-demand + sync via Cloud Run Service timeout 250s per D-0070 sub-decision 5), machine family (C3 Sapphire Rapids — fastest per vCPU for pure-Python compute), `K_target=2,500`, FW-0037 elbow tuple, plus M7 C3 scope expansion to fold in cloud_compute §9 + §10 amendments + analyzer integration + bound shim renderAnalysis call (FW-0030 + FW-0035 absorbed into M7 C3). Quota request tickets filed in GCP Console + returned approvals: **CPUS approved at 300** (requested 2,520; partial), **INSTANCES approved at 350** (requested 350; full), **C3_CPUS approved at 108** (requested 2,520; partial — binding constraint at the C3 family). Per D-0070 sub-decision 7's three-quota rule: `K_approved = min(K_target=2,500, floor(min(300, 108, 350×8=2,800) / 8) × 8) = min(2,500, floor(108/8)×8) = min(2,500, 104) = **104**`. **Closure-K = 104** (13 tasks × 8 trajectories each, all fully packed; C3_CPUS=108 is binding). M7 C2's Batch job spec config will be set at `taskCount=13`, `parallelism=13`. K=104 is well past best-of-K plateau (K=10-20 per FW-0036 — ~5-10× margin over plateau), so the architecture validates fine at this scale; if maintainer wants larger K later, a follow-up C3_CPUS quota request (Google explicitly invites re-requests after first usage history accrues) lifts the cap. See §11 closure entry for full closure narrative.
- **C2 — Cloud Batch implementation.** Container image (`python/rostermonster_service/` extended with a `--worker-mode` entrypoint that pulls input snapshot + pre-computed seed slice from Cloud Storage, runs `multiprocessing.Pool(N)` over the N LAHC trajectories assigned to this VM by the Batch task index, writes results to Cloud Storage). Cloud Batch job spec (orchestrator-side API call: `taskCount = ceil(K_approved / 8)`, `parallelism = taskCount`, `taskCountPerNode = 1`, `c3-highcpu-8`, on-demand allocation, asia-southeast1, **per-task `maxRunDuration` 180s + `maxRetryCount` 1**; the **240s orchestrator-side completion deadline** is implemented as a Cloud Run polling loop + `batch.jobs.cancel` on overrun, NOT a job-spec field — see `docs/cloud_compute_contract.md` §8.7). **Task count + per-task trajectory slice are derived from K_approved (NOT raw approved vCPU count), per D-0070 sub-decision 7's derivation rule:** when K_approved hits K_target=2,500 (full quota approval ≥ 2,504), `taskCount=313` and the final task runs 4 trajectories (slice `[2496, 2500)` = 4 trajectories on cores 0..3, cores 4..7 idle — K_target isn't divisible by 8); when K_approved is below K_target (smaller approvals), the floor-to-multiple-of-8 rule guarantees K_approved is a multiple of 8 so all tasks fully pack — e.g., approval=300 → K_approved=296, taskCount=37 fully packed; approval=1,000 → K_approved=1,000, taskCount=125 fully packed; approval=108 → K_approved=104, taskCount=13 fully packed. The worker computes its trajectory range as `[task_index × 8, min((task_index + 1) × 8, K_approved))` so the total LAHC-emitted trajectory count exactly equals K_approved (matches local-CLI semantics) regardless of how the dense pack slices across tasks. **Seed derivation: orchestrator-side pre-computation via shared helper, not per-task.** Per D-0070 sub-decision 8: the current `_per_candidate_seed(rng: Random)` in `python/rostermonster/solver/solver.py:52` is NOT index-addressable (advances a single `Random(masterSeed & _UINT64_MASK)` stream sequentially via `rng.getrandbits(63)`). The unsigned-mask initialization at `solver.py:261` is load-bearing — it prevents CPython's `Random.seed(int)` `abs()` from aliasing `seed` and `-seed` for contract-valid negative seeds. M7 C2 introduces a new shared helper `derive_K_seeds(masterSeed: int, K: int) -> list[int]` at `python/rostermonster/solver/seeds.py` (separate module, refining D-0070 sub-decision 8's `solver.py` reference per `docs/solver_contract.md` §12A.10) that wraps the masking + sequential `_per_candidate_seed` loop in one place; both the local CLI's K-trajectory loop AND the Cloud Run Service orchestrator call this same helper. The orchestrator generates all K_approved seeds upfront via the helper (matching local-CLI byte-identity for ALL seeds including negatives), then serializes each task's assigned seed slice into Cloud Storage alongside the input snapshot. Each Batch task reads its pre-computed seed list rather than re-deriving from masterSeed. Cloud Run Service orchestrator wiring (call shared helper to get K seeds + per-task-slice serialization + submit-job + poll-for-completion + read-aggregated-results + best-of-K + return). Determinism re-audit per `docs/solver_contract.md` §12A.4 confirms same-master-seed runs across local-CLI vs Cloud-Batch produce byte-identical winners. C2 closes when end-to-end Batch job runs on dev-copy snapshot at K_approved, returns winner identical to local LAHC at same `(masterSeed, K_approved)`.
- **C3 — Async architecture decision (D-0071 + Codex P1.7 single-VM amendment)** *(activated 2026-05-11 on M7 C2 closure; scope pivoted from D-0070 sync-UX framing per M7 C2 perftest evidence; architecture amended from 13-VM + finalizer-task-group → single-VM + inline finalize on 2026-05-11 per Codex P1.7 fix — Cloud Batch v1 supports only one task group per job)*. Docs-only checkpoint per the same direction-setting cadence as M7 C1 (M5 C1, M3 C1, M2 C1 patterns). **D-0071 + Codex P1 fixes lock** the following 17 sub-decisions + multiple round fixes: (1) the C3 scope pivot from sync to async architecture decision; (2) Cloud Run = thin HTTP front door (validate + submit + return SUBMITTED ~3-5s) + Cloud Batch = **single task on one `c3-highcpu-88` VM running `Pool(88)` LAHC trajectories + inline finalize step in the same Python process after `Pool.close() + .join()`** (Codex P1.7 amendment — pre-amendment was 13-VM dense pack with finalizer-task-group, retired because Cloud Batch v1 doesn't support multi-task-group jobs); (3) result delivery via B-prime — the single task POSTs to a new launcher Web App callback `/exec?action=async-render-callback` reusing existing `RMLib.applyWriteback + RMLib.renderAnalysis`; (4) auth via GCP ID-token verification with token carried in POST body's `idToken` field (NOT `Authorization` header — Apps Script `doPost(e)` cannot read HTTP headers; Codex P1 fix) + minted via metadata server with `&format=full` (required for `email`/`email_verified` claims; Codex P1 round 13 fix); (5) email notification on every outcome with `[RosterMonsterV2] Success/Failure: <tab>` subject + technical-details block + AnalyzerOutput JSON attachment — sent by the launcher's `MailApp.sendEmail` inside the callback handler (NOT finalizer-side — Cloud Batch VM has no mail surface); (6) operator email captured at submit via `Session.getActiveUser().getEmail()` and threaded through; (7) **10-min operator-facing hard cap** enforced by finalize-step self-check at **510s elapsed** since `RM_SUBMIT_TIMESTAMP_MS` (reserves ~90s for the finalize step's aggregate+score+analyze+POST+email so operator-facing total wall stays ≤ 600s; Codex P2 round 12 fix), with Batch-level task `maxRunDuration: 660s` as a safety net (Codex P1 round 10 raised this from 540s after finding it could kill the task before the finalize step ran); (8) concurrent-run rejection by spreadsheet ID via Cloud Batch labels with explicit `(status.state=QUEUED OR status.state=SCHEDULED OR status.state=RUNNING)` filter (Cloud Batch doesn't support SQL `IN`; Codex P2 round 11 fix); (9) one-time toast post-submit ("Submitted, we'll email you, can take up to 10 minutes"); (10) always-email on every state where the callback POST succeeds — UNSATISFIED (K'=0 routes here per `docs/solver_contract.md` §12A.8) goes through writeback failure tab + unsatisfied email; OK goes through writeback + renderAnalysis + success email (BOTH return values checked first — failures route to failure email; Codex P2 round 13 fix); COMPUTE_ERROR routes to failure email only; callback-POST-unreachable falls into FW-0039 silent-outcome gap; (11) maintainer status via gcloud CLI (no operator-facing status surface at v1); (12) the finalize step runs inline after `Pool.join` so it always runs (NO `SUCCEEDED_OR_FAILED` dependency mechanic — Cloud Batch v1 doesn't support it; single-task pattern makes this structural rather than via dependency); (13) SRB cloud path hidden from operator menu, kept in code for benchmarks; (14) maintainer `/compute-lahc-test` debug route kept; (15) bound shim menu LAHC-only at M7 C4; (16) FW-0027 absorbed into M7 C3+C4 per this decision; (17) FW-0039 (silent-operator-outcome watchdog) covering both finalizer-crash + callback-unreachable accepted-gap-for-v1; new FW-0040 (K=88 → K=176 future quota-bump dial) per Codex P1.7 amendment. **`maxRetryCount: 0`** (Codex P2 round 8 fix — retry × 660s = 1320s blows the 10-min cap). C3 closes on this PR landing; active checkpoint pointer moves M7 C3 → M7 C4. **No code changes in C3** — D-0071 docs lock + Codex P1/P2 round fixes + supporting sweeps across roadmap / future_work / cloud_compute_contract / blueprint / README / AGENTS / project memory.
- **C4 — Async UX implementation (post-D-0071 PR series)** *(scope reframed from D-0070 sync-UX-implementation framing; new scope per D-0071; **architecture amended per Codex P1.6 + P1.7 fixes 2026-05-11**)*. Implements the architecture D-0071 locked. **Per Codex P1.7 fix**: single-VM dense pack (`c3-highcpu-88` × 1 with `multiprocessing.Pool(88)` for K=88 trajectories, finalize step inline after `Pool.join()`) replaces the original 13-VM two-task-group design — Cloud Batch v1's `Job.taskGroups[]` is limited to one task group, so workers + finalizer run on one VM in one Python process. K reduces 104 → 88 (still 4-9× past best-of-K plateau; quality impact negligible). **Per Codex P1.6 fix**: launcher gains a SECOND Web App deployment (USER_DEPLOYING, Access ANYONE, **version-pinned per Codex P2 round 1 finding 2** — NOT HEAD mode; HEAD-mode deployments auto-update on every `clasp push` which would let any inadvertent broken push immediately reach the production callback path) for the async-render-callback route only; operator-facing routes stay on the existing `@17` USER_ACCESSING deployment; appsscript.json manifest stays USER_ACCESSING (default), the new deployment's `executeAs: Me` is set via the Apps Script editor's Deploy dialog override at M7 C4 implementation. **Per Codex P1 from earlier review round**: OIDC ID token rides in the POST body's `idToken` field (NOT `Authorization` header — Apps Script Web App handlers cannot read HTTP request headers); launcher's callback handler also sends the operator email via `MailApp.sendEmail` since the finalizer on Cloud Batch VM has no mail surface. Files added/modified: `python/rostermonster_service/app.py` (Cloud Run front door — refactored to validate + submit + return SUBMITTED in ~3-5s; concurrent-rejection via `batch.jobs.list --filter=labels.spreadsheet_id` query); `python/rostermonster_service/worker.py` extended with inline finalize step after the Pool joins (aggregate + score + select + analyze + POST callback); `python/rostermonster_service/batch_job_spec.py` updated to ONE task group with one task on `c3-highcpu-88` + new env vars (`RM_SUBMIT_TIMESTAMP_MS`, `RM_OPERATOR_EMAIL`, `RM_LAUNCHER_CALLBACK_URL`); `python/rostermonster_service/lahc_orchestrator.py` deprecated (its ~500 LOC of orchestration logic moves into worker.py's inline finalize step); new `apps_script/launcher/src/AsyncRenderCallback.gs` route handler with GCP ID-token-in-body verification helper + `MailApp.sendEmail` for the operator email; `apps_script/bound_shim/src/Menu.gs` onClickHandler refactored from extract+POST+wait+render to extract+POST+toast+exit. `cloud_compute_contract.md` §9 + §10 + §8.7 amendments + new async-callback contract section implemented per their M7 C3 docs-lock spec (additive — no `contractVersion` bump). C4 closes when dev-copy snapshot runs end-to-end via the bound shim menu, email arrives, and the analysis tabs render correctly in the source spreadsheet.
- **C5 — Live operator validation + M7 closure** *(was C4)*. Real ICU/HD operator cycle. Operator clicks `Roster Monster → Solve Roster` → toast appears → operator goes off → 3-10 min later receives email with subject `[RosterMonsterV2] Success: <tab>` → opens spreadsheet → sees writeback tab + 5 roster tabs + 1 comparison tab. Confirms: full async flow works end-to-end on a real cycle, email arrives, tabs render correctly, AnalyzerOutput attachment is intact, concurrent-rejection works (operator double-clicks → second click gets sync error), parser rejection still surfaces synchronously up-front. M5 analyzer + renderer comparison-tab cross-reference (per D-0066 sub-decision 7): operator can compare against an SRB run by manually invoking the maintainer-only `/compute-lahc-test` debug route OR by running the local CLI's `--strategy SRB` path (SRB cloud path no longer wired into operator menu per D-0071 sub-decision 13). The live-operator validation pathway D-0069 skipped on M6 C4 closure (because the dev-copy signal was already overwhelming) lands here. Verdict closes M7. **FW-0035 (cloud-mode LAHC integration) + FW-0030 (cloud-side FULL retention support) co-promote here per D-0070 sub-decision 10** (unchanged by D-0071 — closure path still co-promotes both, just async now). **FW-0027 also closes here** per D-0071 sub-decision 16's absorption.

</details>

## 8. Current active checkpoint
- **Active checkpoint:** **none** *(M7 C5 closed 2026-05-13 per D-0072 — live async UX validated end-to-end on real ICU + HD operator cycles; M7 milestone closed same day)*

## 9. Task list for the current checkpoint

No active checkpoint. Post-M7 next-work study agenda is non-normative and lives in `docs/future_work.md` (candidate threads listed in §7 above), not §9.

<details>
<summary>Historical M7 C4 task plan (collapsed — preserved verbatim for audit; closure narratives in §11)</summary>

M7 C4 implements the architecture D-0071 locked at M7 C3. Code-only checkpoint (no `contractVersion` bumps — all contract amendments already landed in PR #147). Per the D-0064 cadence: Task 1 (docs — this PR's scope: per-PR breakdown + deploy sequencing) → Task 2A..2E (code, per-PR cadence) → Task 3 (closure when end-to-end async flow works on dev-copy snapshot).

- **Task 1 — Per-PR breakdown + deploy sequencing + project-memory updates (this PR's scope).** Lock the implementation plan. Files touched: `docs/delivery_plan.md` (§6 + §7 + §8 + §9 + §11 + §15); `docs/roadmap.md` (M7 entry C3→closed, C4→active); `README.md` + `AGENTS.md` (active checkpoint pointer); project memory `project_m2_c9_setup_ids.md` (M7 C4 deploy-time checklist for new env vars + 2nd launcher deployment). No code changes. No contract changes.
- **Task 2A — Cloud Batch single-task job spec + worker.py inline finalize step.** Refactor `python/rostermonster_service/batch_job_spec.py` from 13-task (`c3-highcpu-8` × 13, K=104) to single-task (`c3-highcpu-88` × 1, K=88) per the P1.7-amended §8.7 spec. Add env vars on the task: `RM_SUBMIT_TIMESTAMP_MS` + `RM_OPERATOR_EMAIL` + `RM_LAUNCHER_CALLBACK_URL`. Add normalized `labels.spreadsheet_id` for concurrent-rejection. Update `maxRunDuration: 660s` + `maxRetryCount: 0`. In `python/rostermonster_service/worker.py`: extend the worker module with the inline finalize step (aggregate the Pool's in-memory K-candidate list + score + select + analyzer + POST callback) after `Pool.close() + .join()` per §8.7 Finalizer-inline pattern. Most of the ~500 LOC moves from `python/rostermonster_service/lahc_orchestrator.py`'s aggregate+score+select+analyze code path into worker.py. Add finalize-step total-wall self-check at 510s elapsed since `RM_SUBMIT_TIMESTAMP_MS` per §8.7 sub-decision 7 + §10A.7 timeout-failure-callback semantics. **Test surface:** byte-identity audit at K=88 vs local CLI K=88 LAHC at same `(masterSeed, K)`; unit tests for the finalize step's state-dispatch (OK / UNSATISFIED on K'=0 / COMPUTE_ERROR on raised); existing `/compute-lahc-test` maintainer route exercises the full flow without operator-facing surface. **No new operator-facing surface lands.**
- **Task 2B — Launcher SECOND Web App deployment + AsyncRenderCallback.gs route + OAuth scope additions + deployment-URL gate (Codex P1 round 2 finding 4).** Add TWO new scopes to `apps_script/launcher/src/appsscript.json` `oauthScopes` (Codex P2 round 1 finding 1 fix — verified the launcher manifest currently has only `spreadsheets` / `drive` / `userinfo.email`): `https://www.googleapis.com/auth/script.external_request` (for `UrlFetchApp.fetch(...)` to Google's tokeninfo endpoint — launcher doesn't currently have this scope, even though the bound shim does for its Cloud Run calls) + `https://www.googleapis.com/auth/script.send_mail` (for `MailApp.sendEmail`). Add new `apps_script/launcher/src/AsyncRenderCallback.gs` route handler per §10A.1 + §10A.5 + §10A.6: read POST body → JSON-parse → extract `idToken` → validate via tokeninfo (`UrlFetchApp.fetch`) checking `aud` = callback deployment URL, `email` = Compute SA email, `email_verified` = true → strip `idToken` from body before any logging → dispatch on `state` (OK: applyWriteback + renderAnalysis + check both return values + success email; UNSATISFIED: applyWriteback only + check return value + unsatisfied email; COMPUTE_ERROR: failure email only) per §10A.7. Operator gets re-consent prompt on first use after the new scopes deploy. **Deployment-URL gate (Codex P1 round 2 finding 4 fix — security-critical):** `Execute as: Me` + `Access: Anyone` on the new deployment applies to the ENTIRE Apps Script Web App, not just the `?action=async-render-callback` route — without a gate, anyone with the new deployment URL could invoke the operator-facing default-route sheet generation OR `?action=writeback` OR `?action=analysis-render` and execute under the script owner's Drive/Sheets privileges. **Required defensive gate at the top of `doGet(e)` + `doPost(e)` in `Launcher.gs`:** read `ScriptApp.getService().getUrl()`; read `PropertiesService.getScriptProperties().getProperty('CALLBACK_DEPLOYMENT_URL')` (set during T2B deploy ceremony). If the two match (running under the callback deployment), then:
- **`doGet(e)`**: REJECT ALL requests outright with a structured `{state: "INVALID_DEPLOYMENT", code: "GET_NOT_ALLOWED_ON_CALLBACK_DEPLOYMENT"}` response. The callback handler ONLY accepts POST from the finalizer task; allowing GET (even with `?action=async-render-callback`) would fall through `doGet`'s existing dispatch which has no callback branch and lands on the default launcher form — its `google.script.run` handlers would then execute under the script owner's privileges. Reject GETs unconditionally on this deployment to close that fall-through hole (Codex P1 round 3 finding 5 fix).
- **`doPost(e)`**: if `e.parameter.action !== 'async-render-callback'`, REJECT with `{state: "INVALID_DEPLOYMENT", code: "NON_CALLBACK_ROUTE_REJECTED"}`. Only the callback action is allowed on the USER_DEPLOYING deployment.

The existing operator-facing routes (default sheet-gen, writeback form, analysis-render form) continue to work via the USER_ACCESSING deployment unchanged — those routes are GET-driven and only reachable via the original launcher URL, not the new callback deployment. This isolates the callback route's elevated privileges to its single intended use case (POST from the Cloud Batch finalizer). Create the new launcher Web App deployment via Apps Script editor's Deploy → New deployment dialog: `Execute as: Me` (USER_DEPLOYING) + `Access: Anyone` + **pin to a specific version (NOT HEAD mode — Codex P2 round 1 finding 2 fix)**. HEAD-mode deployments auto-update on every `clasp push`, which means any inadvertent broken push immediately reaches the production callback path. Version-pinning (e.g., `@1`, then `@2` on each operator-facing change) mirrors the existing operator-deployment `@17` discipline + gives the maintainer an explicit "bump the deployment when callback behavior changes" gate. After pinning, capture the deployment URL + set Script Property `CALLBACK_DEPLOYMENT_URL` to it (this is what the gate above reads). **Test surface:** manual smoke test — finalizer-impersonator curl call from a maintainer terminal that mints a fresh ID token via metadata server, POSTs synthetic callback payload, verifies the route handler renders tabs + sends email; ADDITIONALLY, smoke test that hitting the new deployment URL via GET (with any action, including `?action=async-render-callback`) returns GET_NOT_ALLOWED_ON_CALLBACK_DEPLOYMENT, NOT any form HTML; AND that POST to the new deployment URL with `?action=writeback` (or any non-callback action) returns NON_CALLBACK_ROUTE_REJECTED. Project memory `project_m2_c9_setup_ids.md` records the new deployment ID + URL + initial pinned version + the `CALLBACK_DEPLOYMENT_URL` Script Property setup.
- **Task 2C — Bound shim Menu.gs defensive async-state handler (lands BEFORE T2D per Codex P2 round 1 finding 3).** Refactor `apps_script/bound_shim/src/Menu.gs` `onClickHandler` to accept the SUBMITTED state shape while ALSO retaining the existing OK / UNSATISFIED handlers. POST body additions (per Codex P2 round 3 finding 6 + cloud_compute §9.3 + D-0071 sub-decision 13): (a) `operatorEmail` field sourced from `Session.getActiveUser().getEmail()` (required for LAHC strategy path per §9.3); (b) `optionalConfig.solverStrategy = "LAHC"` to dispatch into the async Cloud Batch path — without this, cloud_compute §9.3 defaults `solverStrategy` to `SEEDED_RANDOM_BLIND` which stays on the existing sync in-process compute path and the operator never enters the async flow. `optionalConfig.lahcParams` can be omitted; service falls back to the FW-0037 elbow tuple worker-hardcoded defaults per §9.3. Cloud Run still serves the OLD sync responses at this point — sending the new fields is harmless (Cloud Run continues to dispatch to the OLD code paths until T2D's refactor consumes the new `solverStrategy` field). Dispatch on `response.state`: SUBMITTED → toast ("Submitted, we'll email you, can take up to 10 minutes") → exit (new path); OK / UNSATISFIED → existing writeback + renderAnalysis sync flow (preserved); INPUT_ERROR / COMPUTE_ERROR → existing sync-error dialog (preserved). **Critical sequencing rationale (Codex P2 round 1 finding 3 fix):** the pre-amendment ordering had T2C as the Cloud Run refactor + T2D as the bound shim refactor; between T2C and T2D, every operator click would hit `CLOUD_UNEXPECTED_STATE` because the OLD bound shim's `Menu.gs:173-176` only handles OK / UNSATISFIED and throws on anything else (SUBMITTED included). Reordering T2C → T2D as the defensive bound shim first means T2C can safely change the Cloud Run response shape AFTER the bound shim already accepts it. **Test surface:** unit tests for the new SUBMITTED-state dispatch + back-compat for OK/UNSATISFIED + INPUT_ERROR/COMPUTE_ERROR; manual smoke test in dev-copy with OLD Cloud Run (continues to work — SUBMITTED branch not yet exercised, and the OLD Cloud Run code ignores `solverStrategy` in the OLD sync code path) confirming no regression.
- **Task 2D — Cloud Run `/compute` thin front-door refactor.** Refactor `python/rostermonster_service/app.py` `/compute` route: validate input (snapshot shape + operatorEmail required for LAHC + email match against OIDC token) → concurrent-rejection query (`batch.jobs.list --filter='labels.spreadsheet_id=<normalized> AND (status.state=QUEUED OR status.state=SCHEDULED OR status.state=RUNNING)'`; reject with INPUT_ERROR/CONCURRENT_RUN_REJECTED if non-empty) → submit Cloud Batch job (new single-task job spec from T2A) → return `{state: "SUBMITTED", submission: {batchJobName, jobId, runId}}` in ~3-5s. Remove the polling + aggregation + scoring + analyzer logic from Cloud Run (moved to worker.py inline finalize in T2A). Deprecate `python/rostermonster_service/lahc_orchestrator.py` (file moves to historical comment / removed entirely depending on whether anything still references it). Set Cloud Run deploy-time env var `RM_LAUNCHER_CALLBACK_URL` to the T2B deployment URL. **Operator-facing surface goes async at this PR landing — SAFE because T2C already shipped the defensive bound shim handling SUBMITTED.** **Test surface:** SUBMITTED-response shape unit tests; concurrent-rejection unit tests against mocked `batch.jobs.list` results; integration test via curl with operator-allowlisted token; end-to-end smoke test via dev-copy snapshot through bound shim menu (toast appears + email arrives + tabs render).
- **Task 2E (optional, may fold into T2D closure) — Project memory + cleanup.** Update `project_m2_c9_setup_ids.md` with: M7 C4 deploy-time env vars (RM_LAUNCHER_CALLBACK_URL, RM_SUBMIT_TIMESTAMP_MS plumbing); new launcher deployment ID + URL; Cloud Run revision sequence. Clean up any stale code paths discovered during M7 C4 implementation.
- **Task 3 — Closure.** When T2D's dev-copy snapshot run succeeds end-to-end (toast + email + tabs render correctly + concurrent-rejection works + parser rejection still synchronous), M7 C4 closes. Active checkpoint pointer moves M7 C4 → **M7 C5** (live operator validation on real ICU/HD cycle per §7's C5 description).

**Deploy sequencing rationale (amended at Codex P2 round 1 finding 3 — T2C/T2D reordered to eliminate broken-window risk):**
1. **T2A first** — pure code-only change to Python compute core; verifiable via the maintainer-only `/compute-lahc-test` route per D-0071 sub-decision 14; no operator-facing surface change; can land + soak in production behind the test route before exposing to operators.
2. **T2B second** — launcher SECOND Web App deployment with USER_DEPLOYING + version-pinned + AsyncRenderCallback.gs route + OAuth scope additions. Cloud Run's `RM_LAUNCHER_CALLBACK_URL` env var needs T2B's URL before T2D's Cloud Run refactor.
3. **T2C third (defensive bound shim) — lands BEFORE T2D, NOT after.** Pre-amendment ordering had T2C as the Cloud Run refactor + T2D as the bound shim refactor; that ordering created a known-broken window between T2C and T2D where the bound shim's `Menu.gs:173-176` `CLOUD_UNEXPECTED_STATE` handler would fire on every operator click (the OLD bound shim only handles OK/UNSATISFIED and throws on SUBMITTED). The amendment reorders so the bound shim ships SUBMITTED-aware (alongside back-compat for OK/UNSATISFIED) FIRST, then T2D safely changes the Cloud Run response shape. T2C is testable against OLD Cloud Run via dev-copy snapshot (SUBMITTED branch not yet exercised; OK/UNSATISFIED path continues to work end-to-end).
4. **T2D fourth (Cloud Run async cutover)** — operator-facing surface transitions from sync to async at T2D's Cloud Run deploy. **Safe because T2C already shipped the defensive bound shim handling SUBMITTED.** Verify via dev-copy snapshot end-to-end run.
5. **T2E optional** — can fold into T2D's closure narrative if scope is small.

</details>

## 10. Explicitly deferred for now
- Solver implementation details.
- Scorer implementation details.
- Local run implementation mechanics.
- Worker/orchestrator mechanics.
- Benchmark hardening depth beyond milestone-level framing.
- Broad multi-department generalization beyond ICU/HD-first sequencing.
- Public or open-signup operator access for the launcher; first-release scope is named monthly-rotation pilot operators only, gated via GCP OAuth consent-screen Test Users.
- In-app operator allowlist / role model inside the launcher; access gating stays external to the app for pilot scope.
- Operator-editable template or structural mapping; template stays maintainer-owned.
- Persisted per-operator state beyond Google's OAuth session.
- Alternative launcher platforms (for example a static page over the Apps Script API Executable); the pilot sticks with Apps Script Web App per D-0022.

## 11. Recently completed checkpoints

> Closure entries are intentionally short — what closed, when, with which PRs and decisions, plus a one-line summary of what works now. Full audit detail (phase-by-phase narrative, Codex findings, implementation files, test counts, "Main affected surfaces" lists) lives in the merged PR descriptions and git history; one-time setup steps live in operator READMEs and project memory.

- **2026-05-13 (M7 C5 + M7 C4 + M7 milestone closure, this PR):** Closed **M7 C5 — Live operator validation** + **M7 C4 — Async UX implementation** + **M7** (the milestone) per **D-0072**. Maintainer ran the full async path on real live operator cycles — one ICU and one HD — and confirmed all four §7 C5 exit criteria: (a) bound shim's SUBMITTED-state toast fired post-click, (b) `[RosterMonsterV2] Success: <tab>` email arrived with the AnalyzerOutput JSON attachment intact, (c) writeback tab + 5 roster tabs rendered correctly in the source spreadsheet, (d) **the analyzer comparison tab rendered** — closing the D-0069-deferred operator-cycle cross-reference pathway as a natural side-effect of M7 C4's inline-finalize-step analyzer plumbing (per D-0071 sub-decision 2 + Codex P1.7 amendment: aggregate + score + select + analyzer + POST callback all run inline in `worker.py` after `Pool.close() + .join()`). Two-cycle validation strengthens single-fixture grounding from M6 C4 dev-copy + M7 C2 byte-identity audit to **multi-fixture live-cycle confirmation**. **M7 C4 formally closes here** (the prior M7 C4 T2E entry on 2026-05-12 closed only the code-implementation track; the M7 C4 checkpoint itself stayed Active pending T3 — the operator-facing dry run — which has now landed via the live ICU+HD cycles). **M7 C5 closes here** (it was the M7-closing checkpoint). **M7 closes here** — all 5 checkpoints closed (C1 2026-05-10 / C2 2026-05-11 / C3 2026-05-12 / C4 2026-05-13 / C5 2026-05-13). **FW-0027 + FW-0030 + FW-0035 + FW-0038 all DELIVERED via M7 per D-0072** — status flips on each (FW-0027 was "absorbed into M7 C3+C4 per D-0071"; FW-0030 + FW-0035 were "absorbed into M7 C3 per D-0070"; FW-0038 was "promoted to M7 C1 implementation target per D-0070"). FW-0039 (silent-operator-outcome watchdog) + FW-0040 (K=88 → K=176 dial) stay parked with their existing revisit triggers. Stretch exit criteria from §7 C5 (concurrent-rejection works on operator double-click; parser-rejection still surfaces synchronously) have unit-test coverage from M7 C4 T2D PR #157 (30 new tests covering happy SUBMITTED + each validation surface) but are not exhaustively validated on this live cycle — not load-bearing for the closure verdict because the operator-facing happy path is what M7 C5 was designed to prove; any future regression lands as post-M7 bugfix, not re-opened M7 checkpoint. **Active milestone pointer M7 → none**; **active checkpoint pointer M7 C5 → none**. Post-M7 cadence mirrors post-M4 / post-M6 — deliberate study window; candidate next-work threads cataloged in §7. README / AGENTS / blueprint / roadmap / decision_log / future_work / delivery_plan all swept. **No `contractVersion` bumps** anywhere across M7 C4 + C5 — all amendments additive per cloud_compute §11 bump rule (settled at M7 C3 PR #147; M7 C4 + C5 added no contract surface).

- **2026-05-12 (M7 C4 Task 2E — cleanup + memory log, this PR):** Closes M7 C4's code-implementation track (T2A.2 + T2B + T2C + T2D all merged). T2E is in-repo cleanup-only — no operator-facing surface changes. Surveyed the service modules for dead code paths after the M7 C2→C3→C4 architectural pivots; no executable dead code found (the `c3-highcpu-8` / `K=104` / per-task `seeds.json` references that remain are all in module docstrings explicitly tagged "RETIRED" or "single-task" as historical context for future readers). Maintainer-side memory file `project_m2_c9_setup_ids.md` updated out-of-repo with the new M7 C4 deploy-time env-var surface: `RM_LAUNCHER_CALLBACK_URL` (Cloud Run env pointing at the launcher's USER_DEPLOYING callback deployment URL), `LAHC_K_APPROVED` cap (≤ 88 = c3-highcpu-88 vCPU count per round-10 Codex fix on PR #157), `LAHC_BUCKET` / `LAHC_BATCH_REGION` (both must be non-empty post-round-3 Codex fix on PR #157). Maintainer post-deploy sequence (out-of-band, NOT in this PR — order matters: bound-shim push lands BEFORE Cloud Run cutover per delivery_plan §9 Codex P2 round 1 finding 3 + Codex P1 finding on this PR's first review — without that ordering, operator clicks between Cloud Run cutover and bound-shim re-deploy receive `SUBMITTED` from the new front door but hit the OLD bound-shim's `CLOUD_UNEXPECTED_STATE` throw): (1) `clasp push` the launcher project → Apps Script editor `Deploy → New deployment → Web app → Execute as: Me + Access: Anyone` → version-pin → record the new `/exec` URL; (2) `PropertiesService.getScriptProperties().setProperty('CALLBACK_DEPLOYMENT_URL', '<new URL>')` via Apps Script editor; (3) `clasp push` the SUBMITTED-aware bound shim (operator clicks now toast + email-wait); (4) ONLY THEN deploy Cloud Run with `--update-env-vars RM_LAUNCHER_CALLBACK_URL=<URL>,LAHC_K_APPROVED=88,LAHC_BUCKET=rostermonsterv2-lahc,LAHC_BATCH_REGION=asia-southeast1` (per `gcloud run deploy` reference: `--update-env-vars` is the ADDITIVE form; `--set-env-vars` REPLACES the entire env-var set including the M4 C1 production essentials `ALLOWED_EMAILS` + `CONTAINER_IMAGE_URI` + `GCP_PROJECT` — Codex P1 round 2 finding on PR #158) — operator path flips async on this `gcloud run deploy` landing. **M7 C4 code-implementation track closes on this PR landing**; the M7 C4 checkpoint itself stays Active pending T3 (operator-facing dry run via dev-copy snapshot + email verification + tab render confirmation per §9 Task 3 closure gate). Active-checkpoint pointer remains M7 C4 until T3 succeeds. Files touched (in-repo): `docs/delivery_plan.md` §11 entry only — no code changes. **No `contractVersion` bumps** anywhere across M7 C4 — additive surface only per cloud_compute §11 bump rule.

- **2026-05-12 (M7 C4 Task 2D — Cloud Run `/compute` thin front-door + bound shim LAHC cutover):** Landed in PR #157 (squash-merged as commit `5fadaf9`) after **10 rounds of Codex review** (10 P2 findings across the chain, all fixed inline). Operator-facing async cutover: when `optionalConfig.solverStrategy=LAHC`, Cloud Run validates input + checks concurrent-rejection via `batch.jobs.list --filter` + submits Cloud Batch job + returns `SUBMITTED` in ~3-5s; actual compute runs in worker.py inline finalize (T2A.2 PR-A); launcher callback (T2B) delivers writeback + analyzer + operator email asynchronously. SRB sync path unchanged. **Codex P2 rounds:** R1 — unknown solverStrategy values + non-positive `LAHC_K_APPROVED`; R2 — null/empty solverStrategy + explicit `lahcParams` (deferred — not plumbed through; rejected pending follow-up); R3 — non-string `metadata.snapshotId` / `metadata.sourceSpreadsheetId` + empty `LAHC_BUCKET` / `LAHC_BATCH_REGION`; R4 — uncatchable `derive_run_id` ValueError on unsanitizable snapshotId; R5 — explicit null `sourceSpreadsheetId`; R6 — out-of-int64-range seeds; R7 — `parse()` raising on malformed nested fields; R8 — `maxCandidates` honoring (override deploy-time K_approved); R9 — `maxCandidates` cap at K_approved (post-honor over-subscription guard); R10 — `LAHC_K_APPROVED` cap at the fixed VM vCPU count (`_LAHC_VM_VCPU_COUNT = 88`). `bound_shim/Menu.gs` request body now carries `solverStrategy: 'LAHC'` + `operatorEmail: Session.getActiveUser().getEmail()` per D-0071 sub-decisions 6 + 13. New `batch_client.py` `list_jobs(*, project, region, filter_str)` + `InMemoryBatchClient` test fake. Files: `app.py` (+~700 LOC), `batch_client.py` (+81), `bound_shim/Menu.gs` (+24), `test_service.py` (+~960 — 30 new tests covering happy SUBMITTED + each validation surface). Default pytest: 45 tests pass in 91s in test_service.py alone; full sweep 147 passed / 4 deselected.

- **2026-05-12 (M7 C4 Task 2C — bound shim Menu.gs defensive async-state handler):** Landed in PR #156 (squash-merged as commit `81b8fdb`) — clean Codex 👍 on first review. Stage-4 dispatch in `_solveRoster_()` extended with `state === 'SUBMITTED'` → returns `{kind: 'SUBMITTED_ASYNC', cloudState: 'SUBMITTED', submission}`. `_showSolveRosterResult_()` renders SUBMITTED_ASYNC as `SpreadsheetApp.getActiveSpreadsheet().toast("We'll email you when the roster is ready — can take up to 10 minutes.", "Submitted", 10)` per D-0071 sub-decision 9 (one-time non-blocking toast). OK / UNSATISFIED / INPUT_ERROR / COMPUTE_ERROR back-compat preserved (pre-T2D sync path still works). NO request-body change here — T2D flipped strategy to LAHC + threaded operatorEmail through. Lands BEFORE T2D per Codex P2 round 1 finding 3 ordering fix on PR #148 (the OLD `Menu.gs:173-176 CLOUD_UNEXPECTED_STATE` handler would have fired on every operator click between Cloud Run cutover and bound shim re-deploy without this defensive handler). Files: `apps_script/bound_shim/src/Menu.gs` (+41 lines).

- **2026-05-12 (M7 C4 Task 2B — launcher async-render-callback handler + deployment-URL gate):** Landed in PR #154 (squash-merged as commit `b43448a`) + hotfix PR #155 (squash-merged as commit `a5f0a37`). PR #154 added `apps_script/launcher/src/AsyncRenderCallback.gs` (token validation via tokeninfo + idempotency dedup + state dispatch + `MailApp.sendEmail` per state) + `Launcher.gs` `doPost` + `_isCallbackDeployment_` deployment-URL gate (§10A.3 security-critical per Codex P1 round 5 finding 9) + `appsscript.json` `script.external_request` + `script.send_mail` OAuth scopes. PR #154 Codex iteration: P1 — `RMLib` success-state predicates (writeback returns `SUCCESS` on success-tab + `FAILED` on failure-tab-= success-outcome for UNSATISFIED; renderAnalysis returns `OK` not `SUCCESS`); P2 — `200 + body.state` rejection inspection on the finalizer side (Apps Script Web Apps can only emit 200 or 500 via throw; terminal rejection states surface via body). PR #155 hotfix caught a P1 on `b43448a` post-merge (I merged on 0-comment heuristic before user feedback updated my polling rule to require explicit 👍): `MailApp.sendEmail` `attachments` requires `BlobSource[]` not plain dicts — fix via `Utilities.newBlob(data, mimeType, fileName)` helper. Memory `feedback_codex_wait_for_thumbs_up.md` added during the hotfix to record the merge-discipline correction. Files: `apps_script/launcher/src/AsyncRenderCallback.gs` (new, 700 LOC), `Launcher.gs` (+57 lines), `appsscript.json` (OAuth scopes), `python/rostermonster_service/worker.py` (HttpPostFn signature → `(status, body)` for terminal-state inspection), `test_worker.py` (+ 2 tests for 200-with-rejection-body retry semantics).

- **2026-05-12 (M7 C4 Task 2A.2 — worker.py inline finalize step + lahc_orchestrator simplification + K=88 audit, 3-PR split):** Landed Task 2A's deliverable across PR #151 (T2A.2 PR-A — worker.py inline finalize step + post_aggregation helper extraction), PR #152 (T2A.2 PR-B — lahc_orchestrator simplification, drop multi-task fossils), and this PR (T2A.2 PR-C — K=88 byte-identity audit variant + closure docs). **PR-A scope:** new `python/rostermonster_service/post_aggregation.py` shared helper for the score/select/wrapper pipeline (extracted from `lahc_orchestrator.py`); `worker.py` extended with `_inline_finalize` that runs after `Pool.close() + .join()` per §8.7 finalizer-inline pattern (Codex P1.7 amendment) — 510s self-check on `RM_SUBMIT_TIMESTAMP_MS`, state dispatch (OK / UNSATISFIED on K'=0-via-SEED_FAILED / COMPUTE_ERROR on K'=0-via-EXCEPTIONS or analyzer/aggregator raise), §10A.6-schema callback POST to `RM_LAUNCHER_CALLBACK_URL` with §10A.7 retry (3 retries, 2/4/8s exponential backoff on 5xx + transport exceptions; terminal on 4xx). ID token minted per attempt via GCP metadata server with `&format=full`. CLI env plumbing: `RM_OPERATOR_EMAIL` + `RM_LAUNCHER_CALLBACK_URL` + `RM_SUBMIT_TIMESTAMP_MS` + `BATCH_JOB_NAME` (auto-injected by Cloud Batch) + `RM_ANALYZER_TOP_K`. Empty `callback_url` short-circuits the POST (test-path + maintainer `/compute-lahc-test` back-compat per D-0071 sub-decision 14). **PR-B scope:** dropped back-compat aliases for `_assignment_unit_from_dict` / `_build_post_aggregation_envelope` / `_build_unsatisfied_from_aggregation`; dropped multi-task fossils `TRAJECTORIES_PER_TASK` / `_partition_seeds` / `_aggregate_results` retired at T2A.1. Net `-143 / +46` lines on orchestrator. **PR-C scope (this PR):** added K=88 audit variant `test_local_cli_and_cloud_batch_byte_identical_at_K_88` in `test_lahc_determinism_audit.py` (parameterized `_run_cloud_batch_path` + `_run_local_cli_path` on K; module-level `pytestmark = pytest.mark.slow` keeps default `pytest` from selecting); refactor adds the production-parity audit hook without changing the K=4 default. The K=88 audit closes the explicit T2A exit criterion ("byte-identity audit at K=88 vs local CLI K=88 LAHC at same `(masterSeed, K)`"); the byte-identity property is K-independent under §12A.10 + `_candidate_seeds` (per-trajectory independence), so the K=4 audit already proves the invariant — the K=88 variant adds production-parity confirmation runnable manually via `pytest -m slow tests/test_lahc_determinism_audit.py::test_local_cli_and_cloud_batch_byte_identical_at_K_88`. **Codex iteration across all 3 PRs:** PR-A — 3 P2 rounds (all-EXCEPTION K'==0 → COMPUTE_ERROR not UNSATISFIED; timeout callback uses actual K' from agg_result; CLI threads `BATCH_JOB_NAME` env per Cloud Batch docs); PR-B — clean 👍 first round; PR-C — TBD on push. Default pytest sweep: **115 passed, 4 deselected in ~9.5s** (was 112/3 pre-PR-C — added 1 K=88 slow test + 3 PR-A finds × 1 new test each + 1 K=88 entry to slow markers). Files added (production): `python/rostermonster_service/post_aggregation.py` (413 LOC); files modified: `python/rostermonster_service/worker.py` (+~750 LOC), `python/rostermonster_service/lahc_orchestrator.py` (-365 / +46 LOC), `python/tests/test_worker.py` (+~650 LOC), `python/tests/test_lahc_orchestrator.py` (back-compat sweep), `python/tests/test_lahc_determinism_audit.py` (K=88 parameterization + new test), `python/tests/test_post_aggregation.py` (new, 378 LOC). **Active checkpoint stays M7 C4** pending Task 2B (launcher SECOND deployment), 2C (bound shim defensive handler), 2D (Cloud Run thin front door), 2E (optional cleanup), then M7 C4 closure (T3).
- **2026-05-12 (M7 C4 Task 2A.1 + Task 2A.1.1 — Cloud Batch single-task + worker Pool(K) + test rewrites, PRs #149 / #150):** T2A.1 (PR #149) refactored `batch_job_spec.py` from 13-task `c3-highcpu-8` dense pack to single-task `c3-highcpu-88` × 1 (Codex P1.7 amendment); `worker.py` `worker_main` rewritten to take `master_seed` + `K_approved` kwargs and derive K seeds locally via `derive_K_seeds(master_seed, K_approved)` per `docs/solver_contract.md` §12A.10. New env vars on Batch task: `RM_MASTER_SEED`, `RM_K_APPROVED`, `RM_ATTEMPT_ID`, `RM_OPERATOR_EMAIL`, `RM_LAUNCHER_CALLBACK_URL`, `RM_SUBMIT_TIMESTAMP_MS`. `LahcOrchestrator._aggregate_single_task_result` reads single `gs://bucket/runId/result.json` (no per-task subdirs); attempt_id validation closes the concurrent-replay race. `_LAHC_DEFAULT_K_APPROVED = 88` matches Pool capacity. 2 rounds of Codex review caught: per-trajectory lookup losing seeds 8..K-1 under single-task (fix: candidateSeed-only lookup); `time.monotonic` process-local vs `time.time` wall-clock for `RM_SUBMIT_TIMESTAMP_MS` (fix: separate `wall_time_fn=time.time` param); cross-CALL concurrent-replay race on deterministic runId (fix: attempt_id re-introduced after initial drop). T2A.1.1 (PR #150) rewrote the 47 tests that PR #149 marked skipped (test_worker.py + test_lahc_orchestrator.py + test_lahc_determinism_audit.py) for the new single-task Pool(K) shape — 4 commits across 3 Codex P2 rounds: initial 50-test rewrite (97/97 pass); `slow` marker + addopts for orchestrator integration + audit (Codex P2 #1 — fast PR validation); stub executor for worker tests + `slow` on 3 tests genuinely needing real solver (Codex P2 #2); root `pytest.ini` so deselection works from repo root not just from `python/` (Codex P2 #3). Default PR pytest: 89 passed / 8 deselected in 9.28s (was 22:33 pre-fixes).
- **2026-05-12 (M7 C4 Task 1 — implementation-plan docs lock, this PR):** Locked the M7 C4 per-PR breakdown + deploy sequencing on top of the D-0071 architecture pinned in M7 C3. Cadence per D-0064: T1 docs (this PR) → T2A..T2E code (per-PR) → T3 closure. **Task ordering with rationale (see §9 for full per-task scope; ordering amended at Codex P2 round 1 finding 3 — T2C/T2D swapped to land bound shim BEFORE Cloud Run cutover):** T2A (Cloud Batch single-task job spec + `worker.py` inline finalize step) first — pure code-only change verifiable via the maintainer-only `/compute-lahc-test` route per D-0071 sub-decision 14, no operator-facing surface change; T2B (launcher SECOND deployment with `executeAs: USER_DEPLOYING` + `AsyncRenderCallback.gs` route + `script.external_request` + `script.send_mail` OAuth scopes + version-pinned) — Cloud Run's `RM_LAUNCHER_CALLBACK_URL` env var must point at the new deployment URL before T2D's Cloud Run refactor; **T2C (bound shim `Menu.gs` defensive async-state handler — accepts SUBMITTED while retaining OK/UNSATISFIED/INPUT_ERROR/COMPUTE_ERROR back-compat) lands BEFORE T2D**, because the OLD bound shim's `Menu.gs:173-176` `CLOUD_UNEXPECTED_STATE` handler would fire on every operator click between Cloud Run cutover and bound shim re-deploy; **T2D (Cloud Run `/compute` thin front-door refactor: validate + concurrent-rejection via `batch.jobs.list` filter + submit Batch job + return SUBMITTED)** — operator-facing async cutover, safe because T2C already shipped the defensive bound shim handling SUBMITTED; T2E (optional, project memory + cleanup) folded into T2D's closure narrative if scope is small. **No code changes in this PR** — Task 1 docs only. **No `contractVersion` bumps** — all contract amendments already landed in PR #147 (M7 C3 Task 1). Files touched (in-repo): `docs/delivery_plan.md` (§6 + §7 + §8 + §9 + §11 + §15); `docs/roadmap.md` (M7 entry — C3 closed, C4 active); `README.md` + `AGENTS.md` (active-checkpoint narrative M7 C3 → M7 C4); `docs/cloud_compute_contract.md` (§10A.5 callback deployment now requires version-pinning, NOT HEAD mode — small additive amendment per Codex P2 round 4 finding 7 fix; no `contractVersion` bump). **Note on "project memory" references (Codex P2 round 4 finding 8 clarification):** `project_m2_c9_setup_ids.md` is a **maintainer-side memory file stored OUTSIDE this repo** (in Claude's auto-memory directory). It's NOT a repo doc; this PR doesn't touch it via git. T2B/T2D/T2E references to it are maintainer-side ops notes (live deployment IDs, URLs, Cloud Run env vars, OAuth consent steps) — useful as the maintainer's running memory across sessions but not authoritative for implementers. **Implementers should treat `docs/cloud_compute_contract.md` §8.7 + §10A + `docs/delivery_plan.md` §9 as the in-repo source-of-truth** for the deployment posture; the project memory file just records the live-stack identifiers for cross-session continuity. If repo-side recording becomes load-bearing (e.g., new operator onboarding), a future PR can add an in-repo `docs/operator_setup.md` or similar — out of scope for M7 C4. **Active checkpoint pointer moves M7 C3 → M7 C4.**
- **2026-05-12 (M7 C3 — async architecture decision closure):** Closed **M7 C3 — Async architecture decision (D-0071 + Codex P1.7 single-VM amendment)** with PR #147's landing after **17 rounds of Codex review** (4 P1 findings + 26+ P2 findings, ~38 individual fixes across the chain). Architecture pivoted twice during M7 C3: D-0070's original sync-UX framing → D-0071's first async framing (still 13-VM + finalizer-task-group) → Codex P1.7 caught Cloud Batch v1 only supports ONE task group per job, forcing pivot to single-VM dense pack (`c3-highcpu-88` × 1 + `multiprocessing.Pool(88)` for K=88 + inline finalize step in the same Python process after `Pool.close() + .join()`). Final architecture: Cloud Run thin front door (validate + submit + return SUBMITTED ~3-5s) + Cloud Batch single-task job + inline finalize (aggregate + score + select + analyzer + POST callback) + B-prime callback to launcher's SECOND Web App deployment (`executeAs: USER_DEPLOYING` for callback only; manifest stays USER_ACCESSING for operator routes) + always-email-on-success/UNSATISFIED/failure via launcher's `MailApp.sendEmail` (finalizer can't email — Cloud Batch VM has no mail surface); 10-min operator-facing cap via finalize-step self-check at 510s elapsed since `RM_SUBMIT_TIMESTAMP_MS` (reserves 90s for finalize work so operator-facing total wall ≤ 600s) + Batch-level `maxRunDuration: 660s` safety net + `maxRetryCount: 0` (single attempt; retry × 660s would blow the cap); GCP ID-token auth in POST body's `idToken` field (Apps Script can't read HTTP headers) + `&format=full` (required for `email` / `email_verified` claims); concurrent-rejection by spreadsheet ID via Cloud Batch labels with `(status.state=QUEUED OR status.state=SCHEDULED OR status.state=RUNNING)` filter (Cloud Batch doesn't support SQL `IN`); K=104 → K=88 quality-equivalent reduction (both 4-9× past best-of-K plateau per FW-0036); state-dispatch in callback handler with applyWriteback / renderAnalysis return-value checks before success email; K'=0 routes via UNSATISFIED (per `docs/solver_contract.md` §12A.8), not COMPUTE_ERROR. **FW-0027 absorbed into M7 C3+C4 per D-0071 sub-decision 16; FW-0039 expanded** (silent-operator-outcome watchdog covering both finalizer-crash AND callback-unreachable per D-0071 sub-decision 17 + Codex P1 round-2 amendment); **new FW-0040** (K=88 → K=176 future quota-bump dial) per Codex P1.7 amendment. Files touched in PR #147: 8 docs (decision_log + cloud_compute_contract + delivery_plan + roadmap + future_work + blueprint + README + AGENTS). **No code changes; no `contractVersion` bumps** (all amendments additive per cloud_compute §11 bump rule). **Active checkpoint pointer moves M7 C3 → M7 C4.** PR #147 merged as commit `da3ad39`.

- **M7 C1 — Specs locked + GCP quota request filed** *(closed 2026-05-10; PR #136 activation + this closure PR)*
  - M7 architecture locked at activation per D-0070 (Cloud Batch + dense pack via `c3-highcpu-8` × `multiprocessing.Pool(8)` + on-demand pricing + Cloud Run Service timeout 250s + FW-0037 elbow tuple `L=50` / `idleThreshold=3500` / `swapProbability=0.5` + cloud_compute §9 + §10 amendments at M7 C3 + FW-0030/FW-0035 absorbed into M7 C3).
  - **GCP quota approvals received** (asia-southeast1, all three tickets returned within hours of filing): **CPUS approved at 300** (requested 2,520; partial), **INSTANCES approved at 350** (requested 350; full approval), **C3_CPUS approved at 108** (requested 2,520; partial — binding constraint at the C3 family). Closure-K computed per D-0070 sub-decision 7's three-quota rule: `K_approved = min(K_target=2,500, floor(min(300, 108, 350×8) / 8) × 8) = min(2,500, floor(108/8)×8) = min(2,500, 104) = **104**`. **C3_CPUS=108 is the binding constraint**; INSTANCES (350) and CPUS (300) leave headroom but C3 family allowance caps the parallel-trajectory count.
  - **Closure-K = 104 trajectories** at the FW-0037 elbow tuple. M7 C2 Batch job spec config: `taskCount=13`, `parallelism=13`, `c3-highcpu-8`, all 13 tasks fully packed at 8 trajectories each. K=104 sits ~5× past best-of-K plateau (K=10-20 per FW-0036 dev-copy evidence), so M7 architecture validates fine at this scale; future quota re-requests can lift the cap if maintainer wants larger K.
  - No `contractVersion` bumps anywhere — M7 C1 was direction-setting docs only. M7 C2 will introduce `derive_K_seeds(masterSeed, K)` shared helper at `python/rostermonster/solver/seeds.py` per D-0070 sub-decision 8 + `docs/solver_contract.md` §12A.10 (location refined from D-0070's `solver.py` reference to a separate `seeds.py` module at M7 C2 Task 1 docs lock; additive refactor of existing seed-derivation loop, no solver `contractVersion` bump). M7 C3 will land cloud_compute §9 + §10 amendments (additive per cloud_compute §11, no `contractVersion` bump).
  - 249/249 Python tests pass (no code changes in M7 C1). Active checkpoint pointer moves M7 C1 → M7 C2 (Cloud Batch implementation). Quota approval timeline beat expectations (filed + approved same-day on 2026-05-10 within ~3 minutes of submission per email confirmations).

### M7 milestone closure note
M7 (`Parallel solver — Cloud Batch + single-VM dense pack, async UX`) closed on 2026-05-13 with M7 C1 (closed 2026-05-10 per D-0070; GCP quota approvals — CPUS=300 / INSTANCES=350 / C3_CPUS=108 binding), M7 C2 (closed 2026-05-11; full Cloud Batch parallel-LAHC chain delivered across 7 PRs #138 → #144 with §12A.4 byte-identity exit criterion proven 5/5 audit tests), M7 C3 (closed 2026-05-12 per D-0071 + Codex P1.7 single-VM amendment; D-0071 async architecture lock across PR #147 + 17 rounds of Codex iteration / ~38 individual amendments), M7 C4 (closed 2026-05-13 per D-0072; async UX implementation across T2A.1..T2E + post-merge follow-ups #160 Cloud Batch filter syntax / #161 parametric VM size for quota workaround / #162 SUBMITTED modal alert / #163 analyzer FULL-retention bugfix; the prior 2026-05-12 T2E entry closed only the code track, the formal C4 checkpoint closes with M7 C5's live validation), and M7 C5 (closed 2026-05-13 per D-0072 — live operator validation on real ICU + HD cycles) all closed. Architectural decisions span D-0070..D-0072 (M7 framing → M7 C3 async pivot + Codex P1.7 single-VM amendment → M7 C5 + M7 closure verdict). M7 closure verdict (D-0072): full async UX validated end-to-end on real live ICU + HD operator cycles. All four operator-facing exit criteria from §7 C5 passed — toast + email-with-AnalyzerOutput-attachment + writeback + 5 roster tabs + analyzer comparison tab. The comparison-tab cross-reference D-0069 deferred at M6 C4 closure lands here as a natural side-effect of M7 C4's inline-finalize-step analyzer plumbing (per D-0071 sub-decision 2 + Codex P1.7 amendment: aggregate + score + select + analyzer + POST callback all run inline in `worker.py` after `Pool.close() + .join()`). Two-cycle validation (ICU + HD) gives multi-fixture live-cycle grounding to the M7 architecture lock. **FW-0027 (parallel operational search and orchestration), FW-0030 (cloud-side FULL retention support), FW-0035 (cloud-mode LAHC integration), and FW-0038 (parallel solver) all DELIVERED via M7 per D-0072** — status flips from "absorbed into M7" / "promoted to M7 C1 implementation target" → "delivered via M7" on each. FW-0039 (silent-operator-outcome watchdog) and FW-0040 (K=88 → K=176 single-VM dial) stay parked with their existing revisit triggers. Active milestone moves to *none* deliberately — mirrors the post-M4 (2026-05-01) / post-M6 (2026-05-09) cadence, deliberate study window before the next activation; no successor pre-decided. M7 closure trail: D-0070 → D-0071 → D-0072.

### M6 milestone closure note
M6 (`Solver-side score-aware search (LAHC)`) closed on 2026-05-09 with M6 C1 (solver-strategy contract extension + LAHC algorithm spec; D-0067), M6 C2 (LAHC core implementation in Python per §12A; D-0067 spec verbatim), M6 C3 (envelope-shape additive bump + CLI flags + integration tests; local-first per D-0068; D-0068 + FW-0035 + FW-0036), and M6 C4 (M6 closure scope shifted to dev-copy empirical characterization; D-0069 + FW-0036 + FW-0037 + FW-0038) all closed. Architectural decisions span D-0066..D-0069 (M6 framing + LAHC algorithm spec + local-first scope narrowing + closure verdict). M6 closure verdict (D-0069): LAHC empirically validated as superior to `SEEDED_RANDOM_BLIND` on the May 2026 ICU/HD dev-copy fixture — paired t(4)=11.10, p≪0.001, mean Δ +53.85 score points at comparable wall-time budgets, 5/5 LAHC winners across the budgeted seed pool. Sweet-spot defaults established subject to multi-fixture validation per FW-0036/FW-0037: `L=10`, `idleThreshold=2000`, `swapProbability=0.5`, `K=10–20`. Module-level defaults NOT pinned this milestone — empirical sweet-spot values held back as `--lahc-*` overrides until multi-fixture validation. Originally-framed C4 task list (operator cycle × two-comparison-tab cross-reference) shifted because the dominance signal was already overwhelming on the dev-copy fixture and the cloud benchmark (SRB cloud baseline — the cloud wrapper ships `SEEDED_RANDOM_BLIND` only in v1 per D-0068 / FW-0035) surfaced a different load-bearing concern: single-thread bottleneck on Cloud Run (4-vCPU bump only +10% throughput on the SRB baseline; signal generalizes to LAHC because both share GIL-bound pure-Python compute). The M5 analyzer thesis (D-0065) is not invalidated — it would have given the same verdict, just slower; D-0069 records the closure path explicitly so the discipline is documented rather than implicit. Active milestone moves to *none* deliberately: M7 (parallel solver to address single-thread bottleneck) is the natural next step but explicitly held in study before activation per FW-0038. Cloud-mode LAHC integration (FW-0035) stays parked. The optional `swapProbability` LAHC param + `--lahc-swap-probability` CLI flag landed alongside this closure cycle (PR #134 PR-A) so the move-mix coin-flip is tunable for future work without contract churn — not a contract change beyond §12A.7's existing maintainer-tunable surface enumeration. M6 closure trail: D-0066 → D-0067 → D-0068 → D-0069.

- **M6 C4 — Live operator validation via M5 analyzer (closure scope shifted to dev-copy empirical characterization)** *(closed 2026-05-09; PR #134 PR-A swapProbability + audit surfaces, this PR-B closure docs sweep)*
  - C4 was originally framed as live operator validation on a real ICU/HD cycle, with two `AnalyzerOutput` renders cross-referenced in the source spreadsheet per D-0066 sub-decision 7. Closure scope shifted to **dev-copy empirical characterization** because the LAHC-vs-SRB dominance signal was already overwhelming on the May 2026 ICU/HD dev-copy fixture (M6 C3 pre-validation: paired t(4)=11.10, p≪0.001, mean Δ +53.85 score points; 5/5 LAHC winners) and additional cloud benchmarking surfaced a different load-bearing concern — single-thread bottleneck.
  - **Empirical findings on the dev-copy fixture (uncommitted scripts in `experimental/m6_c4_dryrun/` + `experimental/m6_lahc_comparison/`):**
    - **L sweep (30 seeds × 8 log-spaced L values)** confirmed §12A.5's default `L=1000` sits past the cliff in our iter-budget regime: scores plateau at L ∈ {1, 10, 50, 100} (mean +90 to +91, sd ~1) and fall off catastrophically at L ≥ 500 (L=1000 mean +35.5; L=10000 mean +2.8). Wilcoxon adjacent paired tests pin the cliff at L=100 → L=500 (p≪0.001, median Δ −9.6). Mechanism: at large L with `idleThreshold` cutting off short of full history-list refresh, the dual-clause accept rule degenerates to "accept anything not catastrophically worse than seed" (Bazargani & Lobo 2017's "L too large for iter budget" failure mode). **Captured as FW-0037** with N=5 statistical methodology floor pinned via TOST equivalence + Wilcoxon power + bootstrap stability.
    - **idleThreshold sweep** identified `idleThreshold ≈ 2000` as the sweet-spot — earlier termination loses score (still climbing), longer termination wastes budget without gain.
    - **Move-mix sweep (`swapProbability` ∈ {0.0, 0.25, 0.5, 0.75, 1.0})** confirmed the §12A.7 default `0.5` is statistically equivalent to other in-plateau values; extreme values (0.0 reassign-only / 1.0 swap-only) work via §12A.1.a fallback but show no quality lead. The `swapProbability` field landed in PR #134 PR-A so future move-mix work can change the bias without contract churn.
    - **Cloud benchmark on `roster-monster-compute` Cloud Run service** (1 vCPU / 512 MB / 5-min timeout / scale-to-zero, per `docs/cloud_compute_contract.md` §9). The cloud HTTP wrapper ships `SEEDED_RANDOM_BLIND` only in v1 per D-0068 / FW-0035 (still parked through M6 closure), so the cloud runs measured here are **SRB cloud baselines**, not LAHC cloud runs. **SRB-K=32**: cold ~283s, warm ~270s; per-candidate cost ~8.46s warm — substantially slower than local SRB (~10ms/candidate × 32 = ~0.3s + scorer overhead) because the cloud Python compute core runs Phase 1 + Phase 2 + scorer + selector + writeback assembly serially under a GIL-bound 1-vCPU container. **4-vCPU / 2 GiB hardware bump tested and reverted** — only +10% throughput on SRB-K=32, demonstrating that the cloud bottleneck is single-thread compute, not core count. The single-thread finding generalizes to LAHC because LAHC's inner loop is pure-Python sequential work (move generator → rule-engine call → scorer call → accept/reject decision, all GIL-bound) — the same compute discipline SRB shares. **LAHC cloud projection** (NOT a measured cloud LAHC run, since FW-0035 is parked): local LAHC per-trajectory cost ~16s × ~5× cloud slowdown ≈ ~80s/trajectory; K=10 LAHC cloud → ~13 min, well past the 5-min Cloud Run timeout, before parallelism. Therefore M7 (parallel K-trajectory solver) is a prerequisite for cloud LAHC at operator-meaningful K — captured as **FW-0038** (LAHC's K-trajectory independence per §12A.2 makes parallelism structurally permitted).
  - **Code additions in PR #134 PR-A** (split out so this closure docs sweep stays docs-only): `LahcParams.swapProbability: float = 0.5` field with [0.0, 1.0] validation; `--lahc-swap-probability` CLI flag (maintainer-only, only meaningful with `--strategy LAHC`); the resolved value threads through audit surfaces (`SearchDiagnostics.lahcSwapProbability`, `LahcParamsRecord.swapProbability`, `runEnvelope.solverStrategyConfig.lahcParams.swapProbability`); `apps_script/launcher/src/DeleteSheetsById.gs` maintainer utility for headless tab cleanup via `clasp run`. Default 0.5 reproduces historical hardcoded coin-flip byte-identically. Contract updates: `docs/solver_contract.md` §12A.7 (CLI surface) + §12A.9 (`lahcSwapProbability` diagnostic field); `docs/selector_contract.md` §16.5 (optional `swapProbability` in `lahcParams` payload, default-on-absence rule for M6-C3-era envelope back-compat). **No `contractVersion` bump** — additive optional with default-preserving behavior, same precedent as §16.5 introduction in M6 C3 Task 1.
  - Decision: **D-0069** (M6 C4 verdict closure + M6 milestone closure — LAHC empirically validated; sweet-spot defaults captured but not module-pinned; active milestone → none with M7 study deferred per FW-0038; cloud-mode LAHC stays parked at FW-0035 because pre-parallelization cloud rollout is less compelling than waiting on M7). New future-work entry **FW-0038** (parallel solver — M7 candidate framing) added in this PR. **FW-0036** (LAHC operator-cycle K defaults) updated with the dev-copy empirical evidence supporting K=10–20. **FW-0037** (LAHC two-step tune-then-run pattern with statistically-justified L-tuning protocol) updated with idleThreshold + move-mix sweep findings.
  - **`contractVersion` ledger:** no bumps. PR #134 PR-A's `swapProbability` additions sit additively under the existing M6 C3 Task 1 §16.5 + §12A.7 surfaces.
  - 249/249 Python tests pass (244 baseline at M6 C3 close + 5 new tests in PR #134 PR-A: `test_lahc_swap_probability_rejects_out_of_range`, `test_lahc_swap_probability_extremes_complete_via_fallback`, `test_lahc_default_swap_probability_byte_identical_to_pre_field`, `test_lahc_swap_probability_surfaces_in_search_diagnostics`, `test_lahc_params_record_carries_swap_probability`; pipeline 10/10, selector 23/23, run-CLI 12/12, solver 41/41). Active checkpoint pointer moves M6 C4 → none. Active milestone pointer M6 → none. README / AGENTS / blueprint / roadmap / decision_log / future_work all swept.

- **M6 C3 — Envelope / CLI integration (local-first per D-0068)** *(closed 2026-05-08; PRs #129 Task 1, #130 Task 2A, #131 Task 2B, plus this Task 3 closure PR)*
  - LAHC wired into the local Python pipeline per D-0050's local-track. Task 1 (PR #129) added optional additive `solverStrategy` + `solverStrategyConfig` fields to `runEnvelope` per new `docs/selector_contract.md` §16.5 — tagged-union shape `{strategy: "...", lahcParams?: {...}}`; producer obligation enforces both-fields-or-neither at construction time; consumer obligation forbids inferring strategy from absence (no `contractVersion` bump per §16.3 additive-optional rule). Task 2A (PR #130) wired CLI flags `--strategy {SEEDED_RANDOM_BLIND, LAHC}` (default SRB for byte-identical CI semantics) + `--lahc-history-length` / `--lahc-iter-cap` / `--lahc-idle-threshold` (each defaults to §12A.5 values; setting any without `--strategy LAHC` fails loud at exit code 2; explicit `0` reaches `LahcParams.__post_init__` validation rather than silently defaulting). Task 2B (PR #131) added 4 integration tests: pipeline-level LAHC FULL-retention sidecar emission; pipeline-level byte-identical determinism per §12A.4; strategy-choice-changes-output regression guard (using `searchDiagnostics.lahcHistoryListLength` shape, not score-equality which would be brittle to ties); analyzer-chain compatibility (M6 → M5 — LAHC FULL envelope consumed end-to-end by `analyze(...)` without special-casing).
  - Empirical pre-validation characterization (scripts in `experimental/m6_lahc_comparison/`) on the May 2026 ICU/HD dev-copy:
    - **K=1 paired (10 seeds, modest LAHC depth `maxIters=2000`)**: LAHC mean +22.0 vs SRB -23.3, mean Δ +45.3 (t(9)=8.68, p≪0.001); 10/10 LAHC winners.
    - **§12A.5 default LAHC params, K=1 (5 seeds)**: mean wall time 16.15s, mean score +41.9; actual iters 14k–49k (idleThreshold cuts off well below maxIters cap).
    - **Comparable-budget (5 seeds, SRB-K=100 vs LAHC-K=10 at default LAHC params)**: SRB-K=100 mean 161.79s / score +13.0; LAHC-K=10 mean 158.35s / score +66.8. Wall time indistinguishable (t(4)=-0.28, p≈0.78); score Δ +53.85 (t(4)=11.10, p≪0.001); 5/5 LAHC winners. **Captured as FW-0036** (LAHC operator-cycle K defaults — pin a separate `_DEFAULT_MAX_CANDIDATES_LAHC ≈ 10–20` after M6 C4 + multi-cycle validation; the SRB-era K=1000 default at ~16s/trajectory would mean ~4.5h/run and is operationally infeasible).
  - No new architectural decisions. No `contractVersion` bumps anywhere (selector contract additive-optional per §16.3).
  - 244/244 Python tests pass (240 baseline + 4 Task 2B integration tests). Active checkpoint pointer moves M6 C3 → M6 C4. Cloud-mode LAHC integration parked as FW-0035 (cloud HTTP wrapper continues to ship `SEEDED_RANDOM_BLIND`-only in v1).

- **M6 C2 — LAHC core impl in Python** *(closed 2026-05-08; PRs #126 Task 2A, #127 Task 2B, plus this Task 2C + closure PR)*
  - LAHC strategy implemented end-to-end in `python/rostermonster/solver/` per `docs/solver_contract.md` §12A. Task 2A (PR #126) extracted a strategy abstraction so `SEEDED_RANDOM_BLIND` and `LAHC` plug into a shared dispatch surface (SEEDED_RANDOM_BLIND output stays byte-identical post-refactor per §16). Task 2B (PR #127) implemented the LAHC inner loop per §12A.1: seed roster via SEEDED_RANDOM_BLIND Phase 1+2; history-list `L=1000` default; dual-clause accept criterion (`proposedScore >= currentScore` OR `proposedScore >= history[i mod L]`) per §12A.1.c; history-list overwrite per §12A.1.e (NOT max); best-roster + idle-counter atomic update per §12A.1.f; termination at `idleThreshold` OR `maxIters` only per §12A.3; swap + reassign mixed move generator per §12A.1.a (ergodicity over rule-engine-valid roster space); seed-phase + inner-loop rule-engine evaluations both counted into `placementAttempts`; per-trajectory diagnostics (status, iters, accepted moves, best/terminal scores) surface to `SearchDiagnostics` per §12A.9. Task 2C (this PR) added byte-identical determinism (same-process + cross-`PYTHONHASHSEED`) + K-trajectory independence + bestRoster-vs-terminal divergence (best ≥ terminal invariant) + ICU/HD May 2026 fixture integration tests (22-doctor / 29-day / 116-slot real-world workload).
  - No new architectural decisions — implements D-0067 spec verbatim. No contract changes.
  - 233/233 Python tests pass (228 baseline + 5 LAHC integration tests added in Task 2C). Active checkpoint pointer moves M6 C2 → M6 C3.

- **M6 C1 — Solver-strategy contract extension + LAHC design** *(closed 2026-05-07; this PR)*
  - `docs/solver_contract.md` extended with §11.1 "Registered strategies" expansion (now lists both `SEEDED_RANDOM_BLIND` and `LAHC`) + new §12A (full LAHC algorithm spec — K-independent-seeds emission per §12A.2, idle/hard-iter inner termination per §12A.3, deterministic trajectory-seed derivation per §12A.4, history-list `L=1000` / `idleThreshold=5000` / `maxIters=100k` defaults per §12A.5, `scoringConsultation: "READ_ONLY_ORACLE"` extension-clause activation per §12A.6, maintainer-only operator-tunable surface per §12A.7, failure semantics per §12A.8, strategy-specific diagnostics per §12A.9). Editorial notes added in §16 (determinism preservation under read-only oracle), §17 (strategy-specific knobs are NOT at parser boundary), §19 (alignment notes for D-0066 + D-0067), §20 (deferrals updated — drops "second strategy" + "extension-clause activation"), §21 (new "Adopted in M6 C1" sub-section).
  - Decision: **D-0067** (M6 C1 LAHC algorithm spec) — pins all five deferred sub-decisions from D-0066 (K-emission semantics, termination, determinism, history-list defaults, envelope-shape bump location) plus a restated rationale for D-0066 sub-decision 10's `1 → 2` bump (D-0067 sub-decision 7: outcome is correct, but driven by §9 promoting `strategyId` to a required input — input-shape change per §2 — not by LAHC strategy registration; LAHC registration alone via §11.2 would not have required a bump). Envelope `solverStrategy` field location deferred to M6 C3.
  - **`contractVersion` bump 1 → 2.** Solver contract bumps for the §9 input-shape change. LAHC strategy registration is additive per §11.2's extension clause (no bump on its own); the bump is driven by `strategyId` being promoted from implicit to declared required input (because §9's scoring-prohibition rule now branches on the active strategy). Downstream contracts (selector, writeback, cloud_compute, parser_normalizer, analysis, analysis_renderer) reference solver as a producer but don't bind to its version explicitly — no ripple bumps required; existing v1 callers must now pass `strategyId: "SEEDED_RANDOM_BLIND"` explicitly under v2 (was implicit single-strategy default under v1). D-0066 sub-decision 10's `1 → 2` outcome stands, with corrected rationale per D-0067 sub-decision 7.
  - 225/225 Python tests still pass — C1 was docs-only, no Python or Apps Script changes. M6 C2 (LAHC core impl) lands the actual implementation. Active checkpoint pointer moves M6 C1 → M6 C2.

### M5 milestone closure note
M5 (`Operator-side analysis & multi-roster delivery`) closed on 2026-05-07 with M5 C1 (analyzer engine + analysis contract; D-0056..D-0058), M5 C2 (Apps Script renderer + launcher Web App route + cross-page nav; D-0060..D-0063; cloud deployment `@15`), and M5 C4 (live operator validation; D-0065) all closed. M5 C3 (`Upload portal`) was dropped per D-0063 — upload-portal scope was wholly absorbed into C2's launcher route, same discipline as M3 C2 dropped per D-0048. Architectural decisions span D-0055..D-0065. Repo-cross-cutting decisions that landed during M5 but aren't M5-scope-specific: D-0059 (apps_script directory rename — `m1_sheet_generator/` → `launcher/`, `m2_template_bound_script/` → `bound_shim/`, `m2_extractor_library/` → `central_library/`); D-0064 (forward-going Task 1/2/3 cadence vocabulary, formalizing the M5 C1 "drop Phase" terminology fix). M5 closure verdict (D-0065): the analyzer's role as calibration framework was **validated** — the comparison tab successfully surfaced a load-bearing scoring-formulation insight (`pointBalanceGlobal` weight design) the operator would not have seen from `totalScore` alone, exactly the M5 thesis (operator picks among K candidates with full component breakdowns rather than trusting a single scalar) working as designed. D-0055 sub-decision 9 pivot resolved: scoring-formulation issues DID surface but the resolution is operator-tunable weights via the existing Scorer Config tab (D-0037 surface), NOT a lexicographic / threshold / Pareto rework — long-term systematic weight elicitation parked as FW-0033, explicitly NOT rolled into M6 (LAHC-only) scope. Active milestone moves to **M6** same-day per D-0066 (see M6 activation note above).

- **M5 C4 — Live operator validation** *(closed 2026-05-07; this closure PR)*
  - Operator ran end-to-end on the May 2026 ICU/HD smoke-test snapshot: `python -m rostermonster.run --retention FULL --max-candidates 1000` (~27-min pipeline, 116/116 filled, winner `totalScore -842.387`); `python -m rostermonster.analysis --top-k 5` (0.13s, 133KB `AnalyzerOutput`); upload via launcher's `?action=analysis-render` route → 5 roster tabs + 1 comparison tab in the source spreadsheet. Operator review surfaced 29 hot days, 0 locked days, 22 doctorIdMap entries; rank 1..5 totalScores -842 / -1388 / -1441 / -1705 / -1931.
  - Decision: **D-0065** (M5 C4 verdict closure — analyzer's role as calibration framework validated; scoring-formulation rework deferred to FW-0033, NOT M6 scope; M5 milestone closes here). New future-work entry FW-0033 (systematic weight elicitation / tuning) added in this PR. Forward-going Task 1/2/3 cadence vocabulary D-0064 also landed in this PR (formalizing the M5 C1 "drop Phase" terminology fix); historical "Phase 1/2/3" labels in past PR titles + closure entries (M2..M5 C2) preserved as-is.
  - Analyzer-engine contract (`docs/analysis_contract.md` `contractVersion: 1` per D-0056..D-0058) and analyzer-renderer contract (`docs/analysis_renderer_contract.md` `contractVersion: 1` per D-0060..D-0063) both unchanged by C4 — validation was operational, not contract-touching. 225/225 Python tests still pass; no Python or Apps Script code changes in this PR.
  - M5 milestone closes; active milestone pointer M5 → **M6** same-day per D-0066 in companion PR; active checkpoint pointer M5 C4 → **M6 C1** (see M6 activation note above).

- **M5 C2 — Apps Script analyzer renderer** *(closed 2026-05-06; PRs #114 contract draft, #115 implementation, #116 launcher nav header, plus M5 C2 closure PR)*
  - Operator-facing analyzer renderer live end-to-end: launcher Web App at `?action=analysis-render` accepts an `AnalyzerOutput` JSON file, hands it to `RMLib.renderAnalysis(...)` in the central library, which writes K roster tabs + 1 comparison tab into the source spreadsheet identified by `output.source.sourceSpreadsheetId`. Cross-page nav header on all three operator pages (Generate sheet / Apply writeback / Render analysis) so operators can navigate between functions without manual `?action=...` URL surgery.
  - Decisions: D-0060 (renderer module placement = central library, file `apps_script/central_library/src/AnalysisRenderer.gs`), D-0061 (separate `docs/analysis_renderer_contract.md` rather than extending `docs/analysis_contract.md`), D-0062 (target spreadsheet = source spreadsheet, accepting tab-bloat trade-off; always-new-tab collision policy mirrors writeback §11), D-0063 (invocation surface = route on the existing launcher Web App, mirroring D-0046's writeback upload pattern; direct-from-bound-shim deferred).
  - New contract `docs/analysis_renderer_contract.md` (`contractVersion: 1`); analyzer engine contract surface (PR #110) unchanged. Launcher cloud-side deployment bumped from `@14` → `@15` ("M5 C2: analysis-render route + cross-page nav") so the operator-facing tinyurl serves the new content; deployment ID + URL stable.
  - C3 dropped from M5 plan — the upload-portal scope was wholly absorbed into C2's launcher Web App route; same discipline as M3 C2 dropped per D-0048 when C1 already met exit criteria. Active checkpoint pointer moves M5 C2 → M5 C4. M5 milestone stays Active pending C4 live-operator validation.
  - 225/225 Python tests still pass — C2 was Apps Script-side only, no Python changes. Apps Script unit-test coverage remains a separate cleanup (existing Tests.gs writeback helpers reference private library functions post-D-0052 that aren't reachable at runtime; outside M5 C2 scope). M5 C4 operator validation will exercise the renderer end-to-end on real data.

- **M5 C1 — Python analyzer engine + analysis contract draft** *(closed 2026-05-05; PRs #110 contract draft, #111 implementation, plus this closure PR)*
  - Operator can now run `python -m rostermonster.analysis --snapshot S.json --envelope E.json --full-sidecar F.json --output A.json --top-k 5` against any FULL-retention CLI run output and get a single `AnalyzerOutput` JSON with Tier 1–5 comparison data over the top K candidates.
  - Decisions: D-0056 (analyzer top-K = pure score-rank with selector-cascade tiebreak — `pointBalanceGlobal` desc → `crReward` desc → numerically lowest `candidateId` per `docs/selector_contract.md` §12.2; no diversity heuristic at the analyzer stage — diversity belongs in solver-strategy work), D-0057 (analyzer I/O shape — full snapshot + wrapper envelope + `candidates_full.json` in, single `AnalyzerOutput` JSON out; CSV sidecar explicitly excluded), D-0058 (Tiers 1–5 v1 emission scope; Tier 6 deferred to FW-0032; Tier 7 renderer-derived; snapshot-extension fields parked as FW-0031).
  - New contract `docs/analysis_contract.md` (`contractVersion: 1`); cross-contract amendment to `docs/selector_contract.md` §14.2 wording (clarified `candidates` array shape — no `schemaVersion` bump, editorial only). New future-work entries FW-0031 (snapshot-extension analyzer fields incl. PH classification) + FW-0032 (per-candidate rule-violation surface for analyzer Tier 6).
  - PR #110 ran 17 Codex review rounds (16 patches before thumbs); PR #111 ran 7 rounds (6 patches before thumbs). Substantive findings landed across both PRs. 225/225 Python tests pass: 164 baseline + 52 analyzer unit + 9 analyzer integration.
  - Live demo deferred to M5 C4 per the M5 sequencing — engine is ready; renderer + upload portal land in M5 C2 (originally split across C2 + C3, then bundled at C2 closure with C3 dropped) to make the operator-facing surface complete.

### M4 milestone closure note
M4 (`Cloud end-to-end pipeline + dual-track preservation`) closed on 2026-05-01 with M4 C1 closure as its only delivered checkpoint. M4 was reframed from `Parallel operational search and orchestration` per D-0049 to prioritize a boss-clickable proof of concept and preserve the local CLI for forthcoming solver-strategy work. The reframing parked the original M4 scope (parallel orchestration) and the original M5 scope (observability) as future-work entries FW-0027 + FW-0028. Solver-strategy optimization is materially different in character (changes core compute semantics rather than adding a delivery vehicle) and lands as its own forthcoming milestone slot rather than an M4 C2. M3 stays Completed; M2 stays Completed.

- **M4 C1 — Cloud end-to-end pipeline (minimum demo)** *(closed 2026-05-01; PRs #103 docs, #104 amendment, #105 code, #106 closure)*
  - Operator-facing one-click cloud-mode pipeline live: bound shim's `Roster Monster → Solve Roster` menu orchestrates extract → Cloud Run compute → in-memory writeback within one Apps Script invocation. Local CLI preserved unchanged per D-0050's dual-track discipline.
  - Decisions: D-0048 (close M3 early), D-0049 (reframe M4), D-0050 (dual-track Python — shared core + thin CLI + thin HTTP wrapper), D-0051 (Cloud Run + consolidated `RosterMonsterV2` GCP project), D-0052 (Apps Script library reorg — writeback moves into central library), D-0053 (random seed default), D-0054 (Cloud Run auth = public service + Flask-side `X-Auth-Token` + `ALLOWED_EMAILS` — amends D-0051 sub-2 after `aud`-mismatch live-test failure).
  - New contract `docs/cloud_compute_contract.md` (`contractVersion: 1`); cross-contract additive updates to `docs/snapshot_adapter_contract.md` §7/§11 + `docs/writeback_contract.md` §6.3.
  - Phase 2 also broadened bound-shim manifest scopes (`spreadsheets.currentonly` → `spreadsheets`) and applied Path 2-Lite visual polish to writeback tab (FW-0029 captures template-aware label propagation when a second template enters scope). 164/164 Python tests pass.
  - Live demo confirmed end-to-end on dev-copy 2026-05-01.

### M3 milestone closure note
M3 (`Safe result/output and writeback`) closed on 2026-04-30 with M3 C1 closure as its only delivered checkpoint. M3 C2 (`End-to-end demo on a real ICU/HD operator cycle`) was dropped per D-0048 — exit criteria (writeback round-trip with traceability + 3-state diagnostic + no silent partial-state) already met by C1 closure on the dev-copy round-trip. Architectural decisions D-0044..D-0047 plus the M3 C1 Phase 2 contract amendment that grew `docs/writeback_contract.md` §9 from 5 to 6 snapshot categories (closing D-0045 Follow-up #2). Active milestone moved to M4 same-day per D-0049.

- **M3 C1 — Writeback library implementation + launcher route + Python CLI extension** *(closed 2026-04-30; PRs #100 docs, #101 code, #102 closure)*
  - Production writeback path live end-to-end: Python CLI emits wrapper envelope (`FinalResultEnvelope` + snapshot subset + `doctorIdMap`); launcher Web App's `?action=writeback` route accepts upload via file-input form + `FileReader` + `google.script.run.applyWriteback`; Apps Script writeback library writes a new tab with prefilled cells at source-tab `(surfaceId, rowOffset)` coordinates, traceability footer + DeveloperMetadata + read-only protection; 3-state launcher diagnostic per writeback §17.
  - Decisions: D-0044 (transport = file upload via launcher form), D-0045 (envelope shape = single JSON wrapper), D-0046 (placement = launcher Web App route, not bound shim), D-0047 (Python CLI standard output IS the writeback envelope, controlled by `--writeback-ready` flag).
  - Phase 2 contract amendment: writeback §9 grew a 6th category `outputAssignmentRows` carrying the template's `slotType ↔ rowOffset` binding (closes D-0045 Follow-up #2). 143/143 Python pass; 17 Apps Script writeback unit tests.
  - Live demo confirmed on real ICU/HD May 2026 dev-copy snapshot 2026-04-30 (100 candidates, 116/116 filled, score -2199.97).

### M2 milestone closure note
M2 closed 2026-04-29. Across nine checkpoints (C1..C9), the milestone delivered: rule engine + scorer + solver + selector contracts (C1, C2); their Python implementations (C3, C4, C5); the operator-tuneable scoring config surface end-to-end across snapshot + parser + scorer + sheet-generator (C6, C7); the v3 spacingPenalty geometric-decay curve (C8); and the production Apps Script snapshot extractor + Python CLI ingestion path (C9). Architectural decisions span D-0024..D-0043 (twenty decisions). 141 Python tests + Apps Script tests via `clasp run runAllTests_`. Remaining gap to a fully operational pilot is the one-time Drive setup the maintainer performs once per environment per the M2 C9 setup recorded in project memory.

- **M2 C9 — Snapshot extraction (Apps Script) — D-0036 implementation** *(closed 2026-04-29; PRs #95 docs, #96 code, #97 closure)*
  - Production Apps Script extractor live: launcher generates operator sheets via `DriveApp.makeCopy(template)`; bound shim's `onOpen` installs the "Extract Snapshot" menu; central library at HEAD reads the sheet via DeveloperMetadata anchors with sheet-scoped finder + runId-paired tab discovery + per-anchor cardinality validation; Python CLI ingests the JSON.
  - Decisions: D-0040 (transport = browser-download), D-0041 (placement = bound template + central library; resolves FW-0024 architectural blocker as a side effect), D-0042 (snapshot identity = `snapshot_<spreadsheetId>_<extractionTimestamp>`), D-0043 (layout drift = DeveloperMetadata-anchored hard-fail).
  - New contract `docs/snapshot_adapter_contract.md`; cross-contract updates to writeback §6.3, sheet_generation §11B, and parser/snapshot forward-pointers. FW-0025 (per-email Drive sharing tightening) deferred with four explicit revisit triggers. 141/141 Python tests pass (end-to-end CLI run: 116/116 filled, score -21.534).
  - One-time Drive setup steps recorded in project memory `project_m2_c9_setup_ids.md`; per-project READMEs walk through each step.

- **M2 C8 — `spacingPenalty` geometric-decay shape (port v1)** *(closed 2026-04-28; PRs #92 docs, #93 code, closure PR)*
  - Replaced v2's binary `gap < 3` count with v1's geometric-decay curve (`weight / 2^(gap - 2)` for `gap ∈ {2..6}`, zero past `gap = 7`). 7-day cutoff embeds the once-per-week call cadence as the natural rhythm. Restores the continuous gradient the solver's `SEEDED_RANDOM_BLIND` Phase 2 tie-break needs.
  - **Scorer `contractVersion` bumped 2 → 3** per §2 (scoring-semantics change for valid configs); public `ScoringConfig` shape unchanged. Operator-tuneable curve parameters remain FW-0007 deferred.
  - Decision: D-0039. New normative subsection scorer §12A. 4 stale `v2` cross-refs caught by §14 bidirectional audit + fixed in-PR. 136/136 Python tests pass.

- **M2 C7 — Scorer Config tab (D-0037 sheet-generator half)** *(closed 2026-04-28; closure PR + hotfix #90 + UX-polish #91)*
  - Apps Script launcher generates the Scorer Config tab declared in `docs/sheet_generation_contract.md` §11A (D-0037 producer-side complement to M2 C6's consumer-side overlay). Each data row carries `DeveloperMetadata` keyed by canonical componentId; weight column operator-editable, rest locked. 4-column shape (Component / Weight / Description / Sign) with per-row sign-validation.
  - **FW-0024 attempted but reverted on Codex review** (PR #89): simple `onEdit` triggers don't fire on launcher-generated standalone-Web-App spreadsheets; deferred with the trigger-architecture finding captured in the FW-0024 entry.
  - No new architectural decisions — closes the D-0037 producer-side gap M2 C6 left open.

- **M2 C6 — Parser overlay for scoring config** *(closed 2026-04-28)*
  - Wired the Python consumer-side of D-0037 (operator-tuneable scoring config). New `parser/scoring_overlay.py` implements the §9 sheet-wins / template-defaults-backstop overlay; admission cases per parser_normalizer §14 (mis-signed / malformed weights, incomplete pointRules cross-product per D-0038, etc.). Weekday/weekend defaults applied via calendar-based today/tomorrow classification (no public-holiday calendar in first release).
  - Producer-side (Apps Script Scorer Config tab + production extractor) deferred to D-0036's late-M2 checkpoint. Test-only xlsx → snapshot bridge extended to emit empty `scoringConfigRecords`.
  - No new architectural decisions — tracks D-0037 + D-0038 contract semantics. 124/124 Python tests pass.

- **M2 C5 — Local run artifact basics** *(closed 2026-04-28)*
  - Implemented the selector stage (`HIGHEST_SCORE_WITH_CASCADE` per `docs/selector_contract.md` §11.1 + §12) and the `BEST_ONLY` / `FULL` retention surface with sidecar artifacts (`candidates_summary.csv`, `candidates_full.json`) under `FULL`. Closes the contract → implementation gap selector_contract.md has carried since M2 C2 closure.
  - Public entry: `select(scoredCandidateSet, *, retentionMode, runEnvelope, selectorStrategyId, selectorStrategyConfig=None, sidecarTargetDir=None) → FinalResultEnvelope`. Branch discipline (§10.3); failure-branch forwarding (§15); envelope passthrough (§16.4); byte-identical determinism (§18).
  - Architectural import-boundary check in tests confirms selector module does not consume `score(...)` or `evaluate(...)` per §9. No new architectural decisions. 115/115 Python tests pass.

- **M2 C4 — Minimal rule/scorer/solver integration** *(closed 2026-04-27; PRs #78..#86)*
  - Shipped the three remaining M2 compute-pipeline stages (rule engine + scorer + solver) wired onto the parser/normalizer's CONSUMABLE `NormalizedModel` from M2 C3, plus an integration smoke test exercising parser → solver → scorer end-to-end on the real ICU/HD May 2026 fixture.
  - Decisions: D-0037 (operator-tuneable scoring config — scorer v1 → v2 across 5 contracts), D-0038 (`pointRules` fail-loud on missing keys, supersedes D-0037 sub-decision 5).
  - Bidirectional contract-audit rule added to delivery_plan §14 (recurrence prevention for the M2 C2 audit miss D-0037 ultimately closed). FW-0024 surfaced and parked. 92/92 Python tests pass.

- **M2 C3 — Parser/normalizer implementation closure** *(closed 2026-04-27; PRs #70..#75)*
  - First executable code under M2 — the local-first Python parser/normalizer module (`python/rostermonster/`) implementing parser_normalizer_contract §6/§8/§10/§11/§13/§14/§15 + request_semantics §6..§15, with §17 explicit-handoff defense layer + provenance fields + `displayLabel` on `SlotTypeDefinition`.
  - Real-data hand-test against ICU/HD May 2026 dev-copy source via test-only xlsx → snapshot bridge (22 doctors, 29 days, 638 request records, 0 prefilled assignments).
  - Decisions: D-0033 (dateKey adapter normalization), D-0034 (doctor-name matching algorithm: trim + collapse internal whitespace + casefold), D-0035 (provenance shape locked, closes OD-0001), D-0036 (snapshot-extraction Apps Script pinned to late-M2 before M3 activation; transport jointly scoped with writeback §6.3). 24/24 Python tests pass.

- **M2 C2 — Selector contract closure** *(closed 2026-04-26; PRs #65, #66, #67)*
  - Closed the selector-stage contract boundary with `docs/selector_contract.md` covering pure-function surface, pluggable strategy interface mirroring solver's `StrategyDescriptor`, `HIGHEST_SCORE_WITH_CASCADE` first-release strategy, retention modes (`BEST_ONLY` / `FULL`), sidecar artifacts cross-referenced by `candidateId`, byte-identical determinism, sidecar `schemaVersion: 1`.
  - Decisions: D-0030 (selector at v1), D-0032 (selector v1 → v2 in-checkpoint to close contract-seam consistency gap with the writeback scope-ahead contract — added `runEnvelope.sourceSpreadsheetId` + `sourceTabName` to §9 item 3). Selector compliance under v2 implies writeback input compatibility on the source-sheet-identity surface.
  - Writeback contract drafted scope-ahead in PR #66 per D-0031 (M3 territory, not C2 scope).

- **M2 C1 — Rule engine + scorer + solver contract closure** *(closed 2026-04-25; PRs #63, #64)*
  - Closed the three interlocking M2 compute-pipeline contract boundaries: `docs/rule_engine_contract.md` (stateless, full-violation canonical ordering, scoped FixedAssignment handling), `docs/scorer_contract.md` (pure-function, required component breakdown, `HIGHER_IS_BETTER` direction-guard, `crReward` diminishing-marginal-utility), `docs/solver_contract.md` (scoring-blind, strategy-pluggable, `SEEDED_RANDOM_BLIND` first-release, `crFloor`, whole-run failure on unfillable slot, byte-identical determinism).
  - Decisions: D-0024..D-0029 (rule engine, scorer, solver, pipeline-stage separation `solver → scorer → selector`, operator-tuneable surface, `unitIndex` operational-equivalence narrowed to doctor admissibility per D-0029 sub-decision 1).

- **M1.1 C1 — Implement operator launcher web app** *(closed 2026-04-22)*
  - Apps Script web-app (`doGet()` HTML form + `submitLauncherForm` wiring) against `generateIntoNewSpreadsheet` / `generateIntoExistingSpreadsheet` entrypoints, deployed at a stable `/exec` behind `https://tinyurl.com/cghicuhdlauncherv1`. Auto-share of newly-created spreadsheets to "anyone with the link (Editor)" landed as additive operational polish per D-0023 (three-attempt sequence: `DriveApp` under `drive.file` → full `drive` → Drive Advanced Service v3).
  - Verified end-to-end through non-maintainer Test-User consent + generation path on a separate Google account and device; both output modes exercised; pilot-operator self-report independently confirms.

- **C4 — Implement operator-ready sheet generation** *(closed 2026-04-21)*
  - Delivered the ICU/HD request sheet shell end-to-end through Google Apps Script in `apps_script/launcher/`, covering both output modes (new spreadsheet file, new tab in existing spreadsheet). Whole-sheet protection restricted to the script owner with unprotected exceptions for operator-editable surfaces, warning-only regex validation on request-entry cells.
  - Verified against the C3 acceptance checklist for an operator-owned May 2026 cycle via `clasp run` against a user-managed GCP project. `clasp run` OAuth lesson recorded as D-0020.

- **C3 — Define generation acceptance/handoff readiness** *(closed 2026-04-18)*
  - Locked M1 implementation-ready checklist for ICU/HD first-release generation handoff without reopening C1/C2. Declared the first allowed implementation slice (template read + operator inputs + shell generation + output-mode support + practical locking/validation) and explicit out-of-scope items.

- **C2 — Align template artifact vs generation needs** *(closed 2026-04-18)*
  - Closed remaining cross-doc alignment between `docs/template_artifact_contract.md` and `docs/sheet_generation_contract.md`, including first-release visible title/header generated-surface alignment.

- **C1 — Close sheet-generation MVP boundary** *(closed 2026-04-17)*
  - Closed generation inputs, generated structural surfaces, allowed operator edits, editable/protected + validation boundary, explicit non-goals, and acceptance framing at contract/planning level.

### Recently completed milestones
- **M1.1 — Operator-facing launcher** *(closed 2026-04-22)* — closed on hands-on validation via M1.1 C1; addendum-milestone framing preserved (M1 not reopened); D-0021 / D-0022 / D-0023.
- **M1 — Operator-ready request sheet generation** *(closed 2026-04-21)* — closed on operator delivery via C4 against settled C1/C2/C3 contracts. D-0019 (closure), D-0020 (clasp OAuth lesson).

## 12. Change log for this delivery plan

> One line per closure (or material in-flight architectural event). Milestone-level activations / closures are bundled with the relevant checkpoint line. Day-by-day phase mechanics live in PR descriptions and git history.

- **2026-04-16:** Document created. Activated M1 + M1 C1.
- **2026-04-17:** Closed M1 C1 (sheet-generation MVP boundary). Activated M1 C2.
- **2026-04-18:** Closed M1 C2 (template artifact ↔ generation alignment). Activated and closed M1 C3 (acceptance/handoff readiness). Briefly closed M1 + activated M2; reverted same-day so M1 closure aligns with operator delivery rather than contract closure alone. Activated M1 C4 (implement operator-ready sheet generation).
- **2026-04-21:** Closed M1 C4 + closed M1 (operator delivery; D-0019). Activated M1.1 (operator-facing launcher addendum) + M1.1 C1; D-0021 (addendum convention) + D-0022 (launcher architecture).
- **2026-04-22:** Closed M1.1 C1 + closed M1.1 (hands-on validation). Activated M2 at milestone level.
- **2026-04-23:** Activated M2 C1 (rule engine + scorer + solver contract closure).
- **2026-04-25:** Closed M2 C1 (PRs #63, #64; D-0024..D-0029). Activated M2 C2 (selector contract closure).
- **2026-04-26:** During M2 C2, drafted writeback contract scope-ahead of M3 (D-0031 with eleven sub-decisions; FW-0019..FW-0023). Bumped selector v1 → v2 inside the still-active checkpoint to close contract-seam consistency gap with writeback (D-0032). Polish sweep across M2 contracts (PR #67) — wording-only, no semantics change. Closed M2 C2; activated M2 C3 (parser/normalizer implementation).
- **2026-04-26 to 2026-04-27 (M2 C3 in flight):** D-0033 (dateKey adapter normalization) + D-0034 (doctor-name matching) closed alongside T1; D-0035 (provenance shape locked; closes OD-0001) + D-0036 (Apps Script extractor pinned to late-M2 before M3) closed at T3.
- **2026-04-27:** Closed M2 C3 (PRs #70..#75). Activated M2 C4. Added bidirectional contract-audit rule to §14 (recurrence prevention for the M2 C2 audit miss). D-0037 (operator-tuneable scoring config — scorer v1 → v2 across 5 contracts) + D-0038 (`pointRules` fail-loud on missing keys, supersedes D-0037 sub-5) closed mid-checkpoint. FW-0024 added. Closed M2 C4 (PRs #78..#86).
- **2026-04-28:** Activated and closed M2 C5 (local run artifact basics — selector implementation + sidecars). Activated and closed M2 C6 (parser overlay for scoring config). Activated and closed M2 C7 (Scorer Config tab — D-0037 sheet-generator half; FW-0024 attempted but reverted on Codex review). Activated M2 C8 (spacingPenalty geometric-decay shape; D-0039; scorer v2 → v3) and closed it.
- **2026-04-29:** Activated M2 C9 (snapshot extraction Apps Script — D-0036 implementation; D-0040..D-0043; new contract `docs/snapshot_adapter_contract.md`). Closed M2 C9 + closed M2 (PRs #95, #96, #97). The `m1_template_bound_script/` and `m1_extractor_library/` directories created in Phase 2 were renamed to `m2_*` in the closure round to match the milestone-prefix-by-introduction convention.
- **2026-04-30:** Activated M3 + M3 C1 same-day (writeback library + launcher route + Python CLI extension; D-0044..D-0047). Closed M3 C1 (PRs #100, #101, #102) — Phase 2 surfaced contract amendment growing writeback §9 from 5 to 6 categories (closes D-0045 Follow-up #2). Closed M3 at milestone level (M3 C2 dropped per D-0048 — round-trip already proven); activated M4 same-day per D-0049 reframing M4 to `Cloud end-to-end pipeline + dual-track preservation`; old M4/M5 → FW-0027 + FW-0028. Activated M4 C1.
- **2026-05-01:** Closed M4 C1 + closed M4 (PRs #103, #104, #105, #106; D-0048..D-0054). D-0053 (random-seed default) and D-0054 (Cloud Run public-service + Flask-side X-Auth-Token allowlist; amends D-0051 sub-2 after `aud`-mismatch live-test failure) settled in-flight as accepted decisions. FW-0029 (template-aware label propagation in writeback library) added. Active milestone moves to *none*; solver-strategy optimization (D-0049's forward-pointer) lands as its own forthcoming milestone slot rather than M4 C2.
- **2026-05-04:** Activated M5 (`Operator-side analysis & multi-roster delivery`) + M5 C1 same-day (D-0055). M5 reframes the post-M4 priority by sequencing operator-side analysis tooling AHEAD of solver-side score-aware search (LAHC etc., parked for M6) — analysis tooling acts as the calibration framework + multi-objective workaround. Three-piece architecture: Python analyzer engine + `AnalyzerOutput` + Apps Script analyzer renderer, sibling consumer of the wrapper envelope (writeback contract untouched, no new selector retention mode, top-K + diversity is analyzer's responsibility). FW-0030 (cloud-side FULL retention support) added as M6 prerequisite. M5 C1 detailed scope (`AnalyzerOutput` schema, aggregates, diversity criterion, comparison-tab UX) deferred to dedicated design thread that opens after this activation PR merges. No code changes in this PR — direction-setting docs only, mirroring the D-0049 pattern.
- **2026-05-04 (M5 C1 contract draft):** Drafted new contract `docs/analysis_contract.md` (`contractVersion: 1`) closing D-0055's deferred concrete-shape items. D-0056 (analyzer top-K = pure score-rank, no diversity heuristic — operator-tunable `--top-k N`, default 5, bounds `[1, 20]`, fail-loud out of range; tiebreak mirrors `HIGHEST_SCORE_WITH_CASCADE`'s two-level cascade per `docs/selector_contract.md` §12.2: higher `pointBalanceGlobal`, then higher `crReward`, then numerically lowest `candidateId`; promotes Tier 5 `pairwiseHammingDistance` from optional flavor to load-bearing operator diagnostic). D-0057 (analyzer I/O shape — input = full snapshot + wrapper envelope + `candidates_full.json`; CSV explicitly excluded; the wrapper envelope's writeback-narrow `snapshot` sub-object is also explicitly NOT analyzer input — analyzer reads the full snapshot directly because the writeback subset doesn't carry per-doctor `displayName` / full `dayRecords` / post-overlay `scoringConfigRecords`; output = single `AnalyzerOutput` JSON file symmetric with D-0044/D-0045 wrapper-envelope pattern; amended in-PR after Codex review surfaced the wrapper-snapshot-shape mismatch in the original draft). D-0058 (Tiers 1–5 v1 emission scope; Tier 6 per-candidate rule-violation breakdown deferred to FW-0032 because not reachable from declared analyzer inputs without rule-engine coupling or selector-side sidecar extension; Tier 7 decision-support tags renderer-derived; snapshot-extension fields parked as FW-0031). FW-0031 (snapshot-extension analyzer fields — senior-junior pairing, leave-history-aware analysis, rotation-conflict surfacing, public-holiday classification — added with five explicit revisit triggers including the round-3 PH amendment). FW-0032 (per-candidate rule-violation surface for analyzer Tier 6 — added in round-9 amendment; lands via additive analyzer-contract bump per §14 once one of two upstream paths chosen — selector-side sidecar extension carrying violation breakdown, OR analyzer-side rule-engine integration). No code changes in this PR — contract draft only, mirroring the M2/M3/M4 docs-first cadence; analyzer-engine implementation lands as a separate sub-PR.
- **2026-05-05 (M5 C1 implementation):** Implemented the analyzer engine in new `python/rostermonster/analysis/` module per the contract pinned in PR #110 (PR #111). Module split into `output.py` (AnalyzerOutput dataclasses + JSON renderer with byte-identical determinism per §15), `admission.py` (fail-loud admission per §9.1 / §9.2 / §9.5 / §10.0 / §11), `selection.py` (top-K with full selector-cascade tiebreak per §11), `aggregates.py` (Tier 1–5 computations; cumulativeCallPoints reuses `parser/scoring_overlay.py` per §10.6), `__init__.py` (orchestrator), `__main__.py` (`python -m rostermonster.analysis ...` CLI per §16). 52 unit tests + 9 integration tests on the May 2026 ICU/HD fixture. 225/225 Python tests pass.
- **2026-05-05 (M5 C1 closure):** M5 C1 closes; active checkpoint pointer moves M5 C1 → M5 C2 (Apps Script analyzer renderer). M5 itself remains Active — C2/C3/C4 outstanding. Terminology fix: dropped the "Phase 1 / Phase 2 / Phase 3" labels for sub-PR cadence in favor of describing each sub-PR by what it accomplishes (contract draft / implementation / closure). The canonical planning vocabulary stays Product → Milestone → Checkpoint → Task per `docs/blueprint.md` §"Planning vocabulary"; "Phase" was an informal cadence convention from M2/M3/M4 PR titles that wasn't part of the canonical vocab.
- **2026-05-05 (Apps Script directory rename):** Renamed three Apps Script directories from milestone-prefixed to role-based names per D-0059: `apps_script/m1_sheet_generator/` → `apps_script/launcher/`, `apps_script/m2_template_bound_script/` → `apps_script/bound_shim/`, `apps_script/m2_extractor_library/` → `apps_script/central_library/`. Cloud-side Apps Script project name `Roster Monster Extractor Library` correspondingly renamed to `Roster Monster Central Library` (maintainer one-time step — Script ID stable). Cross-doc sweep updates 13 docs/source files + project memory. Supersedes the M2 C9 closure note's "milestone-prefix-by-introduction" convention.
- **2026-05-05 (M5 C2 contract draft):** Drafted new contract `docs/analysis_renderer_contract.md` (`contractVersion: 1`) closing M5 C2's deferred design items. D-0060 (renderer module placement = central library, file `apps_script/central_library/src/AnalysisRenderer.gs` per the `Writeback.gs` precedent in D-0052). D-0061 (separate renderer contract document rather than extending `docs/analysis_contract.md`; mirrors writeback's separate downstream-consumer contract precedent). D-0062 (target spreadsheet = source spreadsheet identified by `output.source.sourceSpreadsheetId`, accepting tab-bloat trade-off — operator workflow simplicity wins; co-exists with writeback's roster tab; always-new-tab collision policy mirrors `docs/writeback_contract.md` §11). D-0063 (invocation surface = route on the existing launcher Web App, mirroring D-0046's writeback upload pattern; direct-from-bound-shim and other surfaces deferred). No code changes in this PR — contract draft only, mirroring the contract-first cadence.
- **2026-05-06 (M5 C2 implementation):** Implemented `apps_script/central_library/src/AnalysisRenderer.gs` per the contract pinned in PR #114 (PR #115). Public `RMLib.renderAnalysis(output)` returns `AnalysisRendererResult` per `analysis_renderer_contract.md` §10. Implements §9.1 fail-loud admission (contractVersion + sourceSpreadsheetId + non-empty topK + duplicate candidateId rejection + openById permission), §10.2 spreadsheetUrl presence rules, §11 source-spreadsheet target, §12 always-new-tab `(N)` collision policy, §13 K roster tabs (header + per-day grid + per-doctor + per-component) + 1 comparison tab (score-decomp matrix with column-best/worst highlighting + equity scalars + hot/locked days + Hamming matrix), §14 best-effort sequential `SpreadsheetApp.flush` per tab, §16 structured `error.code` codes. Reuses writeback's color palette + footer/metadata/protection patterns. Duplicate doctor display names disambiguated per §10.0 (post-Codex-round-2 fix). Partial-state surface lists tabs as soon as `insertSheet` succeeds (post-Codex-round-3 fix). Launcher delegate shim `apps_script/launcher/src/AnalysisRenderer.gs` does the JSON-string deserialize before calling `RMLib.renderAnalysis(output)` (post-Codex-round-1 fix; library boundary is in-memory object per §6 + §9). New `?action=analysis-render` route in `Launcher.gs` `doGet`; `AnalysisRendererForm.html` upload form mirrors `WritebackForm.html`.
- **2026-05-06 (Launcher Web App nav header):** Added cross-page navigation (Generate sheet / Apply writeback / Render analysis) to all three launcher Web App forms (PR #116). Active item rendered as inert `<span>`; inactive items render as absolute-href `<a>` tags using `<?= rootUrl ?>` populated from `ScriptApp.getService().getUrl()` server-side (relative hrefs don't survive Apps Script's iframe sandbox; post-Codex-round-1 fix). Visual style mirrors the existing launcher palette. CSS duplicated across the three forms — Apps Script HtmlService templating doesn't expose clean per-form parameters. No new manifest scopes.
- **2026-05-06 (Cloud rollout):** Pushed `apps_script/central_library/` (8 files including new `AnalysisRenderer.gs`) and `apps_script/launcher/` (18 files including new `AnalysisRenderer.gs`, `AnalysisRendererForm.html`, updated `Launcher.gs` `doGet`, updated `LauncherForm.html`/`WritebackForm.html`/`AnalysisRendererForm.html` with nav). Bumped launcher deployment `AKfycbwCOsci...` from `@14` → `@15` ("M5 C2: analysis-render route + cross-page nav"); deployment ID + tinyurl URL stable. Operator-facing renderer live.
- **2026-05-06 (M5 C2 closure):** M5 C2 closed; active checkpoint pointer moved M5 C2 → M5 C4 (live operator validation). C3 dropped from the milestone plan — the upload-portal scope was wholly absorbed into C2's launcher Web App route per D-0063 + the cross-page nav per PR #116, so a separate C3 added no value (same discipline as M3 C2 dropped per D-0048 when C1 already met exit criteria). M5 milestone stayed Active pending C4 live-operator validation; C4 was operator-availability-bounded, not code-bounded.
- **2026-05-07 (M5 C4 closure + M5 milestone closure, PR #118):** Closed M5 C4 (live operator validation; D-0065) + closed M5 (the milestone). Operator ran the analyzer + renderer triad end-to-end on the May 2026 ICU/HD smoke-test snapshot (`--retention FULL`, 1000 candidates, 27-min pipeline, 116/116 filled, winner `totalScore -842.387`); analyzer ran in 0.13s producing 133KB `AnalyzerOutput`; uploaded via launcher's `?action=analysis-render` route; reviewed K=5 roster tabs + 1 comparison tab in source spreadsheet. Verdict per D-0065: analyzer's role as calibration framework **validated** — the comparison tab surfaced a load-bearing scoring-formulation insight (`pointBalanceGlobal` weight design) the operator would not have seen from `totalScore` alone, exactly the M5 thesis working as designed. D-0055 sub-decision 9 pivot resolved: scoring-formulation issues DID surface but the resolution path is operator-tunable weights via the existing Scorer Config tab (D-0037 surface), NOT a lexicographic / threshold / Pareto rework — long-term systematic weight elicitation parked as **FW-0033**, explicitly NOT rolled into M6 (LAHC-only) scope. **D-0064** also landed forward-going Task 1/2/3 cadence vocabulary (formalizing the M5 C1 "drop Phase" terminology fix per the 2026-05-05 entry above); historical "Phase 1/2/3" labels in past PR titles + closure entries (M2..M5 C2) preserved as-is. Active milestone pointer M5 → **M6** same-day per the companion M6 activation PR (D-0066, line below); active checkpoint pointer M5 C4 → **M6 C1**. README / AGENTS / blueprint / roadmap / decision_log / future_work / delivery_plan all swept.
- **2026-05-07 (M6 activation, PR #119):** Activated M6 (`Solver-side score-aware search (LAHC)`) + M6 C1 same-day per D-0066, immediately after M5 closure (line above) merged. Scope is LAHC-only: cloud Deep Solve + email-notification architecture + cloud-side FULL retention promotion (FW-0030) + scoring-formulation rework (FW-0033) all explicitly carved off to FW or future milestones. Strategy is internal to the solver boundary; two strategy-aware fields end up crossing the boundary at v2 — `strategyId` as a required input per §9 (clarified in D-0067 sub-decision 7) and the wrapper envelope's `solverStrategy` enumerant as an output (so the M5 analyzer + ops trail can see what ran). Maintainer-only operator-tunable surface (Python module constants for cloud defaults; CLI flag overrides for local tuning; no operator-facing UI changes). One contract change pinned: `docs/solver_contract.md` `contractVersion: 1` → `2` additive bump; concrete LAHC algorithm spec + sub-decisions defer to M6 C1 design thread (forthcoming PR; provisional D-0067). Validation loop = M5 analyzer's comparison tab. No code changes in this PR — direction-setting docs only, mirroring D-0049 / D-0055 / D-0065 patterns.
- **2026-05-07 (M6 C3 scope narrowed to local-first, this PR; D-0068):** D-0066 sub-decision 8 originally framed M6 C3 to include cloud-mode plumbing for LAHC. Narrowed to local-only per maintainer's 2026-05-07 direction during LAHC dry-run setup ("we don't think about cloud first - just aim to optimize local pipeline first"). C3 still delivers envelope-shape additive bump (`solverStrategy`) + local CLI flags + integration tests; cloud HTTP wrapper continues to ship `SEEDED_RANDOM_BLIND`-only in v1. Cloud-mode LAHC integration carved off to **FW-0035** (revisit triggers: local LAHC empirically validated on 3+ real cycles; maintainer requests pilot rollout; comparison data justifies operator-facing rollout). No code change in this PR (M6 C2 hasn't started yet, so there's no existing code to scope-back). No contract change beyond the M6 C1 solver-contract bump.
- **2026-05-07 (M6 C1 closure, this PR):** Closed M6 C1 (solver-strategy contract extension + LAHC design; D-0067). `docs/solver_contract.md` extended with §11.1 "Registered strategies" + new §12A (full LAHC algorithm spec) + editorial updates to §16 / §17 / §19 / §20 / §21. **D-0067** pins all five deferred sub-decisions from D-0066 (K-independent-seeds emission, idle/hard-iter inner termination, deterministic trajectory-seed derivation, `L=1000` / `idleThreshold=5000` / `maxIters=100k` defaults, `scoringConsultation: "READ_ONLY_ORACLE"` extension-clause activation) plus a restated rationale for D-0066 sub-decision 10's `contractVersion: 1` → `2` bump: outcome stands, but driven by §9 promoting `strategyId` to a required boundary input (input-shape change per §2) rather than by LAHC strategy registration (which would NOT have required a bump per §11.2). D-0066 sub-decision 10 receives a parenthetical restatement pointer to D-0067 in this PR. Envelope `solverStrategy` field location deferred to M6 C3. Active checkpoint pointer moves M6 C1 → M6 C2 (LAHC core impl). 225/225 Python tests still pass — C1 was docs-only, no code changes. README / AGENTS / blueprint / delivery_plan / decision_log all swept.
- **2026-05-08 (M6 C2 Task 2A, PR #126):** Extracted strategy abstraction in `python/rostermonster/solver/`: `_StrategyDescriptor` registry (`strategy_registry.py`) + `_StrategyOutcome` shared shape (`strategy.py`) so `SEEDED_RANDOM_BLIND` and `LAHC` plug into a shared dispatch surface; `LAHC` registered as a placeholder run function until Task 2B. SEEDED_RANDOM_BLIND output stays byte-identical post-refactor per `docs/solver_contract.md` §16 — pinned by determinism regression tests already in `test_solver.py`. No contract changes.
- **2026-05-08 (M6 C2 Task 2B, PR #127):** Implemented LAHC inner loop in new `python/rostermonster/solver/lahc.py` per `docs/solver_contract.md` §12A.1: seed roster via SEEDED_RANDOM_BLIND Phase 1+2; history-list `L=1000` default with overwrite (NOT max) per §12A.1.e; dual-clause accept criterion (`proposedScore >= currentScore` OR `proposedScore >= history[i mod L]`) per §12A.1.c; best-roster + idle-counter atomic update per §12A.1.f; termination at `idleThreshold` OR `maxIters` only per §12A.3 (bounded random sampler misses don't terminate the trajectory — only `idleThreshold`/`maxIters` exit the inner loop); swap + reassign mixed move generator per §12A.1.a (ergodicity over rule-engine-valid roster space — swap alone closes over seed-roster doctor set, reassign opens it); rule-engine evaluations counted into `placementAttempts` (seed-phase + inner-loop, both move-types' attempts including exhausted-then-fallback); per-trajectory diagnostics surface to `SearchDiagnostics` per §12A.9 (status, iters, accepted moves, best/terminal scores, history-list length, params, seed-derivation function). Whole-run failure aggregation per §12A.8 (LAHC drops failed trajectories rather than aborting; `_build_unsatisfied` preserves every per-trajectory `ValidationIssue` un-deduped, while `unfilledDemand` stays unit-deduped for operator-facing summary). PR #127 ran 7 Codex review rounds before clean — substantive findings on per-strategy failure aggregation, FixedAssignment-derived `fixed_coords`, ergodicity, diagnostics surfacing, dedup discipline, seed-phase counter fold-in, rule-engine evaluation counting, no-terminate-on-soft-misses. No contract changes.
- **2026-05-08 (M6 C2 Task 2C + closure, this PR):** Added byte-identical determinism (same-process + cross-`PYTHONHASHSEED`) + K-trajectory independence + bestRoster-vs-terminal divergence (best ≥ terminal invariant per §12A.1.f) + ICU/HD May 2026 fixture integration tests for LAHC. M6 C2 closes; active checkpoint pointer moves M6 C2 → M6 C3 (envelope / CLI integration; local-first per D-0068 — cloud-mode LAHC integration parked as FW-0035). 233/233 Python tests pass (228 baseline + 5 LAHC integration tests). No contract changes — implements D-0067 spec verbatim. README / AGENTS / blueprint / delivery_plan all swept.
- **2026-05-08 (M6 C3 Task 1 — envelope-shape additive bump, PR #129):** Settled D-0067 sub-decision 8's deferred envelope `solverStrategy` field location. `docs/selector_contract.md` §16 gains new §16.5 declaring two **optional** additive fields on `runEnvelope`: `solverStrategy` (enumerant per `docs/solver_contract.md` §11.1) + `solverStrategyConfig` (tagged-union of strategy-specific actual params used at run time — `{strategy: "SEEDED_RANDOM_BLIND"}` empty payload, or `{strategy: "LAHC", lahcParams: {historyListLength, idleThreshold, maxIters}}` for LAHC; future strategies extend cleanly via §11.2 extension clause). §9 item 3 + §16.2 + §16.4 updated to mention the optional fields. **No `contractVersion` bump** — additive optional per §16.3; prior-version-targeted readers can ignore the fields and still process the envelope correctly. Producer obligation: post-M6 C3 Task 1 producers MUST populate both fields together (Codex P2 round-1 + 2 fixes — no SEEDED_RANDOM_BLIND inference from absence; consumer obligation: treat missing as unknown / legacy). No code changes in this PR.
- **2026-05-08 (M6 C3 Task 2A — CLI flags + envelope population, PR #130):** Wired `--strategy {SEEDED_RANDOM_BLIND, LAHC}` (default SRB for byte-identical CI semantics) + `--lahc-history-length` / `--lahc-iter-cap` / `--lahc-idle-threshold` per `docs/solver_contract.md` §12A.7 in `python/rostermonster/run.py`. `pipeline.run_pipeline()` gains `strategy_id` + `lahc_params` kwargs threaded through to `solve()`. New `_build_run_envelope` populates `runEnvelope.solverStrategy` + `solverStrategyConfig` per §16.5 producer obligation; new `LahcParamsRecord` / `SeededRandomBlindStrategyConfig` / `LahcStrategyConfig` dataclasses in `selector/result.py`; `RunEnvelope.__post_init__` enforces the §16.5 both-or-neither + cross-field invariants at construction time. CLI usage-error handling: setting `--lahc-*` without `--strategy LAHC` exits 2 + stderr; invalid `LahcParams` (e.g., `--lahc-iter-cap 0`) caught at construction and surfaced as exit 2 + stderr (Codex P2 round-1 + 2 fixes — `or` → `is not None` to let explicit zero reach validation, then catch ValueError as CLI usage error). 240/240 Python tests pass (228 baseline + 6 new Task 2A tests). Empirical hint embedded in the LAHC tests: at seed=20260504 / K=1 / `--lahc-iter-cap 200`, score = -6.4 (vs SRB at same seed K=3 = -21.5).
- **2026-05-08 (M6 C3 Task 2B — integration tests + analyzer chain, PR #131):** Added 4 pipeline-level + analyzer-chain integration tests proving `--strategy LAHC` works end-to-end. `test_pipeline_lahc_strategy_full_retention_emits_sidecars` (LAHC + FULL retention emits sidecars + envelope carries §16.5 strategy metadata); `test_pipeline_lahc_byte_identical_under_fixed_seed` (§12A.4 byte-identical determinism at the pipeline composition level); `test_pipeline_strategy_choice_changes_winner` (regression guard against silent dispatch routing — uses `searchDiagnostics.lahcHistoryListLength` shape per Codex P2 round-1 fix, NOT score-equality which would false-fail on legitimate ties); `test_analyzer_consumes_lahc_full_envelope` (M6 → M5 chain compatibility — LAHC FULL envelope consumed by `analyze(...)` without special-casing per `docs/analysis_contract.md`'s solver-agnostic property). 244/244 Python tests pass (240 baseline + 4 new Task 2B tests). No code changes — tests-only PR.
- **2026-05-08 (M6 C3 Task 3 + closure, this PR):** Added **FW-0036** to `docs/future_work.md` capturing the LAHC operator-cycle K-defaults observation from M6 C3 closure characterization runs (scripts in `experimental/m6_lahc_comparison/`): SRB-K=100 ≈ LAHC-K=10 in wall time (~160s) but LAHC dominates by ~+50 score points on the May 2026 ICU/HD dev-copy; the SRB-era K=1000 default is operationally infeasible for LAHC at ~16s/trajectory. FW-0036 promotion waits for M6 C4 + 2+ cycle validation before pinning a separate `_DEFAULT_MAX_CANDIDATES_LAHC` constant in `pipeline.py`. M6 C3 closes; active checkpoint pointer moves M6 C3 → **M6 C4** (live operator validation via M5 analyzer — operator-availability-bounded; code is in place). README / AGENTS / blueprint / delivery_plan / roadmap / future_work all swept.
- **2026-05-08 (FW-0037 added — LAHC L-tuning methodology + two-step deep-solve pattern, this PR):** Post-M6 C3 closure characterization on the May 2026 ICU/HD fixture surfaced a second forward-work entry. **FW-0037** captures (i) empirical finding that §12A.5's default `L=1000` sits past the cliff in our iter-budget regime (history list barely refreshes before `idleThreshold=5000` cuts trajectories; documented "L too large for iter budget" failure mode per Bazargani & Lobo 2017 GECCO); (ii) mechanism confirmed via iter-by-iter trajectory trace at seed=1001 (current_score random-walks at L≥1000, best_so_far plateaus low — best is correctly preserved per §12A.1.f, the trajectory simply never reaches the optimum); (iii) statistical methodology pinning N=5 seeds for the L-tuning step via TOST equivalence (all plateau pairs equivalent at margin δ=±2), Wilcoxon power = 100% at N=5 (test-mechanics floor), and bootstrap stability with P(plateau pick) = 100% from N=1; (iv) two-step deep-solve pattern (Step 1: tune L per problem class one-time at ~11 min; Step 2: K-trajectory production run at the cached tuned L) explicitly chosen over per-trajectory adaptive LAHC (Bazargani & Lobo 2017) because adaptive overhead doesn't amortize across K trajectories or repeated runs. The optional `trace_callback` hook landed in `python/rostermonster/solver/lahc.py` during the trajectory-trace audit — kept in production (no runtime cost when not provided); useful for future debugging. FW-0037 promotion (operator-facing `tune-l` CLI + cache-by-problem-signature) waits for M6 C4 + 2+ cycle validation before being scoped as M7 work. Empirical scripts + PDFs in `experimental/m6_lahc_comparison/` (uncommitted per convention; PDFs synced to OneDrive `Claude_remote/`). No active-checkpoint change — M6 C4 stays active.
- **2026-05-09 (M6 C4 + M6 milestone closure, PR #134 PR-A code + this PR-B docs sweep):** Closed M6 C4 (closure scope shifted from a live operator-cycle comparison-tab cross-reference to dev-copy empirical characterization — the dominance signal was already overwhelming and the cloud benchmark surfaced a different load-bearing concern: single-thread bottleneck) + closed **M6** (the milestone). PR #134 PR-A landed `LahcParams.swapProbability: float = 0.5` (new field, default-preserving) + `--lahc-swap-probability` CLI flag + maintainer `apps_script/launcher/src/DeleteSheetsById.gs` utility, threaded swapProbability through audit surfaces (`SearchDiagnostics.lahcSwapProbability`, `LahcParamsRecord.swapProbability`, `runEnvelope.solverStrategyConfig.lahcParams.swapProbability`); `docs/solver_contract.md` §12A.7 + §12A.9 + `docs/selector_contract.md` §16.5 updated additively (no `contractVersion` bumps anywhere). Closure PR (this docs sweep) writes the milestone-closure narrative + adds **D-0069** (M6 C4 verdict + M6 milestone closure verdict — LAHC empirically validated as superior to SRB on the dev-copy fixture; sweet-spot defaults `L=10` / `idleThreshold=2000` / `swapProbability=0.5` / `K=10–20` captured but **not** pinned to module-level constants pending multi-fixture validation per FW-0036/FW-0037; active milestone moves to *none* with M7 — parallel solver — explicitly held in study before activation; cloud-mode LAHC integration stays parked at FW-0035 because pre-parallelization cloud rollout is less compelling than waiting on M7) + adds **FW-0038** (parallel solver — M7 candidate framing; goal: 64 K-trajectory runs in <5 min on cloud; structural parallelizability per §12A.2 K-trajectory independence; deployment shape — multiprocessing vs threading vs Cloud Run job parallelism vs Cloud Run concurrency — held for in-detail study). FW-0036 + FW-0037 updated with concrete dev-copy empirical evidence (K=10–20 sweet-spot; idleThreshold=2000 sweet-spot; swap_p=0.5 statistically equivalent to other in-plateau values). 249/249 Python tests pass. Active milestone pointer M6 → **none**; active checkpoint pointer M6 C4 → **none**. README / AGENTS / blueprint / roadmap / decision_log / future_work / delivery_plan all swept.
- **2026-05-10 (M7 C1 closure, this PR):** Closed **M7 C1** on GCP quota approvals (asia-southeast1, all three tickets returned same-day within minutes of filing): CPUS=300 (requested 2,520; partial), INSTANCES=350 (requested 350; full), C3_CPUS=108 (requested 2,520; partial — binding). Closure-K computed per D-0070 sub-decision 7's three-quota rule: `K_approved = min(K_target=2,500, floor(min(300, 108, 350×8) / 8) × 8) = min(2,500, floor(108/8)×8) = **104**`. M7 C2 Batch job spec will run at K=104 / taskCount=13 / all tasks fully packed at 8 trajectories each. K=104 is ~5× past best-of-K plateau (K=10-20 per FW-0036), so the architecture validates fine at this scale; future quota re-requests can lift the cap. Active checkpoint pointer moves **M7 C1 → M7 C2** (Cloud Batch implementation). 249/249 Python tests still pass (closure was docs-only). README / AGENTS / blueprint / roadmap / decision_log / future_work / delivery_plan all swept.
- **2026-05-10 (M7 activation, PR #136):** Activated **M7** (`Parallel solver — Cloud Batch + intra-request K-fanout`) + M7 C1 same-day per **D-0070**. Direction-setting docs PR pinning architecture (Cloud Batch + dense pack via `c3-highcpu-8` + `multiprocessing.Pool(8)` + on-demand pricing + sync UX with Cloud Run Service timeout LOWERED to 250s (per D-0070 sub-decision 5: 360s Apps Script wall = 10s extract + 250s cloud cap + 30s writeback + 60s renderAnalysis + 10s variance, exact fit at the tail)), machine family (C3 Sapphire Rapids — fastest per vCPU for pure-Python LAHC compute), `K=2,500` target (or whatever GCP approves quota at — best-of-K plateau is K=10-20 per FW-0036 so any K ≥ ~50 sits ~3-5× past plateau, K ≥ ~100 sits ~5-10× past — comfortable statistical sufficiency), FW-0037 elbow tuple (`L=50` / `idleThreshold=3500` / `swapProbability=0.5`). FW-0037 idle elbow updated 2000 → 3500 to match the maintainer's direct read of the sweep PDFs (the earlier "sweet-spot ≈ 2000" wording was a quality-cost minimum-cost call; 3500 is where the score-vs-idle curve actually bends). FW-0038 status flips from "M7 candidate framing — held in study" → "promoted to M7 C1 implementation target" (entry retained as the design-rationale source-of-truth). Cloud Engine API enabled on `rostermonsterv2` GCP project; current asia-southeast1 vCPU quotas surveyed (CPUS=100, INSTANCES=24, C3_CPUS=24, PREEMPTIBLE_CPUS=0). Quota request tickets to be filed as M7 C1 Task 2 (out-of-band). FW-0035 (cloud-mode LAHC integration) co-promotes with M7 closure (M7 makes cloud LAHC viable at sync UX — FW-0035 is no longer a future gate, it's effectively delivered alongside M7 C3). No code changes in this PR — direction-setting docs only, mirroring D-0049 / D-0055 / D-0066 patterns. README / AGENTS / blueprint / roadmap / decision_log / future_work / delivery_plan all swept.
- **2026-05-11 (M7 C3 Task 1 — async architecture decision lock, this PR):** Locked **D-0071** — M7 C3 architecture pivot from sync UX to async UX via Cloud Batch finalizer task group + email notification + B-prime callback. 17 sub-decisions covering: (1) C3 scope pivot (sync→async, docs only); (2) Cloud Run = thin front door + Cloud Batch = workers + finalizer; (3) result delivery via launcher Web App callback `/exec?action=async-render-callback` reusing existing `RMLib.applyWriteback + RMLib.renderAnalysis`; (4) GCP ID-token auth (auto-rotation; maintainer's "fire once and forget" preference); (5) always-email-on-every-outcome with `[RosterMonsterV2] Success/Failure: <tab>` subject + technical-details block + AnalyzerOutput JSON attachment; (6) operator email captured via `Session.getActiveUser().getEmail()`; (7) **10-min total hard cap** enforced by finalizer self-check + per-task `maxRunDuration` raised 180s → 540s; (8) concurrent-run rejection by spreadsheet ID via Cloud Batch labels; (9) one-time toast post-submit ("Submitted, we'll email you, can take up to 10 minutes"); (10) always-email-on-failure for every non-success path (parser rejection sync, all-workers-failed via finalizer, 10-min timeout via finalizer self-check, scorer/selector/analyzer raised, callback POST fails after 3 retries with exponential backoff + fallback email with attached AnalyzerOutput + writebackEnvelope JSONs); (11) maintainer status via gcloud CLI only at v1; (12) finalizer task group runs regardless of worker success/failure (`condition: SUCCEEDED_OR_FAILED`); (13) SRB cloud path hidden from operator menu, kept in code as legacy/research; (14) maintainer-only `/compute-lahc-test` debug route kept; (15) bound shim menu LAHC-only at M7 C4; (16) **FW-0027 absorbed into M7 C3+C4** per this entry (mirrors how D-0070 absorbed FW-0030 + FW-0035 into M7 C3); (17) **new FW-0039 — finalizer-crash watchdog** for the gap where finalizer Python code crashes BEFORE the email-send line (accepted at v1, deferred to post-pilot scale). Files touched: `docs/decision_log.md` (D-0071 entry); `docs/future_work.md` (FW-0027 absorbed marker + new FW-0039); `docs/cloud_compute_contract.md` (§8.7 finalizer task group additions + §9 SUBMITTED state + operatorEmail input field + §10 SUBMITTED response state + new async-callback contract section, all additive — no `contractVersion` bump); `docs/delivery_plan.md` §6 + §7 + §9 + §11 + §15; `docs/roadmap.md` M7 entry C3/C4/C5 scope refresh; `README.md` + `AGENTS.md` active-checkpoint narrative; `docs/blueprint.md` §6 implementation-ownership note; project memory `project_m2_c9_setup_ids.md` (M7 C3 async design notes). **No code changes** — direction-setting docs only, mirroring D-0049 / D-0055 / D-0066 / D-0070 patterns. **Active checkpoint pointer moves M7 C3 → M7 C4** (async UX implementation per D-0071). M7 C4 task cadence pinned at M7 C4 Task 1 (docs lock).
- **2026-05-11 (M7 C2 closure, this PR):** Closed **M7 C2 — Cloud Batch implementation** on the §12A.4 byte-identity exit criterion proven at T2G (5/5 audit tests; local-CLI vs Cloud-Batch produce identical winning assignments + identical full writebackEnvelope across positive seeds, multiple seeds, the load-bearing negative-seed `_UINT64_MASK` case, and the `+seed`/`-seed` distinct-output regression guard). M7 C2 delivered the full Cloud Batch parallel-LAHC chain across 7 PRs: T1 docs lock (PR #138; §8.7 + §12A.10 pinned); T2A `derive_K_seeds` shared helper (PR #139; `python/rostermonster/solver/seeds.py`); T2B Cloud Batch infra + smoke (out-of-band, no PR; `gs://rostermonsterv2-lahc` bucket, `iam.serviceAccountUser` self-grant, 2-task `gcloud batch jobs submit` round-trip); T2C `solve()` `_candidate_seeds` private override (PR #140; the orchestrator/worker escape hatch + §9 re-cadence to T2A..T2G); T2D worker module (PR #141; `python/rostermonster_service/worker.py` + §8.7 result.json schema); T2E Cloud Batch job-spec builder (PR #142; `python/rostermonster_service/batch_job_spec.py` encoding all §8.7 invariants); T2F orchestrator wiring + maintainer-only `/compute-lahc-test` route (PR #143; `python/rostermonster_service/lahc_orchestrator.py` + `batch_client.py` + extracted `gcs.py`); T2G determinism re-audit + scorer/selector/wrapper envelope assembly + concurrent-replay race closure via attempt-id validation (PR #144; closes the T2F-deferred P1 + adds the §13 byte-identity audit). Codex review surfaced 8+ legitimate findings across the chain (truncation collisions, Batch duplicate-job IDs, stale GCS reads, env-typo Flask 500s, lazy-import wrapping, ms-collisions, per-trajectory diagnostics gaps, CR floor parity, parser-rejection pre-dispatch routing, full-envelope audit) — all addressed inline; the orchestrator surface is significantly more robust than the initial T2F draft. **Architecture delivered:** Cloud Batch dense-pack (`c3-highcpu-8` × `multiprocessing.Pool(8)` × 1 LAHC trajectory per vCPU, K_approved=104 → 13 fully-packed tasks) inside a sync UX with the orchestrator's 240s polling deadline + 180s per-task `maxRunDuration` per §8.7. **Determinism preserved:** local-CLI ↔ Cloud-Batch byte-identical writebackEnvelope at the same `(masterSeed, K)`, including the load-bearing `_UINT64_MASK` negative-seed semantics. **Concurrent-replay race closed:** orchestrator stamps per-call `attemptId` into seeds.json; worker echoes back into result.json; aggregation validates on read so two parallel replays of the same `(snapshot, seed)` cannot pollute each other's K' aggregation. **Files added (production):** 5 new modules in `python/rostermonster_service/` (worker.py, lahc_orchestrator.py, batch_client.py, batch_job_spec.py, gcs.py); §8.7 amendment in `docs/cloud_compute_contract.md` pinning seeds.json + result.json schemas with attemptId field; §12A.10 amendment in `docs/solver_contract.md` pinning the shared `derive_K_seeds` helper. **Tests added:** test_seeds.py (22), test_batch_client.py (10), test_batch_job_spec.py (25), test_worker.py (17), test_lahc_orchestrator.py (25), test_lahc_determinism_audit.py (5) — all green; pre-M7-C2 baseline 249/249 → 18 test files / 367+ tests at closure. **Active checkpoint pointer moves M7 C2 → M7 C3** (sync UX + bound shim wiring + cloud_compute §9/§10 amendments + analyzer integration). M7 C3 task cadence will be locked at M7 C3 Task 1 (docs lock) per D-0064. M7 itself stays Active pending C3 + C4 closure. README / AGENTS / blueprint / roadmap / decision_log / future_work / delivery_plan all swept; project memory `project_m2_c9_setup_ids.md` updated to reflect M7 stack live (worker + orchestrator + Cloud Batch infra all running). No `contractVersion` bumps anywhere across M7 C2 — additive deployment-posture pinning under cloud_compute §8.7 + additive solver §12A.10 helper + additive `_candidate_seeds` private override on a private surface.
- **2026-05-10 (M7 C2 Task 2G determinism re-audit + scorer/selector/wrapper wiring + concurrent-replay race closure, this PR):** Three deliverables landed together. **(1) Concurrent-replay race fix (T2F deferred P1 closure):** orchestrator generates a per-call `attemptId` (uuid4 hex), stamps it into every per-task `seeds.json`; worker reads the field + echoes back into `result.json`; aggregation validates `result.attemptId == expected_attempt_id` on read with mismatches treated as missing per §8.7's primary K' definition. Closes the concurrent-replay race window the T2F clear-then-write fix couldn't address (two orchestrator calls running in parallel against same `(snapshot, seed)` can no longer pollute each other's K' aggregation — at worst one attempt sees its inputs stomped → UNSATISFIED, never mixed/wrong rosters). `docs/cloud_compute_contract.md` §8.7 amended with the new `attemptId` field on both seeds.json + result.json schemas. **(2) Scorer + selector + wrapper-envelope wiring:** new helper `_build_post_aggregation_envelope(...)` in `lahc_orchestrator.py` re-parses the snapshot, reconstructs `CandidateSet` from per-task aggregated candidates (`_assignment_unit_from_dict` deserializes the worker's JSON dicts back to `AssignmentUnit` objects), scores via `scorer.score(...)`, builds `SearchDiagnostics` with LAHC-specific fields populated from per-task data, builds `RunEnvelope` with the `LahcStrategyConfig` matching the FW-0037 elbow tuple, runs `selector.select(...)`, assembles wrapper via `_assemble_writeback_wrapper(...)`. The `/compute-lahc-test` route now returns the same `writebackEnvelope` shape as `POST /compute`. **(3) Determinism re-audit per `docs/solver_contract.md` §12A.4:** new test file `python/tests/test_lahc_determinism_audit.py` runs the local-CLI path (`pipeline.run_pipeline(strategy_id=LAHC, seed=masterSeed)`) and the Cloud-Batch path (`orchestrate_lahc_run(master_seed=masterSeed, ...)` with a `WorkerSimulatingBatchClient` that invokes `worker_main` inline) end-to-end and asserts identical winning assignments. 4 tests cover positive seed, multiple positive seeds (1, 999, 42), the load-bearing negative-seed `_UINT64_MASK` case (master_seed=-12345), and a `+seed` vs `-seed` distinct-output regression guard mirroring `test_solver.py::test_negated_seed_produces_distinct_output` for the Cloud-Batch path. All 4 audit tests pass — **M7 C2's byte-identity exit criterion is met**. New tests this PR: 2 in `test_worker.py` (attemptId echo + null handling), 2 in `test_lahc_orchestrator.py` (mismatched-attemptId filter + seeds.json attemptId stamp), 4 in `test_lahc_determinism_audit.py`. Full Python suite 18/18 files green (was 17/17 + 1 new file = 18; individual test count: 344/344). No `contractVersion` bumps anywhere — additive `attemptId` field under cloud_compute §8.7 + additive scorer/selector/wrapper wiring inside the orchestrator (no boundary input/output changes). Active checkpoint pointer stays at M7 C2 (Task 3 closure queues next: docs sweep — README / AGENTS / blueprint / roadmap / decision_log / future_work / project memory all reflecting M7 C2 closure-K=104 + Cloud-Batch path delivered + determinism preserved).
- **2026-05-10 (M7 C2 Task 2F LAHC orchestrator wiring, this PR):** Landed the M7 LAHC Cloud Run orchestrator end-to-end behind a maintainer-only `POST /compute-lahc-test` route per `docs/cloud_compute_contract.md` §6.2 (route is removed when M7 C3 amends §9 to recognize `solverStrategy="LAHC"` publicly). New modules: `python/rostermonster_service/gcs.py` (extracted shared `make_gcs_adapter(bucket)` from the worker so both worker + orchestrator import the same adapter); `python/rostermonster_service/batch_client.py` (production `BatchClient` wrapping `google.cloud.batch_v1` lazy-imported + `InMemoryBatchClient` test double with deterministic state-sequence transitions); `python/rostermonster_service/lahc_orchestrator.py` with `orchestrate_lahc_run(...)` that derives runId from `(snapshotId, masterSeed)` (sanitized to Cloud Batch job_id constraints, length-capped at 63), pre-derives all K_approved seeds via `derive_K_seeds(masterSeed, K_approved)` per §12A.10, partitions into per-task slices of up to 8 per the §8.7 dense-pack invariant, writes snapshot.json + per-task seeds.json files to GCS at the §8.7 key paths, builds the Batch job spec via T2E's `build_lahc_batch_job_spec(...)` and submits, polls `batch.jobs.get` at 3s cadence with the §8.7 240s orchestrator-side completion deadline (calls `cancel_job` on overrun, distinguishes `CANCELLED_OVER_DEADLINE` from a maintainer-issued `CANCELLED`), reads per-task `result.json` files (missing/unreadable → 0 candidates per §8.7's primary K' definition), aggregates `K' = sum(len(candidates))` + `dropped_count = K_approved - K'`, returns a structured response dict surfacing the K' summary + raw aggregated candidates. `app.py` adds the `POST /compute-lahc-test` route gated by the same operator-allowlist auth as `POST /compute`, with deploy-time env vars `CONTAINER_IMAGE_URI` (the Cloud Run image URI to dispatch into worker mode), `GCP_PROJECT` (the Batch project ID), and optional `LAHC_K_APPROVED` (overrides the M7 C1 closure-K=104 default for staging). All I/O ports (BatchClient, GCS read/write, sleep, time) are injectable for testability — production wires real adapters; tests pass `InMemoryBatchClient` + in-memory dict-backed GCS + `_no_sleep` + virtual clock. Wrapper-envelope assembly (scorer → selector → final envelope) is NOT in T2F per the §9 cadence — that lands at T2G alongside the determinism re-audit, which needs the scored winner to verify byte-identity vs the local CLI; T2F's response is a raw K' summary suitable for maintainer inspection. `cloud_compute_service/requirements.txt` adds `google-cloud-batch>=0.17,<2.0`. 18 new tests in `python/tests/test_lahc_orchestrator.py` (runId derivation + sanitization + length cap + negative-seed encoding, partition math at K=104 / K=2,500, happy-path orchestration with pre-seeded result.jsons, GCS key-path completeness, aggregate-counter sums, deadline cancellation, partial-failure tolerance with missing per-task result.json, K'==0 → UNSATISFIED, full worker-integration round-trip where a `WorkerSimulatingBatchClient` invokes `worker_main` inline against the same in-memory GCS, COMPUTE_ERROR for missing snapshotId, masterSeed/K_approved validation), 5 new tests in `python/tests/test_batch_client.py` (terminal-state set, state-string constants, InMemoryBatchClient submit/poll/cancel semantics + sequence advancement). Existing `test_worker.py` uses the new shared `gcs.make_gcs_adapter` after the refactor; full Python suite 336/336 (was 313/313 + 23 new across the two new test files). No `contractVersion` bumps anywhere — additive deployment-posture wiring under cloud_compute §6.2 + §8.7 + the new test route. Active checkpoint pointer stays at M7 C2 (Task 2G queues next: scorer + selector + wrapper envelope assembly + determinism re-audit byte-identity check vs the local CLI).
- **2026-05-10 (M7 C2 Task 2E Cloud Batch job-spec builder, this PR):** Landed `python/rostermonster_service/batch_job_spec.py` — the pure-function spec builder the M7 C2 Task 2F orchestrator passes to Cloud Batch v1 `jobs.create`. `task_count_for_K(K_approved)` implements D-0070 sub-decision 7's `ceil(K_approved/8)` rule (current production K=104 → 13 tasks; full quota K=2,500 → 313 tasks); `build_lahc_batch_job_spec(...)` returns the §8.7-conformant dict with all invariants encoded as defaults: `c3-highcpu-8`, `taskCountPerNode=1`, on-demand (`STANDARD` provisioning), `asia-southeast1`, per-task `maxRunDuration: "180s"`, per-task `maxRetryCount: 1`, `parallelism = taskCount`, full-VM compute claim (`cpuMilli=8000`, `memoryMib=14000` leaving ~2 GB OS headroom), Cloud Logging logs destination. Single-image dispatch via `commands[]` override invoking `python -m rostermonster_service.worker --run-id <runId>` per the M7 C2 Task 2D worker module entry; `BATCH_TASK_INDEX` flows from Cloud Batch's per-task env (no need to encode in the override). Bucket name flows via per-task `LAHC_BUCKET` env so deploys can override staging buckets without re-baking the image. 25 new tests in `python/tests/test_batch_job_spec.py` covering task-count derivation (K=104, K=2,500, K=8/16/296, K=1, edge cases), every §8.7 invariant individually (machineType, taskCountPerNode, provisioning, region, maxRunDuration, maxRetryCount, logs destination, compute resource), single-image dispatch correctness (`commands[]` shape + image URI passthrough + bucket env passthrough), boundary validation (empty/non-string `run_id` / `container_image_uri` / `bucket`, non-positive `K_approved`, negative `per_task_max_retry_count`, bool variants of int args). Full Python suite 313/313 (was 288/288 + 25 new). No `contractVersion` bumps anywhere — the spec assembly is a deployment-posture surface under cloud_compute §8.7, additive per §11. Active checkpoint pointer stays at M7 C2 (Task 2F queues next: orchestrator wiring — derive K seeds, partition + write per-task seeds.json + snapshot.json to GCS, submit Batch job using this spec, poll, aggregate, return).
- **2026-05-10 (M7 C2 Task 2D worker module, this PR):** Landed `python/rostermonster_service/worker.py` — the production Cloud Batch worker that the §8.7 single image dispatches into when invoked with `python -m rostermonster_service.worker --run-id <runId>`. Per-task flow: read snapshot.json + per-task seeds.json from GCS at the §8.7 key paths → parse + build LAHC inputs (FW-0037 elbow tuple `L=50` / `idleThreshold=3500` / `swapProbability=0.5` hardcoded per the M7 production lock) → `multiprocessing.Pool(8)` where each child calls `solve(strategyId=LAHC, terminationBounds=K=1, _candidate_seeds=[its_one_seed], lahcParams=<elbow tuple>, ...)` per the M7 C2 Task 2C escape hatch → aggregate per-trajectory results into per-task `result.json` → write to GCS at the §8.7 result key. Per-trajectory exceptions are isolated (caught + surfaced in optional `trajectoryExceptions` block) so one child raising doesn't kill the other 7's results, per §12A.8 drop-and-continue. I/O is fully injectable for testability — production uses a `google.cloud.storage`-backed adapter (lazy-imported); tests pass an in-memory dict-backed adapter + a serial pool executor. CLI defaults `--task-index` to the `BATCH_TASK_INDEX` env var Cloud Batch sets per-task. Single-image discipline preserved per `docs/decision_log.md` D-0050: Cloud Run keeps `gunicorn rostermonster_service.app:app`; Cloud Batch overrides `taskSpec.runnables[0].container.commands[]` to dispatch into worker mode. `docs/cloud_compute_contract.md` §8.7 amended with the per-task `seeds.json` + `result.json` schemas (orchestrator → worker + worker → orchestrator); `cloud_compute_service/requirements.txt` adds `google-cloud-storage>=2.18,<4.0`. 15 new tests in `python/tests/test_worker.py` covering: round-trip via in-memory GCS adapter, candidate-field schema invariants, byte-identity across calls (§12A.4 through the worker), per-trajectory exception isolation, oversized / empty / non-list seeds rejection at the boundary, aggregate-counter semantics, CLI exit-code + env-var defaulting, real-adapter URI guard. Full Python suite 288/288 (was 273/273 + 15 new). Dockerfile unchanged (worker runs as a module entry; no CMD bake-in needed since the §8.7 single-image strategy uses `commands[]` override on the Batch side). No `contractVersion` bumps anywhere — additive deployment-posture pinning under cloud_compute §8 (the seeds.json + result.json schemas are §8.7 surfaces, additive per §11), no boundary input/output changes. Active checkpoint pointer stays at M7 C2 (Task 2E queues next: orchestrator-side Cloud Batch job spec).
- **2026-05-10 (M7 C2 Task 2B infra-smoke + Task 2C `_candidate_seeds` override, this PR):** Two M7 C2 work units landed together. **Task 2B (out-of-band, no code):** brought the §8.7 Cloud Batch infra live — created bucket `gs://rostermonsterv2-lahc` (asia-southeast1, uniform bucket-level access, 90-day delete lifecycle); enabled the Cloud Batch API on `rostermonsterv2`; granted `roles/iam.serviceAccountUser` self-impersonation on the Compute SA `693837275969-compute@developer.gserviceaccount.com` (the only §8.7 IAM grant NOT already covered by the SA's existing legacy `roles/editor` per empirical check); ran a 2-task `gcloud batch jobs submit` smoke job using `google/cloud-sdk:slim` container runnable with a `gsutil cp` round-trip — both tasks succeeded, expected per-task `result.json` files landed at `gs://rostermonsterv2-lahc/smoke/task-{0,1}/result.json` with correct `BATCH_TASK_INDEX` flow. Validated end-to-end: bucket access + IAM `actAs` + `c3-highcpu-8` allocation in asia-southeast1 + `taskCountPerNode: 1` honored + container `commands[]` override works. Cost ~$0.04 (2 × c3-highcpu-8 × ~2min). **Task 2C (this PR's code scope):** added `_candidate_seeds: list[int] | None = None` private kwarg to `solve()` in `python/rostermonster/solver/solver.py`. When provided, `solve()` bypasses the `derive_K_seeds(seed, maxCandidates)` step and uses the supplied list directly; length MUST match `maxCandidates`; underscore prefix marks as orchestrator/worker-only — public callers continue to pass `seed` alone. The Task 2D Cloud Batch worker calls `solve(_candidate_seeds=its_8_seeds, terminationBounds=K=8, seed=masterSeed, ...)` per pool child with the per-task slice the orchestrator pre-derived via `derive_K_seeds`. 6 new tests in `python/tests/test_solver.py` cover byte-identical equivalence to the default derive path (the cross-surface invariant the orchestrator/worker rely on), distinct-override-distinct-output, length mismatch rejection, non-list rejection, non-int element rejection, bool element rejection. Full Python suite 273/273 (was 267/267 + 6 new). **Re-cadence:** §9 task list expanded from `T1 / T2A..2E / T3` to `T1 / T2A..2G / T3` to interleave the new T2B (smoke) + T2C (override) ahead of the worker module (now T2D); old T2B/2C/2D/2E renumbered to T2D/2E/2F/2G. The split surfaces infra gaps before the worker code consumes the same surface + reduces per-PR blast radius. No `contractVersion` bumps anywhere — `_candidate_seeds` is private (additive escape hatch on a private surface; §12A.2 K-trajectory independence invariant unchanged).
- **2026-05-13 (M7 C5 + M7 C4 + M7 milestone closure, this PR):** Closed **M7 C5** + **M7 C4** + **M7** per **D-0072** on live operator validation across both real ICU and HD cycles. All four §7 C5 exit criteria passed — bound shim SUBMITTED toast → `[RosterMonsterV2] Success: <tab>` email with AnalyzerOutput JSON attachment → writeback + 5 roster tabs → analyzer comparison tab (closing the D-0069-deferred operator-cycle cross-reference pathway as a natural side-effect of M7 C4's inline-finalize-step analyzer plumbing). Two-cycle live-cycle validation gives multi-fixture grounding on top of the M6 C4 dev-copy + M7 C2 byte-identity audit foundation. **FW-0027 + FW-0030 + FW-0035 + FW-0038 all DELIVERED via M7 per D-0072** — status flips from "absorbed into M7" / "promoted to M7 C1 implementation target" → "delivered via M7" on each; FW-0039 + FW-0040 stay parked. **Active milestone pointer M7 → none**; **active checkpoint pointer M7 C5 → none**. Mirrors the post-M4 / post-M6 deliberate-study-window cadence; no successor pre-decided. README / AGENTS / blueprint / roadmap / decision_log / future_work / delivery_plan all swept. No `contractVersion` bumps anywhere across M7 C4 + C5 — all contract amendments settled additively at M7 C3 (PR #147).
- **2026-05-10 (M7 C2 Task 1 docs lock, this PR):** Locked the M7 C2 design decisions surfaced in the post-D-0070 sequencing conversation (after M7 C1 closure, before code-side M7 C2 Task 2A starts). `docs/cloud_compute_contract.md` §6.2 acknowledges maintainer-only test routes (the LAHC Cloud Batch test path lands at M7 C2 Task 2D and is removed when §9 is amended at M7 C3); new §8.7 pins Cloud Batch posture for the LAHC strategy path — bucket `rostermonsterv2-lahc` in asia-southeast1 with 90-day lifecycle, GCS object naming convention per `runId` + `task_index`, Cloud Batch job spec invariants (`c3-highcpu-8`, on-demand, **`taskCountPerNode=1`** so 1 task = 1 VM and the dense-pack math holds, **per-task `maxRunDuration: 180s` with `maxRetryCount: 1`**), **240s orchestrator-side completion deadline** implemented as a polling loop + `batch.jobs.cancel` on overrun (NOT a job-spec field — Batch `Job` schema has no job-level `maxRunDuration`), full IAM additions on the default Compute SA (acting as both job creator + Batch task identity): `roles/batch.jobsEditor` project-wide (job creator) + `roles/batch.agentReporter` project-wide (Batch task VM agent state reporting) + `roles/iam.serviceAccountUser` on the SA itself (self-impersonation for `actAs` during `submitJob`) + `roles/storage.objectAdmin` bucket-scoped (read/write run artifacts on both surfaces); single-image dispatch via `--worker-mode`, partial-failure tolerance with K' defined as `sum(len(candidates_per_completed_task))` (source-of-truth, covers task-level failures + per-trajectory §12A.8 seed-construction drops within completed tasks; `dropped_count = K_approved - K'` is the derived diagnostic; whole-run UNSATISFIED only fires when K' == 0), GCS-only data path (Drive is NOT a pipeline data path). The optional `candidates_full.json` artifact is reframed as **out-of-band maintainer-audit additive to D-0070 sub-decision 10**: D-0070's "no serialization needed" claim refers to in-pipeline analyzer dependency (analyzer always runs on in-memory aggregation, unchanged); the GCS serialization is a fire-and-forget side-effect for forensic replay (`python -m rostermonster.analysis` against historical runs) and MUST NOT block the operator response or be a dependency for the analyzer pass-through. `docs/solver_contract.md` new §12A.10 pins the shared `derive_K_seeds(masterSeed: int, K: int) -> list[int]` helper with location at `python/rostermonster/solver/seeds.py` (separate module, refining D-0070 sub-decision 8's reference to `solver.py`), preserving the existing `_UINT64_MASK` negative-seed semantics. `docs/delivery_plan.md` §7 + §9 task list restructured to D-0064 cadence (Task 1 docs lock → Task 2A-2E code → Task 3 closure). Project memory `project_m2_c9_setup_ids.md` updated with M7 additions section. Cloud Run Service memory bumped 512MB → 1GB during M7 C2 Task 2D for K=104 candidate-set safety margin (~$0.005 per run cost delta, trivial). Renderer wall-time benchmark scoped as M7 C3 prep (operational TBD, ahead of M7 C3 wiring; uses existing M5 C4 FULL-retention sample). Cost budget alerts ($5/month with %-tier alerts at 1× / 2× / 3× / 5× / 10× / 20×) to be configured by maintainer as out-of-band GCP setup. No `contractVersion` bumps anywhere — additive deployment-posture pinning under cloud_compute §8 + additive solver §12A.10 helper. No code changes in this PR — Task 1 docs only.

## 13. Relationship to other repo docs
- `README.md` = front door orientation.
- `docs/blueprint.md` = stable architecture truth.
- `docs/roadmap.md` = milestone-level delivery order.
- `docs/delivery_plan.md` = active execution guide.
- `docs/decision_log.md` = accepted directional decisions.
- `docs/open_decisions.md` = pending decisions surfaced by implementation, awaiting closure.
- `docs/future_work.md` = non-normative parking lot for ideas.
- Contract docs = normative technical boundary definitions.

## 14. Maintenance rules for this document
- Keep exactly one active milestone unless there is a deliberate exception.
- Keep exactly one active checkpoint unless there is a deliberate exception.
- Update this document whenever execution focus changes.
- Do not duplicate normative contract wording here.
- Do not turn this document into a full issue tracker.
- If a task does not support the active checkpoint, it likely does not belong here.
- Forward-going checkpoint task cadence is **Task 1 (docs) / Task 2 (code, with optional Task 2A / 2B / 2C sub-letters when work has logically distinct chunks) / Task 3 (closure)** per D-0064. Historical "Phase 1/2/3" labels in §11 closure entries (M2..M5 C2) are preserved as-is.
- Cross-contract consistency audits MUST trace every producer-consumer seam in BOTH directions. Forward: when contract X says it consumes Y, verify Y's producer contract declares Y as a normative output. Reverse: when contract X references producing Y for a downstream consumer, verify the consumer's contract declares Y in its input shape. The reverse direction is the harder one to remember (the M2 C2 contract-surface audit missed `scorer_contract.md` §15 ↔ `parser_normalizer_contract.md` §9 because of it; the gap surfaced four contract-touches later, during M2 C4 T2 implementation, instead of at the C2 audit window).

## 15. Initial seed content
Reflects the 2026-05-13 closure of **M7 C5** + **M7 C4** + **M7** itself per **D-0072** — full async UX validated end-to-end on real live ICU + HD operator cycles. All four §7 C5 exit criteria passed: toast → email-with-AnalyzerOutput attachment → writeback + 5 roster tabs → analyzer comparison tab (closing the D-0069-deferred operator-cycle cross-reference pathway as a natural side-effect of M7 C4's inline-finalize-step analyzer plumbing). Two-cycle live validation gives multi-fixture grounding atop the M6 C4 dev-copy + M7 C2 byte-identity audit foundation. **FW-0027 + FW-0030 + FW-0035 + FW-0038 all DELIVERED via M7 per D-0072**; FW-0039 + FW-0040 stay parked. **Active milestone moves to *none*** mirroring the post-M4 / post-M6 deliberate-study-window cadence — no successor pre-decided. On top of the 2026-05-12 closures of M7 C3 (D-0071 async architecture lock + Codex P1.7 single-VM amendment) + M7 C4 code-implementation track, the 2026-05-11 closure of M7 C2 (Cloud Batch parallel-LAHC chain), the 2026-05-10 closures of M7 C1 (GCP quota approvals) and M7 activation per D-0070, and the 2026-05-09 closure of M6 (LAHC empirically validated per D-0069).

- active milestone **none** *(M7 closed 2026-05-13 per D-0072 — live async UX validated end-to-end on real ICU + HD operator cycles)*; active checkpoint **none**. M7 closure trail: **D-0070 → D-0071 → D-0072**. The single-VM dense-pack architecture (`c3-highcpu-88` running `multiprocessing.Pool(88)` for K=88 trajectories + inline finalize step in `worker.py` after `Pool.close() + .join()`) is now the production cloud LAHC path; operator clicks `Roster Monster → Solve Roster` → toast → operator goes off → 3-10 min later receives `[RosterMonsterV2] Success: <tab>` email with AnalyzerOutput JSON attachment → opens spreadsheet → sees writeback tab + 5 roster tabs + 1 analyzer comparison tab. SRB sync path stays as the back-compat fast path for the legacy `solverStrategy=SEEDED_RANDOM_BLIND` request shape (kept in code for benchmarks; hidden from operator menu per D-0071 sub-decision 13). 10-min operator-facing cap enforced via finalize-step self-check at 510s elapsed since `RM_SUBMIT_TIMESTAMP_MS`; task `maxRunDuration: 660s` is a Batch-level safety net intentionally above the 600s self-check ceiling; `maxRetryCount: 0` (single attempt). FW-0037 elbow tuple locked at the M7 production config: `L=50` / `idleThreshold=3500` / `swapProbability=0.5`. K=88 reflects the largest c3-highcpu-X size that fits the current C3_CPUS=108 quota — future quota bump unlocks K=176 (FW-0040 dial; stays parked at closure because K=88 vs K=176 is a quality wash per FW-0036's plateau evidence). Cloud Run per-instance concurrency, threading, and Spot pricing explicitly excluded per D-0070 sub-decisions. Post-M7 next-work study window is deliberate (mirrors post-M4 / post-M6 cadence); candidate threads include FW-0033 (scoring-weight elicitation), FW-0036/FW-0037 multi-fixture promotion, FW-0028 (observability hardening), FW-0031/FW-0032 (analyzer extensions), operational broadening beyond ICU/HD-first scope.
- closed milestones: **M1 (C1–C4) + M1.1 (C1) + M2 (C1–C9) + M3 (C1) + M4 (C1) + M5 (C1, C2, C4 — C3 dropped per D-0063) + M6 (C1, C2, C3, C4) + M7 (C1, C2, C3, C4, C5)**, anchored by **D-0019..D-0072** with closure entries in §11.
- M7 closure trail: **D-0070** (M7 framing — parallel solver via Cloud Batch + intra-request K-fanout; sync UX original framing) → **D-0071** (M7 C3 architecture pivot — sync UX → async UX via Cloud Batch finalizer task group + email notification + B-prime callback; FW-0027 absorbed; 10-min hard cap; reject concurrent same-spreadsheet runs; ID-token-in-body auth on launcher callback; Codex P1.7 amendment narrowed to single-VM with inline finalize) → **D-0072** (M7 C5 verdict + M7 milestone closure — full async UX validated end-to-end on real live ICU + HD operator cycles; comparison-tab cross-reference D-0069 deferred lands as side-effect of M7 C4 analyzer plumbing; FW-0027/0030/0035/0038 all delivered via M7).
- M6 closure trail: **D-0066** (M6 framing — solver-side score-aware search; LAHC as the first alternative strategy; strategy-internal-to-solver framing; maintainer-only operator-tunable surface; cloud Deep Solve + email-notification + FW-0030 + FW-0033 explicitly carved off) → **D-0067** (M6 C1 LAHC algorithm spec — K-independent-seeds emission, idle/hard-iter inner termination, deterministic trajectory-seed derivation, `L=1000` / `idleThreshold=5000` / `maxIters=100k` defaults, `scoringConsultation: "READ_ONLY_ORACLE"` extension-clause activation; restates D-0066 sub-decision 10 — solver contract bumps `contractVersion: 1` → `2` for §9 input-shape change rather than LAHC strategy registration) → **D-0068** (M6 C3 narrowed to local-first; cloud-mode LAHC integration carved off to FW-0035) → **D-0069** (M6 C4 + M6 milestone closure — LAHC empirically validated; sweet-spot defaults captured but not module-pinned; active milestone → none with M7 study deferred per FW-0038). M5 closure: **D-0065** (analyzer's calibration-framework thesis validated). M5 activation: **D-0055**.
- M4 architectural surface: **D-0048..D-0054** (close M3 early; reframe M4; dual-track Python; Cloud Run + consolidated GCP project; Apps Script library reorg; random-seed default; public-service auth via X-Auth-Token allowlist). New contract `docs/cloud_compute_contract.md` (`contractVersion: 1`).
- Full **sheet → snapshot → Python compute → writeback** round-trip runs end-to-end on real ICU/HD data in **both modes**: local (browser-download Snapshot JSON → Python CLI → file upload via launcher form → writeback tab) and cloud (bound shim's `Roster Monster → Solve Roster` menu → POST to Cloud Run → in-memory writeback within one Apps Script invocation; M6 C4 cloud benchmark per D-0069 measures the sync wait at **~283s cold / ~270s warm** at the default `_DEFAULT_MAX_CANDIDATES=32` SRB cloud baseline — only ~17s cold-path / ~30s warm-path headroom under the 5-min Cloud Run timeout per `docs/cloud_compute_contract.md` §8.4; the older "~30-90s sync wait" framing was uncalibrated against the live cloud service and is superseded). Both modes share the same Python compute core per D-0050 and produce byte-identical wrapper envelopes at same `(snapshot, optionalConfig)` when seed is explicitly set per D-0053's parity precondition.
- **249/249 Python tests pass** via standalone runners (164 baseline + 52 analyzer unit + 9 analyzer integration added in M5 C1 + 8 LAHC tests added across M6 C2 Tasks 2A/2B/2C + 11 LAHC + envelope tests added across M6 C3 Tasks 2A/2B + 5 swapProbability / audit-surface tests added in M6 C4 PR-A; scorer / parser / pipeline / selector / solver / run-CLI runners all green at M6 closure). Apps Script tests run via `clasp run runAllTests_` on the launcher project (17 writeback-helper unit tests added under M3 C1).
- scorer contract is at `contractVersion: 3` per D-0039; `ScoringConfig` public shape unchanged from v2. Writeback contract `contractVersion: 1`, §9 6 categories. Selector v2.
- contracts settled across M2 + M3 + M4 + M5 + M6: snapshot, snapshot_adapter, parser_normalizer, template_artifact, sheet_generation, rule_engine, scorer (v3), solver (`contractVersion: 2`; M6 C1 added §12A LAHC strategy registration via §11.2 extension clause + promoted `strategyId` to required boundary input in §9 per D-0067 — bumped from `1` due to §9 input-shape change; LAHC strategy registration alone via §11.2 would not have required a bump; M6 C4 PR-A added §12A.7 + §12A.9 mentions of `swapProbability` (CLI surface + diagnostic field), and this PR-B closure docs sweep added the corresponding `swapProbability = 0.5` default to §12A.5's "Default values" list + an empirical post-M6-closure note pointing at FW-0036/FW-0037's tuned tuple (`L=10` / `idleThreshold=2000`); both are additive maintainer-tunable knobs with default-preserving behavior, no contractVersion bump), selector (v2; M6 C3 added §16.5 optional `solverStrategy` + `solverStrategyConfig` fields per the §16.3 additive-optional rule, no contractVersion bump; M6 C4 PR-A added optional `swapProbability` to the `lahcParams` payload with default-on-absence rule for M6-C3-era envelope back-compat, no contractVersion bump), writeback (`contractVersion: 1`, §9 6 categories), cloud_compute (`contractVersion: 1`), analysis (`contractVersion: 1`, drafted + implemented under M5 C1 per D-0056 / D-0057 / D-0058), analysis_renderer (`contractVersion: 1`, drafted + implemented + deployed under M5 C2 per D-0060 / D-0061 / D-0062 / D-0063).
- transport: **local mode** — inbound = browser-download per D-0040; outbound = file upload via launcher form per D-0044 (no new OAuth scopes). **Cloud mode** — inbound = in-memory snapshot via central library entrypoint; outbound = HTTP request body + structured-response envelope per `docs/cloud_compute_contract.md`. Cloud mode required four bound-shim manifest scopes per D-0051 sub-3a + D-0054: `script.external_request`, `openid`, `userinfo.email`, and `spreadsheets` (broadened from `currentonly`). Cloud Run runs `--allow-unauthenticated`; operator-identity gating is via Flask-side `ALLOWED_EMAILS` per D-0054.
- `docs/open_decisions.md` is empty.
- explicit deferrals retained from M1.1 + M4 + M5 + M6 + M7: public signup, in-app allowlist, operator-editable template, persisted per-operator state, alternative launcher platforms; **parallel operational search and orchestration (FW-0027) — DELIVERED via M7 C3+C4 per D-0072** (async UX supersedes the original M4-era framing); observability → FW-0028 (candidate next-work thread in the post-M7 study window); template-aware label propagation in writeback library → FW-0029; service-account-based Cloud Run auth → D-0054 §7.6; **cloud-side FULL retention support (FW-0030) — DELIVERED via M7 C3+C4 per D-0072** (in-memory K-candidate aggregation inside the single Cloud Batch task IS the FULL retention; analyzer runs inline before the callback POST — no `candidates_full.json` serialization needed); snapshot-extension analyzer fields (senior-junior pairing, leave history, rotation conflicts, public-holiday classification) → FW-0031; per-candidate rule-violation breakdown (Tier 6 — `softCount` / `softByRule` / `hardByRule`) → FW-0032; systematic weight elicitation / tuning → FW-0033 (M5 C4 verdict per D-0065 surfaced this thread but the maintainer's sequencing call was "search next, formulation rework later"; explicitly NOT rolled into M6 or M7 scope, stays parked through M7 closure — candidate next-work thread in the post-M7 study window); diversity-aware top-K selection at the analyzer stage → explicitly out of scope per D-0056 (diversity belongs in solver-strategy work, not analyzer); **cloud-mode LAHC integration (FW-0035) — DELIVERED via M7 C3+C4 per D-0072** (bound shim sends `solverStrategy: 'LAHC'` + `operatorEmail`; Cloud Run dispatches into async Cloud Batch path; SRB cloud path stays sync per D-0071 sub-decision 13, hidden from operator menu); LAHC operator-cycle K defaults → FW-0036 (M6 C4 dev-copy evidence + M7 C5 live ICU+HD cycles support K=10–20 sweet-spot but module-level pinning waits for 2+ additional post-M7 cycles — passive data accrual now happens as a side effect of every M7 production run); LAHC two-step tune-then-run pattern + L-tuning protocol → FW-0037 (the M7 production tuple `L=50` / `idleThreshold=3500` / `swapProbability=0.5` is locked inside the worker per D-0070; tooling + cache-by-signature waits for the next solver-strategy work thread); **parallel solver (FW-0038) — DELIVERED via M7 per D-0072** (M7 itself was the implementation target; design-rationale archive retained for future maintainers); silent-operator-outcome watchdog → FW-0039 (accepted-gap-for-v1 per D-0071 sub-decision 17 + Codex P1 round-2 fix; revisit triggers stay armed — operator pool growth beyond ~3-5, actual finalizer-crash OR callback-unreachable incident, FW-0028 observability promotion, Google Workspace migration); single-VM K upgrade K=88 → K=176 → FW-0040 (parameterized dial; K=88 vs K=176 is a quality wash per FW-0036 plateau, quota-bump investment waits for concrete demand).
