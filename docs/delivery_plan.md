# Delivery Plan

## 1. Purpose of this document
This is the **living execution guide** for Roster Monster v2.

It defines what is actively being delivered now (active milestone, active checkpoint, and supporting tasks) and what is intentionally next.

It does **not** replace:
- architecture truth in `docs/blueprint.md`
- milestone-level sequencing in `docs/roadmap.md`
- normative technical boundaries in contract docs
- issue-level implementation tracking

## 2. Planning vocabulary
- **Product**: the full end-to-end capability being built.
- **Milestone**: a major delivery state that advances product readiness.
- **Checkpoint**: a bounded, reviewable step inside a milestone.
- **Task**: a concrete work item used to close a checkpoint.

Working rule: this repo should normally maintain **one active milestone** and **one active checkpoint** at a time.

## 3. Product definition
Roster Monster v2 builds a reusable roster-allocation core with department-specific template control, while preserving Google Sheets as the operational front end. ICU/HD is the first concrete implementation target.

## 4. Product-level delivery principles
- Preserve Google Sheets as the operational front end.
- Keep architecture-first and contract-first boundaries where needed.
- Prioritize work that directly unblocks real operator workflow.
- Avoid reopening settled contracts unless there is a strong consistency reason.
- Avoid spending effort on items that do not support the active checkpoint.

## 5. Milestone map

### M1 — Operator-ready request sheet generation
- **Goal:** Deliver an operator-usable ICU/HD request sheet shell, backed by closed generation contract boundaries.
- **Why it matters:** Immediate operational need; unblocks the next request sheet cycle and anchors downstream work.
- **Status:** **Completed** *(closed 2026-04-21 on operator delivery; see §11 and D-0019)*
- **Dependencies:** template/request/generation contract surfaces.
- **Exit criteria:** generation inputs, structural surfaces, operator edit boundaries, non-goals, and acceptance criteria are closed, and operator-ready ICU/HD sheet-shell generation is implemented for the empty-form use (new spreadsheet file or new tab in existing spreadsheet) with intended editable/protected behavior and practical validation in place.
- **Likely checkpoints:**
  - Close sheet-generation MVP boundary. *(C1, closed 2026-04-17)*
  - Align template artifact surfaces with generation needs. *(C2, closed 2026-04-18)*
  - Generation acceptance/handoff readiness. *(C3, closed 2026-04-18)*
  - Implement operator-ready sheet generation. *(C4, closed 2026-04-21)*

### M1.1 — Operator-facing launcher *(addendum to M1)*
- **Goal:** Provide a narrow operator-facing launcher so named monthly-rotation pilot operators can invoke empty ICU/HD request-sheet generation without running Apps Script by hand.
- **Why it matters:** Closes M1's operator-facing story end-to-end; turns a maintainer-only generator into a pilot-usable launcher without expanding M1's compute scope.
- **Status:** **Completed** *(closed 2026-04-22 on hands-on validation; addendum milestone; M1 itself stays Completed, not reopened; see §11)*
- **Dependencies:** M1 complete; `docs/sheet_generation_contract.md` §12 (launcher surface).
- **Exit criteria:** a non-maintainer test operator can, after one-time Google consent, load the launcher URL, submit the form, and receive a working generated sheet or tab in either output mode, validated hands-on.
- **Likely checkpoints:**
  - Implement operator launcher web app.
- **Addendum framing:** M1.1 is derivative of M1's operator-facing surface, not a compute-line milestone. Addendum numbering convention (`M<parent>.<n>` with integer `n`, no nested decimals) is recorded in D-0021.

### M2 — Minimal local compute pipeline
- **Goal:** Stand up deterministic local parse/normalize/solve flow against closed contracts.
- **Why it matters:** Establishes executable core path before scale/orchestration.
- **Status:** **Completed** *(closed 2026-04-29 on M2 C9 closure; the full sheet → snapshot → Python pipeline runs end-to-end on real ICU/HD May 2026 data; see §11)*
- **Dependencies:** M1 complete; snapshot/parser/domain boundaries stable.
- **Exit criteria:** repeatable local run path with interpretable outputs for ICU/HD scenarios.
- **Likely checkpoints:** parser-normalizer implementation closure (C3); minimal rule/scorer/solver integration (C4); local run artifact basics (C5); parser overlay for scoring config (C6); Scorer Config sheet-generator half (C7); spacingPenalty geometric-decay shape (C8); snapshot-extraction Apps Script implementation (C9 — final M2 checkpoint, per `docs/decision_log.md` D-0036). All nine checkpoints closed.

### M3 — Safe result/output and writeback
- **Goal:** Define and implement safe output surfaces and writeback behavior.
- **Why it matters:** Operator trust depends on safe, clear result delivery. Without writeback, the operator has a JSON file on disk and no easy path to "Tuesday Dr. X is on MICU call"; M3 closes the loop by writing the winning roster back into a new tab on their source spreadsheet, with full traceability.
- **Status:** **Completed** *(closed 2026-04-30 on M3 C1 closure; M3 C2 dropped per `docs/decision_log.md` D-0048 — the dev-copy round-trip exercised under M3 C1 already met the exit criteria, and live-operator demo is no longer the right next investment per the maintainer call recorded in D-0049)*
- **Dependencies:** M2 complete (closed 2026-04-29 on M2 C9 closure per `docs/delivery_plan.md` §11); writeback contract closed scope-ahead in M2 C2 round (`docs/writeback_contract.md`, `docs/decision_log.md` D-0031); selector contract v2 source-sheet identity (`docs/selector_contract.md` v2 §9 item 3, D-0032).
- **Exit criteria:** operator can take a CLI-produced envelope, upload it via the launcher's writeback form, and see a new tab in the source spreadsheet carrying the winner allocation with full run-envelope traceability per `docs/writeback_contract.md` §16; failure-branch envelopes produce a "FAILED" tab per §13; runtime errors surface through the launcher diagnostic per §17.3 without silent partial-state. **All exit criteria met by M3 C1 closure on 2026-04-30** — verified live on the dev-copy round-trip (100 candidates, 116/116 filled, writeback tab written with traceability + protection).
- **Likely checkpoints:**
  - **C1 — Writeback library implementation + launcher route + Python CLI extension.** *(closed 2026-04-30; see §11)* Production writeback path landed end-to-end: Apps Script writeback library implementing the pure adapter per `docs/writeback_contract.md` §6.2 / §10 / §11 / §12 / §13 / §14 / §16; launcher Web App grew a `?action=writeback` route + form per D-0044 / D-0046; Python CLI extended to emit the wrapper envelope per D-0045 / D-0047. End-to-end demo executed on real dev-copy snapshot (100 candidates, 116/116 filled, score -2199.97; writeback tab written, traceability footer + DeveloperMetadata + read-only protection verified live).

### M4 — Cloud end-to-end pipeline + dual-track preservation
- **Goal:** Deliver a cloud-deployed end-to-end pipeline so operators can drive a one-click roster-generation flow without local Python tooling, while preserving the existing local CLI as a maintainer-side dev-velocity surface for solver-strategy experiments.
- **Why it matters:** Pre-pilot political/visibility goal — project stakeholder ("boss") needs a boss-clickable proof of concept that doesn't require explaining or installing local Python tools. The dual-track preservation matters because solver-strategy work (the next priority after M4 closes) needs the fast local-CLI iteration loop; collapsing to cloud-only would force every experiment through a deploy cycle.
- **Status:** **Completed** *(closed 2026-05-01 on M4 C1 closure as the only delivered checkpoint; solver-strategy optimization — flagged at M4 reframing per `docs/decision_log.md` D-0049 as the post-M4-C1 priority — is its own forthcoming milestone rather than an M4 C2; see §5's next milestone slot)*
- **Dependencies:** M3 complete (closed 2026-04-30 per §11); writeback library functioning end-to-end; Python CLI emitting the wrapper envelope per D-0045 / D-0047; consolidated `RosterMonsterV2` GCP project with all three Apps Script projects linked (operational prerequisite for M4 C1 Phase 2 per `docs/decision_log.md` D-0051).
- **Exit criteria:** an authorized operator can open a request-entry spreadsheet, click `Roster Monster → Solve Roster` from the in-sheet menu, wait ~30-60s synchronously, and see a new writeback tab appear in that spreadsheet carrying the winner allocation — no local Python install, no JSON download/upload, no separate launcher URL. Failure-branch envelopes produce a "FAILED" tab per `docs/writeback_contract.md` §13; cloud-side errors (input validation, compute exceptions, infrastructure failures) surface through a structured bound-shim dialog per `docs/cloud_compute_contract.md` §10. Local CLI continues to work unchanged. **All exit criteria met** by M4 C1 closure on 2026-05-01 — verified live on the maintainer's dev-copy spreadsheet (visual fidelity matches M1.1 empty-shell aesthetic per Path 2-Lite polish pass; new tab + traceability footer + read-only protection in place).
- **Likely checkpoints:**
  - **C1 — Cloud end-to-end pipeline (minimum demo).** *(closed 2026-05-01; see §11)* Production cloud-mode pipeline landed end-to-end: Cloud Run HTTP wrapper around shared Python compute core (D-0050) deployed as `roster-monster-compute` in the `RosterMonsterV2` GCP project (D-0051); Apps Script library reorganization moves writeback into the central library (D-0052); bound shim "Solve Roster" menu orchestrates extract → cloud compute → writeback inline. D-0054 amends the D-0051 auth model after live testing surfaced an `aud` mismatch — service runs `--allow-unauthenticated` with Flask-side `X-Auth-Token` validation against an `ALLOWED_EMAILS` env var. Path 2-Lite polish pass aligns the writeback tab's visual style with M1.1's empty-shell aesthetic (FW-0029 captures the template-aware label-propagation path for when a second template enters scope).
  - **No further M4 checkpoints.** Solver-strategy optimization — flagged in D-0049 as the maintainer's stated post-cloud priority — promotes to its own forthcoming milestone (M5 territory) rather than an M4 C2, because (a) its scope is materially different from "deliver the cloud demo" (it changes core compute semantics, while M4 was about delivery surface), and (b) closing M4 cleanly at C1 honors the original "minimum demo" framing that justified the D-0049 reframing.

## 6. Current active milestone
- **Active milestone:** *(none)*

This is none now because:
- M4 (`Cloud end-to-end pipeline + dual-track preservation`) closed at the milestone level on 2026-05-01 with M4 C1 closure as the only delivered checkpoint. The `Roster Monster → Solve Roster` one-click operator UX is live in production (Cloud Run service + bound shim menu); local CLI is preserved unchanged for solver-strategy experiments per D-0050's dual-track discipline. See §11 and the M4 closure note there.
- The maintainer's stated post-M4 priority is solver-strategy optimization — different in character from M4's "delivery surface" framing (changes core compute semantics rather than adding a delivery vehicle), so it lands as its own milestone slot rather than an M4 C2. Activation timing is left to the maintainer's call; no auto-activation per the same discipline that gated M3 → M4 (`activate at milestone level + first checkpoint scoped immediately` is a same-day pattern, not an automatic-roll-forward pattern).

## 7. Checkpoint plan for the active milestone

*(no active milestone; checkpoint plan is empty until the next milestone activates per §5 / §6)*

The most recently closed milestone M4 (`Cloud end-to-end pipeline + dual-track preservation`) had a single checkpoint:
- **C1 — Cloud end-to-end pipeline (minimum demo).** *(closed 2026-05-01; see §11)* Sole M4 checkpoint; M4 closed at C1 closure.

The previous active milestone M3 (`Safe result/output and writeback`) closed at C1 on 2026-04-30 (C2 dropped per D-0048); the closure trail is preserved in §11.

## 8. Current active checkpoint
- **Active checkpoint:** *(none)*

M4 C1 (`Cloud end-to-end pipeline (minimum demo)`) closed 2026-05-01 per §11. M4 also closed at that point (C1 was the only checkpoint). Active milestone moves to *none*; activation of the next milestone (solver-strategy optimization most likely, per the D-0049 forward-pointer) is deferred to the maintainer's call.

## 9. Task list for the current checkpoint

*(no active checkpoint; task list is cleared until the next milestone + checkpoint activate per §5 / §6 / §7 / §8)*

## 10. Explicitly deferred for now
- Solver implementation details.
- Scorer implementation details.
- Local run implementation mechanics.
- Writeback implementation mechanics. The writeback contract is now drafted scope-ahead per D-0031 (`docs/writeback_contract.md`); implementation mechanics remain deferred until M3 activates.
- Worker/orchestrator mechanics.
- Benchmark hardening depth beyond milestone-level framing.
- Broad multi-department generalization beyond ICU/HD-first sequencing.
- Public or open-signup operator access for the launcher; first-release scope is named monthly-rotation pilot operators only, gated via GCP OAuth consent-screen Test Users.
- In-app operator allowlist / role model inside the launcher; access gating stays external to the app for pilot scope.
- Operator-editable template or structural mapping; template stays maintainer-owned.
- Persisted per-operator state beyond Google's OAuth session.
- Alternative launcher platforms (for example a static page over the Apps Script API Executable); the pilot sticks with Apps Script Web App per D-0022.

## 11. Recently completed checkpoints
### M4 milestone closure note
M4 (`Cloud end-to-end pipeline + dual-track preservation`) closes on 2026-05-01 with M4 C1 closure as its only delivered checkpoint. M4 was reframed from the original `Parallel operational search and orchestration` framing per `docs/decision_log.md` D-0049 to prioritize a boss-clickable proof of concept (political/visibility goal) and preserve the local CLI for forthcoming solver-strategy work. The reframing parked the original M4 scope (parallel orchestration) and the original M5 scope (observability) as future-work entries FW-0027 + FW-0028. M4 closes at C1 because C1's exit criteria — "an authorized operator can drive a one-click roster-generation flow inside their spreadsheet" — was the only outcome the reframed milestone scoped; solver-strategy optimization is materially different in character (changes core compute semantics rather than adding a delivery vehicle) and lands as its own forthcoming milestone slot rather than an M4 C2. Active milestone moves to *none*; activation of the next milestone is deferred to the maintainer's call. M3 stays Completed; M2 stays Completed.

- **M4 C1 — Cloud end-to-end pipeline (minimum demo)** *(closed 2026-05-01)*
  - Sole M4 checkpoint per §5 — closes the "boss-clickable cloud demo" production path. After this checkpoint closes, the full **operator-facing one-click cloud-mode pipeline** runs end-to-end: operator opens spreadsheet → `Roster Monster → Solve Roster` menu → ~30-90s synchronous wait → new writeback tab appears with the winner allocation, M1.1-style visual fidelity, traceability footer, and read-only protection. No local Python install, no JSON download/upload, no separate launcher URL. Local CLI continues to work unchanged for maintainer-side dev velocity per D-0050's dual-track discipline.
  - **Phased docs-first delivery** per the M2 C8 / M2 C9 / M3 C1 cadence: Phase 1 docs (PR #103) → Phase 1 amendment for D-0053 random-seed default (PR #104) → Phase 2 code (PR #105) → Phase 3 closure (this PR).
  - **Phase 1 architectural decisions** (PR #103): D-0048 (close M3 early — drop M3 C2; round-trip already proven), D-0049 (reframe M4 from `Parallel operational search and orchestration` to `Cloud end-to-end pipeline + dual-track preservation`; old M4 / M5 → FW-0027 / FW-0028), D-0050 (dual-track Python architecture — shared compute core + thin CLI wrapper + thin HTTP wrapper), D-0051 (Cloud Run platform + `ScriptApp.getIdentityToken()` → IAM auth + consolidated `RosterMonsterV2` GCP project), D-0052 (Apps Script library reorganization — writeback moves into central library so bound shim can invoke it inline; bound shim menu adds "Solve Roster"). New contract `docs/cloud_compute_contract.md` (`contractVersion: 1`) pins the HTTP boundary shape, auth model, structured-error envelope, determinism / idempotency stance, and Cloud Run deployment posture. Cross-contract additive updates to `docs/snapshot_adapter_contract.md` §7 + §11 (in-memory entrypoint + cloud-mode dual-track addenda) and `docs/writeback_contract.md` §6.3 (bound-shim direct-invocation path). Codex review on PR #103 surfaced the auth-related scope additions captured as D-0051 sub-decision 3a (`script.external_request` + `openid` + `userinfo.email`).
  - **Phase 1 amendment** (PR #104): D-0053 changed the solver seed default from the prior `_DEFAULT_SEED = 20260504` constant to a per-invocation `random.randint(0, 2**31-1)`. Rationale: the "Solve Roster" menu's "click again to try a different roster" use case requires fresh seeds per click; tests that need byte-identical re-runs pass `--seed N` explicitly (already true across the suite). The chosen seed is recorded in `runEnvelope.seed` so any run can be replayed. Refines D-0050's parity claim with an explicit precondition: byte-identical at same `(snapshot, optionalConfig)` continues to hold WHEN `optionalConfig.seed` is explicitly set.
  - **Phase 2 implementation** (PR #105, eight commits across workstream a + workstream c + auth-fix + scope-broadening + visual polish):
    - **Workstream a** — Python HTTP wrapper around shared compute core (commit `86a5e3c`). New `python/rostermonster/pipeline.py` hosts `run_pipeline()` that both the CLI (refactored to ~250 lines, was ~600) and the new Flask wrapper (`python/rostermonster_service/app.py`) call into. Same code path, byte-identical at same input. New `Dockerfile` at repo root + `cloud_compute_service/` bundle (Dockerfile, requirements.txt, README) for Cloud Run. 19 new tests: `test_pipeline.py` (7) + `test_service.py` (14 after Codex iterations).
    - **Workstream c** — Apps Script reorganization + bound shim "Solve Roster" menu (commit `8cf072a`). `Writeback.gs` MOVED from launcher (`m1_sheet_generator/`) to central library (`m2_extractor_library/`) so both the launcher's existing Web App writeback route AND the bound shim's new menu can invoke `RMLib.applyWriteback(envelope)`. Launcher gains an `RMLib` library import + thin delegate wrapper for `google.script.run` reach. Central library gains `extractSnapshotInMemoryForActiveSheet()` (in-memory snapshot — no file boundary in cloud-mode flow) and `Config.gs` exposing `getCloudRunUrl()` from Script Properties. Bound shim Menu.gs orchestrates extract → cloud fetch → writeback inline within one Apps Script invocation.
    - **D-0054 auth fix** (commit `1f64371`). Live testing surfaced the original D-0051 IAM-based auth model fails: `ScriptApp.getIdentityToken()`'s `aud` claim is the Apps Script project's GCP OAuth client ID, not the Cloud Run service URL. Cloud Run's IAM check rejected every request with HTTP 401. Plus, switching to `--allow-unauthenticated` to fix that breaks the standard `Authorization` header (Cloud Run drops it on public services). D-0054 amends D-0051 sub-decision 2: Cloud Run runs `--allow-unauthenticated`; bound shim sends ID token via `X-Auth-Token` (custom header, not stripped); Flask validates via `google.oauth2.id_token.verify_oauth2_token(audience=None)` and gates by `ALLOWED_EMAILS` env var.
    - **Bound-shim spreadsheets scope broadening** (commit `e19aedd`). Live writeback test caught: `spreadsheets.currentonly` is insufficient for `SpreadsheetApp.openById()` even on the bound spreadsheet's own ID. Apps Script's scope check fires before the spreadsheet-existence check. Bound shim manifest `spreadsheets.currentonly` → `spreadsheets` (full). The cloud_compute_contract §7.3 had flagged this exact possibility as a contingency in PR #103; the contingency triggered. M2 C9's narrow-scope optimization is officially retired; the bound shim now matches the launcher's `spreadsheets` scope.
    - **Path 2-Lite visual polish** (commit `e4373c0`). Writeback tab visual style now matches `apps_script/m1_sheet_generator/src/Layout.gs`'s empty-shell aesthetic: navy title bg + white text, gray section bg, yellow point bg, green lower-shell bg, salmon weekend column shading. Friendly section/slot/point-row labels via hardcoded ICU/HD maps. Two-row date+weekday header. Frozen panes + column widths. FW-0029 captures the template-aware label propagation path for when a second template enters scope; pilot has only `cgh_icu_hd` in flight today.
  - **Operational changes executed out-of-band during Phase 2** (one-time per environment, NOT scripted by this PR — same pattern as M2 C9's one-time Drive setup):
    a. GCP project consolidation: bound shim (`1S4iBoojs9CuCG0...`) and central library (`1q6H6fVLjiJXneiD...`) Apps Script projects linked to `RosterMonsterV2` GCP project (project number `693837275969`). Maintainer-side UI work; ~5 min.
    b. Cloud Run service `roster-monster-compute` deployed to `RosterMonsterV2` GCP project, region `asia-southeast1`, scale-to-zero, max-5, concurrency-1, 5 min timeout, `--allow-unauthenticated`, env var `ALLOWED_EMAILS=arthurjyong@gmail.com`. Service URL: `https://roster-monster-compute-693837275969.asia-southeast1.run.app`. Image built via `gcloud builds submit --tag gcr.io/rostermonsterv2/roster-monster-compute:latest` then `gcloud run deploy --image ...` (the two-command pattern bypasses Cloud Build's source-deploy autodetection which prefers buildpacks when `pyproject.toml` is present anywhere in the source tree).
    c. Default Compute Engine SA granted `cloudbuild.builds.builder` + `storage.objectViewer` + `logging.logWriter` IAM roles (required for Cloud Build to function on the project; not auto-granted on first project setup).
    d. Launcher Apps Script project re-deployed at `@14` with the RMLib library dependency added.
    e. Central library Script Property `CLOUD_RUN_URL` set via the Apps Script editor UI.
  - **Codex review** on PR #105 surfaced 7 findings across 4 rounds, all addressed in the same PR:
    - Round 1 (3 findings): P1 deploy command in `cloud_compute_service/README.md` was stale (`--no-allow-unauthenticated` from pre-D-0054); P1 README's Configuration section claimed no env-var knobs beyond PORT (missed `ALLOWED_EMAILS` requirement); P2 `service` extra in `pyproject.toml` omitted `google-auth` + `requests`.
    - Round 2 (1 finding): P2 `_coerce_optional_int` accepted `maxCandidates: 0` / negative values, silently flowing into the solver and surfacing as COMPUTE_ERROR (misclassifying caller defect as server-side failure).
    - Round 3 (2 findings): P1 `verify_oauth2_token()` can raise `GoogleAuthError` subclasses (transient transport errors); old block only caught `ValueError`. P1 `DISABLE_AUTH_FOR_LOCAL_TESTING` env var was treated as truthy on any non-empty value (so `=false` would silently disable auth).
    - Round 4 (1 new finding): P1 `optionalConfig` validation used `or {}` short-circuit, silently coercing falsy non-dict values (false/0/""/[]) to `{}` and running with defaults instead of rejecting as INVALID_OPTIONAL_CONFIG.
  - **Tests:** 14 service tests (Round 4) + 7 pipeline tests (unchanged from PR #105 initial) + 143 inherited from earlier modules = **164/164 Python tests pass**. Apps Script unit tests run via `clasp run runAllTests_` on the launcher project (17 writeback-helper unit tests inherited from M3 C1; no new bound shim tests because most of `_solveRoster_` is orchestration with side effects on UrlFetchApp + RMLib + UI — same coverage discipline as M3 C1's writeback library).
  - **Live operator-facing demo** executed 2026-05-01 on the maintainer's dev-copy spreadsheet: `Roster Monster → Solve Roster` menu invocation → consent flow (operator approves new scopes including `spreadsheets`, `script.external_request`, `openid`, `userinfo.email`) → ~60s wait → new writeback tab (`v0430234839_RM<YYMMDDHHMMSS>`) appears with M1.1-style visual fidelity (navy title, gray section bg, yellow point bg, green lower-shell, salmon weekend columns, friendly slot labels, frozen panes + column widths). Confirmed working end-to-end.
  - **What this PR does NOT cover** (deferred to forthcoming milestone(s)):
    a. Solver-strategy optimization (the maintainer's stated post-cloud priority — different in character from delivery surface; lands as its own milestone slot, see this M4 closure note's first paragraph).
    b. Pilot rollout to non-maintainer operators (boss demo conducted on maintainer's dev copy is the closure target; broader pilot is a future milestone or operational rollout step).
    c. Template-aware label propagation in writeback library (FW-0029 — promote when a second template enters scope).
    d. Async / queue-based compute mode (FW-0027 — promote when concrete drivers surface).
    e. Observability + benchmark hardening (FW-0028 — promote when concrete drivers surface).
    f. Service-account-based Cloud Run auth (D-0054 §7.6 — promote when pilot scope grows or formal least-privilege posture is required).
  - Locked architectural decisions: D-0048..D-0054 (close M3 + reframe M4 + dual-track Python + Cloud Run + library reorganization + random seed default + auth model amendment).
  - Delivered as four PRs (Phase 1 docs PR #103 + Phase 1 amendment PR #104 + Phase 2 code PR #105 + Phase 3 closure PR — this PR).
  - Main affected surfaces: `docs/decision_log.md` (D-0048..D-0054 appended); `docs/cloud_compute_contract.md` (new in PR #103, §7 / §10 amended in PR #105 per D-0054); `docs/snapshot_adapter_contract.md` §7 / §11 (cloud-mode addenda); `docs/writeback_contract.md` §6.3 (bound-shim direct-invocation path); `docs/future_work.md` (FW-0027 / FW-0028 / FW-0029 appended); `python/rostermonster/pipeline.py` + `python/rostermonster/run.py` (refactored); `python/rostermonster_service/app.py` (new); `cloud_compute_service/Dockerfile` + `requirements.txt` + `README.md` (new bundle); `apps_script/m2_extractor_library/src/` (Writeback.gs moved in + Config.gs new + Extractor.gs in-memory entrypoint added); `apps_script/m2_template_bound_script/src/` (Menu.gs Solve Roster handler + appsscript.json scope additions); `apps_script/m1_sheet_generator/src/` (RMLib library dep added + Writeback.gs replaced with thin delegate); `docs/delivery_plan.md` (this section + §5 + §6 + §7 + §8 + §9 + §12 + §15 transition).

### M4 C1 sign-off note
M4 C1 is complete. The cloud-end-to-end pipeline is in place: a Cloud Run service (`roster-monster-compute` in `RosterMonsterV2` GCP project, asia-southeast1, scale-to-zero, ALLOWED_EMAILS-gated) hosts a Flask wrapper around the shared Python compute core; the bound shim's `Roster Monster → Solve Roster` menu orchestrates extract → cloud fetch → writeback inline; the writeback tab renders with M1.1-style visual fidelity. Local CLI is preserved unchanged for maintainer-side dev velocity per D-0050. Live operator-facing demo confirmed end-to-end on the maintainer's dev-copy spreadsheet on 2026-05-01. **M4 closes at this checkpoint** per the closure note above. Active milestone moves to *none*; activation of the next milestone (most likely solver-strategy optimization per D-0049's forward-pointer) is deferred to the maintainer's call. M3 stays Completed; M2 stays Completed.

### M3 milestone closure note
M3 (`Safe result/output and writeback`) closes on 2026-04-30 with M3 C1 closure as its only delivered checkpoint. M3 C2 (`End-to-end demo on a real ICU/HD operator cycle`) was dropped per `docs/decision_log.md` D-0048 — M3's exit criteria (writeback round-trip with traceability + 3-state diagnostic + no silent partial-state) was already met by C1 closure; C2 was a live-operator confirmation step, not a missing capability. Across one checkpoint, M3 delivered: writeback contract scope-ahead resolution (D-0044..D-0047), Python CLI emitting the wrapper envelope per D-0045, Apps Script writeback library implementing the pure adapter per `docs/writeback_contract.md` §6.2, launcher Web App `?action=writeback` route + form, and a live operator-facing round-trip (sheet → snapshot → CLI compute → upload → writeback tab). Architectural decisions span D-0044..D-0047 (writeback transport + envelope shape + placement + CLI-output-is-the-envelope) plus the M3 C1 Phase 2 contract amendment that grew `docs/writeback_contract.md` §9 from 5 to 6 snapshot categories (closing D-0045 Follow-up #2). Active milestone moves to M4 (`Cloud end-to-end pipeline + dual-track preservation`) per D-0049, activated same-day at milestone level + first checkpoint M4 C1. M3 stays `Completed`.

- **M3 C1 — Writeback library implementation + launcher route + Python CLI extension** *(closed 2026-04-30)*
  - First M3 checkpoint per §5 — closes the production writeback path the writeback contract scope-ahead in M2 C2 (PR #66 round, D-0031, `docs/writeback_contract.md` `contractVersion: 1`) had been waiting for. The full sheet → snapshot → Python compute → writeback round-trip now runs end-to-end on real operator data: launcher generates an operator sheet → operator fills it → extractor emits a Snapshot JSON → Python CLI ingests + computes + emits a wrapper envelope JSON → operator uploads to the launcher's writeback form → new tab appears in the source spreadsheet carrying the winner allocation with full run-envelope traceability.
  - **Phased docs-first delivery** per the M2 C8 / M2 C9 cadence: Phase 1 docs (PR #100) → Phase 2 code (PR #101) → Phase 3 closure (this PR).
  - **Phase 1 architectural decisions** (PR #100): D-0044 (writeback transport = file upload via the launcher web form, refining D-0040's "drag/paste" wording for the writeback direction; no new OAuth scopes); D-0045 (writeback envelope shape = single JSON wrapper carrying `FinalResultEnvelope` + snapshot subset + `doctorIdMap`, so the operator uploads ONE file rather than three); D-0046 (writeback placement = launcher Web App route, NOT in-sheet bound shim menu — keeps the M2 C9 bound-template architecture clean and reuses the launcher's existing form infrastructure); D-0047 (Python CLI's standard output IS the writeback envelope, controlled by a new `--writeback-ready` flag defaulting to `true`). `docs/writeback_contract.md` §6.3 received the resolved-transport addendum citing D-0044 (parallel to the snapshot extractor's D-0040 resolution); `docs/snapshot_adapter_contract.md` §11 grew a forward-pointer to D-0047. Codex review on PR #100 caught a single P1 (the Phase 1 draft conflated `google.script.run` and `doPost(e)` — they are mutually exclusive in Apps Script; D-0044 sub-decision 3 fixed across 6 places).
  - **Phase 2 implementation** (PR #101, three commits a8e1905 + bc29e36 + 1870a3a): Python CLI extension (`python/rostermonster/run.py` `_assemble_writeback_wrapper` + `_build_writeback_snapshot_subset` + `_build_doctor_id_map` projecting the in-memory `Snapshot` to the wrapper categories per D-0045; `--writeback-ready` flag defaults `true`); Apps Script writeback library (`apps_script/m1_sheet_generator/src/Writeback.gs`, ~440 lines) implementing the pure adapter — public entry `applyWriteback(envelopeJsonString)`, success-branch tab + failure-branch tab + tab-name discipline (length truncation per §11.1.1, collision suffix per §11.1.2), atomicity per §14 (cleanup-on-failure with orphan-tab name surfaced if cleanup fails per §14.2), traceability footer per §16.1 + DeveloperMetadata per §16.2, read-only protection per §10.3; launcher writeback route (`Launcher.gs` `doGet(e)` dispatches `?action=writeback` → new `WritebackForm.html` with file input + `FileReader` + `google.script.run.applyWriteback` + 3-state diagnostic surface per §17). Codex review surfaced 5 findings across 3 rounds that all landed as patches on the same PR before merge: round 1 P1 (prefilled cells were rendered in a separate annotation block instead of at their source-tab cell positions per §10.1) + P2 (success URL didn't anchor to the new tab via `#gid=`); round 2 P1 (the round-1 fix used `surfaceId` as a slot row key but snapshot `surfaceId` values are output-surface IDs like `lowerRosterAssignments`, not slot rows — proper fix needs the template's slotType ↔ rowOffset binding) + P2 (`removeEditors` can throw when access is inherited via Google Group / domain settings; preserve effective user before removing others); round 3 P2 (tab creation `_buildWritebackTabName_` + `ss.insertSheet` ran outside the try/catch so exceptions bypassed `_writebackError_` and broke the §17.3 RUNTIME_ERROR diagnostic invariant).
  - **Contract amendment surfaced during Phase 2 Codex round 2**: the round-2 P1 fix required adding a 6th category `outputAssignmentRows` to `docs/writeback_contract.md` §9, carrying `[{surfaceId, slotType, rowOffset}, ...]` from the template's `outputMapping.surfaces[].assignmentRows[]`. Without this binding, the writeback library cannot place prefilled cells at their source-tab cell positions because `AssignmentUnit` carries `slotType` but no `rowOffset` (per `python/rostermonster/domain.py`), and snapshot `surfaceId` values are surface-level identifiers rather than per-slot anchors. D-0045's Follow-up #2 had foreseen this kind of pinning during M3 C1 Phase 2 implementation, so the addition closes that follow-up rather than introducing a new decision. The wrapper envelope's `schemaVersion` was deliberately kept at `1` because the Apps Script library carries a graceful fallback (`_resolveOutputAssignmentRows_` falls back to sorted slot keys when the field is absent) — additive in a v1-targeted-reader-tolerant way.
  - **Tests:** Apps Script — 14 new `Tests.gs` unit tests cover the pure helpers (`_validateWritebackEnvelopeShape_`, `_isAllocationResult_`, `_buildDoctorIdToDisplayName_`, `_groupColumnAByDoctorSection_`, `_orderedSectionKeys_`, `_groupCallPointsByRowKey_`, `_resolveOutputAssignmentRows_`, `_composeSpreadsheetUrlWithGid_`); tab-mutation paths covered by integration smoke against a live spreadsheet rather than unit tests. Python — 7/7 `test_run_cli.py` tests pass (one new test asserts the wrapper-shape contract per D-0045 + writeback §9 6 categories); full Python suite 143/143 pass.
  - **Live operator-facing demo** executed 2026-04-30: the M2 C9 dev-copy snapshot (real Drive ID `1XXfA3AyNtZ1dqp74aX1Qx6KDVnzms2T19LUUmduLnyg`, source tab `v0429214357`) ran through the CLI at 100 candidates (score -2199.97, 116/116 filled); the wrapper envelope was uploaded via the redeployed launcher (`@13`) at `?action=writeback`; a new tab `v0429214357_RM<YYMMDDHHMMSS>` appeared in the source spreadsheet with the winner allocation, traceability footer, hidden DeveloperMetadata, and read-only protection. The launcher diagnostic surfaced the SUCCESS state with the `#gid=`-anchored link to the new tab. Round-trip confirmed working end-to-end.
  - **What this PR does NOT cover** (deferred to M3 C2 per §7):
    a. End-to-end demo with a live operator (not the maintainer's own dev copy).
    b. Operator-facing UX feedback (error-message clarity, form polish).
    c. Any follow-on architectural items the live demo surfaces.
  - Locked architectural decisions: D-0044..D-0047 (transport, envelope shape, placement, CLI-output-is-the-envelope). FW entries unchanged; no new FW surfaced.
  - Delivered as three sequential PRs (Phase 1 docs PR #100 + Phase 2 code PR #101 + Phase 3 closure PR — this PR).
  - Main affected surfaces: `docs/decision_log.md` (D-0044..D-0047 appended); `docs/writeback_contract.md` (§6.3 resolved-transport addendum citing D-0044; §9 grew the 6th `outputAssignmentRows` category during Phase 2 Codex round 2 fix); `docs/snapshot_adapter_contract.md` §11 (forward-pointer to D-0047); `python/rostermonster/run.py` (CLI gains wrapper-envelope emission + `--writeback-ready` flag); `python/tests/test_run_cli.py` (wrapper-shape test added); `apps_script/m1_sheet_generator/src/` (new `Writeback.gs` + `WritebackForm.html` + `Launcher.gs` `doGet(e)` dispatch + `Tests.gs` writeback unit tests); `docs/delivery_plan.md` (this section + §5 + §6 + §7 + §8 + §9 + §12 + §15 transition).

### M3 C1 sign-off note
M3 C1 is complete. The production writeback path is in place: the Python CLI emits a single-file wrapper envelope (`FinalResultEnvelope` + snapshot subset including the new 6th `outputAssignmentRows` category + `doctorIdMap`); the launcher Web App's `?action=writeback` route accepts the upload via a `<input type="file">` form with client-side `FileReader` + `google.script.run.applyWriteback`; the Apps Script writeback library implements the pure adapter per `docs/writeback_contract.md` §6.2, writing a new tab in the source spreadsheet with the winner allocation, prefilled cells (when present) at their source-tab `(surfaceId, rowOffset)` coordinates, traceability footer, hidden DeveloperMetadata, and whole-tab read-only protection. The 3-state launcher diagnostic per §17 surfaces SUCCESS / FAILED / RUNTIME_ERROR with the `#gid=`-anchored spreadsheet URL on the success branch. Live round-trip confirmed end-to-end on the dev-copy snapshot. **Active milestone (M3) unchanged**; active checkpoint moves to *none* per §8 pending separate scoping of M3 C2 (`End-to-end demo on a real ICU/HD operator cycle + M3 closure`). M2 stays Completed.

- **M2 C9 — Snapshot extraction (Apps Script) — D-0036 implementation** *(closed 2026-04-29)*
  - Final M2 checkpoint per §5 — closes the inbound Apps Script ↔ Python seam committed-to in `docs/decision_log.md` D-0036. With C9 closure, **M2 (`Minimal local compute pipeline`) closes** per §6 / the new milestone-status update in §5. The full pipeline now runs end-to-end on real ICU/HD May 2026 data: launcher generates an operator sheet → operator fills it → in-sheet menu extracts a Snapshot JSON → operator-saved download → Python CLI ingests → parser/normalizer + rule engine + scorer + solver + selector → `FinalResultEnvelope` JSON output.
  - **Phased docs-first delivery** per the M2 C8 cadence: Phase 1 docs (PR #95) → Phase 2 code (PR #96) → Phase 3 closure (this PR).
  - **Phase 1 architectural decisions** (PR #95): D-0040 (transport = operator-saved JSON file via browser download — symmetric inbound + outbound mechanic resolving D-0036's joint-scoping with writeback §6.3); D-0041 (extractor placement = bound template spreadsheet + central Apps Script library at HEAD — operator sheets created via `DriveApp.makeCopy(template)` so simple `onOpen`/`onEdit` triggers fire natively, resolving the FW-0024 architectural blocker as a side effect; library indirection insulates already-distributed sheets from frozen-bound-script staleness; broader `drive` OAuth scope on the launcher; anyone-with-link Drive sharing accepted as pilot-scope simplification with FW-0025 capturing the per-email tightening as deferred work); D-0042 (snapshot identity = `snapshot_<spreadsheetId>_<extractionTimestamp>`); D-0043 (DeveloperMetadata-anchored extraction with sheet-scoped finder + runId-paired tab discovery + per-anchor cardinality / uniqueness / value-coverage validation, no visible-text fallback). New contract `docs/snapshot_adapter_contract.md` drafted (13 sections); writeback §6.3 + sheet_generation §11B + snapshot/parser forward-pointers + FW-0024 architectural-unblock note all updated. Codex review surfaced 10 rounds of findings — five real architectural P1s addressed (HEAD-vs-Editor library access, link-wide-vs-per-email sharing, OAuth scope expansion, template Drive ACL, workbook-vs-sheet-scoped finder), two internal-consistency P2s addressed (§4/§6 finder consistency, §1/§3 auth-surface wording), two delivery-plan/D-0040 wording P2s, one round-8 P1 silent-data-loss path closed (per-anchor completeness validation), and three sharing-related P1s deliberately deferred to FW-0025 with the trade-off explicitly documented.
  - **Phase 2 implementation** (PR #96): three Apps Script projects (launcher updated with `makeCopy` + broader scope + new metadata anchors; new `apps_script/m2_template_bound_script/` carrying the `onOpen` menu shim; new `apps_script/m2_extractor_library/` carrying the extractor logic per `docs/snapshot_adapter_contract.md` §6). Python CLI (`python -m rostermonster.run --snapshot path/to/file.json`) reading the JSON file from disk and running the full M2 pipeline. ICU/HD template promoted from test fixtures to production module (`python/rostermonster/templates/icu_hd.py`) so the CLI doesn't import test code. Codex round 1 caught two real bugs: P0 templateVersion path (`template.templateVersion` was wrong; should be `template.identity.templateVersion`) which would have silently propagated `"unknown"` to every generated sheet and broken the CLI's downstream `int(templateVersion)` deserialization; P1 assignment-row coverage (the partial-loss check that protected doctor rows under PR #95's round-8 fix wasn't applied symmetrically to assignment rows) — both addressed in commit 1c421ec, Codex round 2 clean.
  - **JSON shape correction surfaced during Phase 2.** Phase 1 docs described the `sourceLocator.path = {...}` typed-shape framing per `docs/snapshot_contract.md` §10, but the live `Snapshot` dataclass + parser actually consume a flattened locator shape (fields inline on `sourceLocator`, no `path` wrapper). The Apps Script `SnapshotBuilder.gs` was producing the docs-described shape; flattened all locators to match the live shape so the parser accepts the extracted JSON without translation. Field-name corrections at the same time: `DoctorRecord` uses `sourceDoctorKey/displayName/rawSectionText` (not `doctorId/rawDisplayNameText`); metadata block adds `templateId` + `templateVersion` to match the dataclass.
  - **Tests:** 141/141 Python tests pass (was 136 pre-Phase-2; +5 new CLI tests covering end-to-end run on the May 2026 fixture, byte-identical determinism, missing-snapshot exit-code, `_snapshot_from_dict` round-trip, and `_to_jsonable` recursive serialization). End-to-end CLI run on the real fixture: 116/116 assignments filled, score -21.534. Apps Script tests for the two new projects deferred until clasp-clone setup is complete on the operator's environment (one-time manual setup required).
  - **What this PR does NOT cover** (operator-side one-time setup, deliberately not scripted per `docs/snapshot_adapter_contract.md` §3):
    a. Create the `[INTERNAL] Roster Monster Template` spreadsheet in Drive.
    b. Inside that spreadsheet, Extensions → Apps Script (creates the bound script project).
    c. Create the central library Apps Script project (Drive → New → More → Google Apps Script).
    d. Set Drive sharing on both (anyone-with-link Viewer for template, Editor for library).
    e. In the bound shim's manifest, add the central library as a dependency at version 0 (HEAD / development mode).
    f. `clasp clone` both new projects into the repo, replace auto-cloned `appsscript.json` with repo versions, `clasp push`.
    g. Set `TEMPLATE_FILE_ID` in the launcher's Script Properties.
    h. `clasp push` + `clasp deploy` the launcher to publish the broader OAuth scope.
    These run once per environment; per-project READMEs walk through each step.
  - Locked architectural decisions: D-0040..D-0043 (transport, placement, identity, layout-drift handling). FW-0025 (per-email Drive sharing tightening) deferred with four explicit revisit triggers.
  - Delivered as three sequential PRs (Phase 1 docs PR #95 + Phase 2 code PR #96 + Phase 3 closure PR — this PR).
  - Main affected surfaces: `docs/decision_log.md` (D-0040..D-0043 appended); `docs/snapshot_adapter_contract.md` (new); `docs/writeback_contract.md` §6.3 (resolved-transport addendum); `docs/sheet_generation_contract.md` §11B (new launcher-attached metadata anchor declaration); `docs/snapshot_contract.md` §3 + `docs/parser_normalizer_contract.md` §5 (forward-pointers); `docs/future_work.md` FW-0024 + new FW-0025; `apps_script/m1_sheet_generator/` (launcher updated for `makeCopy` + broader scope + metadata anchors); `apps_script/m2_template_bound_script/` (new); `apps_script/m2_extractor_library/` (new); `python/rostermonster/run.py` (new); `python/rostermonster/templates/icu_hd.py` (new — promoted from test fixtures); `python/tests/test_run_cli.py` (new); `docs/delivery_plan.md` (this section + §5 + §6 + §7 + §8 + §9 + §12 + §15 transition).

### M2 C9 sign-off note
M2 C9 is complete. The production Apps Script extractor is in place: launcher generates operator sheets via `makeCopy(template)` carrying a bound shim; the bound shim's `onOpen` installs a "Roster Monster → Extract Snapshot" menu; the menu delegates to a central library at HEAD; the library reads the sheet via DeveloperMetadata anchors with sheet-scoped finder + runId-paired tab discovery + per-anchor cardinality validation; the library returns a downloadable JSON blob; the Python CLI ingests the JSON and runs the full M2 pipeline. End-to-end: a single operator can now generate a sheet, fill it in, click Extract Snapshot, save the JSON locally, and run the compute pipeline — without any maintainer involvement after the one-time Drive setup. **M2 (`Minimal local compute pipeline`) closes at this checkpoint** per §6's "active milestone = none" transition. Active milestone moves to *none*; M3 (`Safe result/output and writeback`) is the next planned milestone but its activation date is left to the maintainer's call. M3 stays `Planned` until then.

### M2 milestone closure note
M2 is closed. The compute pipeline is end-to-end on real data with a realistic operator workflow. Across nine checkpoints (C1..C9), the milestone delivered: rule engine + scorer + solver + selector contracts (C1 + C2); their Python implementations (C3 + C4 + C5); the operator-tuneable scoring config surface end-to-end across snapshot + parser + scorer + sheet-generator (C6 + C7); the v3 spacingPenalty geometric-decay curve (C8); and the production Apps Script snapshot extractor + Python CLI ingestion path (C9). Architectural decisions span D-0024..D-0043 (twenty decisions). Test footprint: 141 Python tests at standalone-runner pass + Apps Script tests via `clasp run runAllTests_` on the launcher project. The remaining gap to a fully operational pilot is the one-time Drive setup the maintainer performs once per environment per the M2 C9 sign-off note above. Active milestone moves to *none*; M3 activation deferred to maintainer call.

- **M2 C8 — `spacingPenalty` geometric-decay shape (port v1)** *(closed 2026-04-28)*
  - Replaced v2's binary `gap < 3` `spacingPenalty` count with v1's geometric decay per `docs/decision_log.md` D-0039 + the new normative section `docs/scorer_contract.md` §12A. Per-pair contribution is now `weight / 2^(gap - 2)` for `gap ∈ {2..6}`, zero past `gap = 7`; the 7-day cutoff embeds the once-per-week call cadence as the natural rhythm we neither encourage nor discourage.
  - **Scorer `contractVersion` bumped from `2` to `3`** per §2's bump rule. Same `ScoringConfig` produces different `spacingPenalty.score` under v3 vs v2 because gaps in `[3, 6]` now contribute non-zero where they previously contributed zero — that is a scoring-semantics change for valid configs, so consumers relying on the version signal need the bump. Public `ScoringConfig` shape is unchanged (`weights[spacingPenalty]` stays a single scalar). The bump is the discriminator vs D-0038, which tightened admission of `pointRules` keys (no semantics change) and therefore did not bump.
  - **Phased docs-first delivery:** Phase 1 docs (PR #92) → Phase 2 code (PR #93) → Phase 3 closure (this PR). Phase 1 codex review surfaced 3 findings across 2 rounds: P1 contractVersion bump (initial draft said no bump) + P2 strict-monotonic-decrease wording self-contradictory at `weights[spacingPenalty] = 0` (the operator-disable state) + follow-on P2 stale `v2` refs in 4 downstream contracts (`docs/snapshot_contract.md` §11A, `docs/sheet_generation_contract.md` §11A, `docs/parser_normalizer_contract.md` §9, `docs/future_work.md` FW-0024 context) flagged by the §14 bidirectional-audit rule. Each fix lands in the same PR.
  - **`python/rostermonster/scorer/components.py`** — `_SPACING_MIN_GAP_DAYS = 3` replaced with `_MAX_SOFT_GAP_DAYS = 6`. `spacing_penalty()` rewritten to sum per-pair contributions across ALL same-doctor call-pair combinations (not just consecutive) per §12A's "across all combinations" wording. Curve formula `weight / 2^(gap - 2)` applied for `gap ∈ {2..MAX_SOFT_GAP_DAYS}`; gaps past cutoff and gaps `< 2` (defensive — rule engine already rejects `BACK_TO_BACK_CALL`) contribute zero.
  - **`python/tests/test_scorer.py`** — model fixture widened from 5 days to 14 days so spacing tests can probe gaps up to + past `MAX_SOFT_GAP_DAYS`. Updated `test_spacing_penalty_fires_on_close_call_pair` to assert exact `-2.0` at gap=2. Replaced the gap=4 zero-pin (now `-0.5` under decay) with `test_spacing_penalty_zero_past_soft_cutoff` at gap=7. Added 3 property tests: `test_spacing_penalty_geometric_decay_progression` (exact halving sequence at gaps 2..6), `test_spacing_penalty_strict_monotonic_decrease_property` (§12A property test across multiple negative weights), `test_spacing_penalty_zero_when_weight_disabled` (operator-disable path). Tests: 23 scorer + 38 parser + 14 rule_engine + 29 solver + 20 selector + 9 integration smoke + 3 real-data = **136/136 pass** via standalone runners.
  - **Bidirectional audit dogfooded again.** D-0039's contract-side change (scorer §12A new + scorer §2 contractVersion bump) was self-audited against the §14 rule. The forward direction (consumer references producer) caught nothing new. The reverse direction (producer references consumer) caught the four stale `v2` cross-refs in PR #92's Codex re-review — a recurrence of the M2 C2 audit-coverage gap that motivated §14 itself, surfaced and fixed inside the same PR rather than four contract-touches later.
  - Locked architectural decisions: D-0039 (spacingPenalty geometric-decay shape pinned + scorer v2 → v3 bump). FW-0007 (operator-tuneable curve parameters: alternative `MAX_SOFT_GAP_DAYS`, alternative decay shapes such as linear or exponential with operator-supplied half-life) remains deferred — same discipline as `crReward`'s curve in §12.
  - Delivered as three sequential PRs (Phase 1 docs PR #92 + Phase 2 code PR #93 + Phase 3 closure PR — this PR).
  - Main affected surfaces: `docs/scorer_contract.md` (§2 v2 → v3, §2.1 v3 history entry, §12A new normative subsection, §22 v3 follow-on); `docs/decision_log.md` (D-0039 appended); `docs/snapshot_contract.md`, `docs/sheet_generation_contract.md`, `docs/parser_normalizer_contract.md`, `docs/future_work.md` (downstream `v2` → `v3` cross-ref bumps); `python/rostermonster/scorer/components.py` (`spacing_penalty()` rewritten); `python/tests/test_scorer.py` (4 new spacing tests; model fixture widened); `docs/delivery_plan.md` (this section + §7 + §8 + §9 + §12 + §15 transition).

### M2 C8 sign-off note
M2 C8 is complete. The scorer's `spacingPenalty` component now matches v1's geometric-decay reference shape, restoring the continuous gradient the solver's `SEEDED_RANDOM_BLIND` Phase 2 tie-break needs to distinguish "tightly-spaced but legal" from "comfortably-spaced" pairs. Scorer `contractVersion` is now `3`; the four downstream contracts that pin scorer's contract version were synchronized in the same change round so producer/consumer docs no longer drift across the version bump. Operator-tuneable curve parameters remain FW-0007 deferred. Remaining M2 candidate per §7 is the snapshot extractor (D-0036 — pinned to late-M2 before M3 activation). Active milestone (M2) unchanged; active checkpoint set back to "no active checkpoint" per §8. M3 stays `Planned`.

- **M2 C7 — Scorer Config tab (D-0037 sheet-generator half)** *(closed 2026-04-28)*
  - Apps Script launcher now generates the **Scorer Config tab** declared in `docs/sheet_generation_contract.md` §11A (D-0037 producer-side complement to M2 C6's consumer-side wiring). Pairs each request-entry tab with a `Scorer Config <vMMDDHHMMSS>` companion tab in the same spreadsheet.
  - **`apps_script/m1_sheet_generator/src/TemplateArtifact.gs`** extended: `pointRows[].slotType` binding declared (`MICU_CALL_POINT → MICU_CALL`, `MHD_CALL_POINT → MHD_CALL`) per D-0037; `scoring.componentWeights` declares the 9 first-release defaults (penalties non-positive, rewards non-negative) per template_artifact §11.
  - **`apps_script/m1_sheet_generator/src/ScorerConfigTab.gs`** (new) — 9 rows × 3 columns: friendly label / weight (operator-editable) / sign-orientation hint. Each data row carries `DeveloperMetadata` with key `rosterMonster:componentId` for stable extractor lookup decoupled from cell layout. Tab protection locks all cells except the weight column. Frozen header row.
  - **`apps_script/m1_sheet_generator/src/GenerateSheet.gs`** — both new-spreadsheet and existing-spreadsheet entrypoints now call `buildScorerConfigTab_` after the request-entry tab finishes; response shape gains `scorerConfigSheetName`.
  - **Tests:** `Tests.gs` gains 6 unit-runnable cases — pointRows.slotType present + references known CALL slot, componentWeights covers all 9 components, sign-orientation correct, tab-name pairing rule, canonical component order matches Python `ALL_COMPONENTS`. Apps Script tests run via `clasp run runAllTests_`.
  - **FW-0024 attempted but reverted on Codex review.** A `CallPointAutoFill.gs` module with a simple `onEdit(e)` trigger was drafted in this PR but Codex P1 (PR #89) flagged that simple triggers don't fire on launcher-generated standalone-Web-App spreadsheets. The proper fix needs installable triggers (`ScriptApp.newTrigger(...).forSpreadsheet(...).onEdit().create()`) with quota / ownership / cleanup discipline. Deferred to a dedicated checkpoint when pilot-operator feedback or D-0036 extractor work warrants the trigger-management cost. The FW-0024 entry in `docs/future_work.md` was updated to capture the trigger-architecture finding so the next attempt has a clearer starting point.
  - **Manual verification path:** after merge, deploy via `clasp push` then run the live launcher (https://tinyurl.com/cghicuhdlauncherv1) to confirm:
    1. Two tabs appear post-generation: request-entry + Scorer Config (matching version suffix).
    2. Scorer Config tab shows 9 rows pre-populated with template defaults; weight column is operator-editable; rest is locked.
    3. Per-day call-point cells on the request-entry tab are pre-populated with weekday/weekend defaults (already shipped pre-C7 via `Layout.gs`'s existing `computeDefaultCallPoint_` call).
  - Locked architectural decisions: none new — closes the D-0037 producer-side gap that M2 C6 left open. FW-0024 stays deferred per the updated entry in `docs/future_work.md`.
  - Delivered as the closure PR (single PR — Apps Script module additions + tests + delivery-plan transition).
  - Main affected surfaces: `apps_script/m1_sheet_generator/src/TemplateArtifact.gs`, `GenerateSheet.gs`, `Tests.gs` (existing); `ScorerConfigTab.gs` (new); `docs/future_work.md` (FW-0024 updated with trigger-architecture finding); `docs/delivery_plan.md` (this section + §7 + §8 + §12 + §15 transition).

### M2 C7 sign-off note
M2 C7 is complete. The producer-side of D-0037 now lives in code: the Apps Script launcher generates the Scorer Config tab declared in §11A, and the request-entry tab carries `pointRows[].slotType` bindings the parser overlay needs. Together with M2 C6's consumer-side overlay, the full operator-tuneable scoring-config surface from D-0037 is wired end-to-end on both stack halves — except for the snapshot-extraction step that bridges them. FW-0024 (auto-repopulate empty call-point cells) was attempted in this PR but reverted on Codex review — simple `onEdit` triggers don't fire for launcher-generated standalone-Web-App spreadsheets; the FW-0024 entry has been updated to capture the trigger-architecture finding for the next attempt. Remaining M2 candidate per §7 is the snapshot extractor (D-0036 — pinned to late-M2 before M3 activation). Active milestone (M2) unchanged; active checkpoint set back to "no active checkpoint" per §8. M3 stays `Planned`.

- **M2 C6 — Parser overlay for scoring config** *(closed 2026-04-28)*
  - Wired the Python consumer-side of D-0037 (operator-tuneable scoring config surface). Producer-side — Apps Script sheet generator emitting the Scorer Config tab + the production snapshot extractor reading it — remains scoped to D-0036's late-M2 Apps Script checkpoint.
  - **`python/rostermonster/snapshot.py`** — `Snapshot` grows `scoringConfigRecords: ScoringConfigRecords` (default empty) per snapshot_contract §5. New raw record types `ComponentWeightRecord` + `CallPointRecord` and locator types `ComponentWeightLocator` (`surfaceKey=scorerConfigCells`) + `CallPointLocator` (`surfaceKey=callPointCells`) per §10 + §11A.
  - **`python/rostermonster/template_artifact.py`** — `TemplateArtifact` grows `pointRows` and `componentWeights` per template_artifact §9 + §11. New `PointRowDefinition` (with `slotType` binding per D-0037) + `PointRowDefaultRule` (the four-field weekday/weekend mapping: `1.0 / 1.75 / 2.0 / 1.5` for ICU/HD first release).
  - **`python/rostermonster/parser/result.py`** — `ParserResult.scoringConfig: ScoringConfig | None` field added; CONSUMABLE branch carries the parser-built config, NON_CONSUMABLE forces both `normalizedModel` and `scoringConfig` to `None` per parser_normalizer §9 (D-0037).
  - **`python/rostermonster/parser/scoring_overlay.py`** (new) — `build_scoring_config(snapshot, template, model)` implements the §9 sheet-wins / template-defaults-backstop overlay. Component weights: parse + sign-validate per scorer §10 / §15 (penalty ≤ 0; reward ≥ 0); blank cells fall back to template default. Per-day call points: parse populated cells, blank cells fall back to `pointRows.defaultRule` keyed by today/tomorrow weekday-vs-weekend classification (calendar-based; first release has no public-holiday calendar). Translates `(callPointRowKey, dayIndex) → (slotType, dateKey)` via `pointRows.slotType` binding. Total cross-product coverage enforced per D-0038 (defensive belt-and-braces check at the end).
  - **`python/rostermonster/parser/admission.py`** — overlay invoked after `_verify_handoff_consistency`; any overlay issue surfaces as NON_CONSUMABLE, accumulated alongside upstream issues per the parser's existing per-stage idiom.
  - Admission cases per parser_normalizer §14 (D-0037 + D-0038): mis-signed operator weight; malformed/non-numeric operator-edited cell value (component weight or call-point); duplicate component or call-point record; unknown component / call-point row key / day index; incomplete pointRules cross-product; template default itself mis-signed (template-validity defect distinct from operator-side mis-sign); template default missing for a first-release component.
  - Producer-side (NOT in scope for this checkpoint, deferred to D-0036): the launcher-generated Scorer Config tab and the production snapshot extractor reading it. The test-only xlsx → snapshot bridge (`python/tests/extract_icu_hd_may_2026.py`) was extended to emit empty `scoringConfigRecords` so the parser overlay falls back to template defaults end-to-end on the dev-copy fixture.
  - Tests: `python/tests/test_parser.py` gains 8 overlay cases (defaults backstop, weekday/weekend default mapping, operator weight override, blank-falls-back, mis-signed weight NON_CONSUMABLE, malformed weight NON_CONSUMABLE, per-day call-point override flowing through `pointRows.slotType` binding, malformed call-point NON_CONSUMABLE). `python/tests/test_real_icu_hd_may_2026.py` extended to assert `scoringConfig` is populated on the real-data CONSUMABLE path with full cross-product coverage. `python/tests/test_integration_smoke.py` gains a "parser-supplied scoringConfig flows end-to-end" test proving the parser → scorer handoff for scoring config is wired on real data.
  - Tests: 29 parser + 3 real-data + 14 rule engine + 20 scorer + 29 solver + 20 selector + 9 integration smoke = **124/124 pass** via standalone runners; pytest-compatible.
  - Locked architectural decisions: none new — implementation faithfully tracks D-0037 + D-0038 contract semantics.
  - Delivered as the closure PR (single PR — module additions + tests + delivery-plan transition).
  - Main affected surfaces: `python/rostermonster/snapshot.py`, `python/rostermonster/template_artifact.py`, `python/rostermonster/parser/result.py`, `python/rostermonster/parser/admission.py`, `python/rostermonster/parser/scoring_overlay.py` (new); `python/tests/fixtures.py` (componentWeights + pointRows defaults), `python/tests/extract_icu_hd_may_2026.py` (emit empty scoringConfigRecords), `python/tests/data/icu_hd_may_2026_snapshot.json` (re-extracted), `python/tests/test_parser.py` + `test_real_icu_hd_may_2026.py` + `test_integration_smoke.py` (overlay coverage); `docs/delivery_plan.md` (this section + §7 + §8 + §12 + §15 transition).

### M2 C6 sign-off note
M2 C6 is complete. The Python consumer-side of D-0037 (operator-tuneable scoring config surface) now lives in code: `Snapshot.scoringConfigRecords` is a real field, the parser admission pipeline runs the §9 sheet-wins / template-defaults-backstop overlay, and the §14 admission cases (mis-signed weights, malformed numerics, incomplete pointRules cross-product per D-0038) are guarded by tests on both synthetic and real-data fixtures. The integration smoke now exercises the parser → scorer handoff for scoring config end-to-end. The remaining producer-side work — Apps Script sheet generator emitting the Scorer Config tab + the production snapshot extractor reading it — is the substantial half of the D-0037 story still outstanding, scoped to the late-M2 D-0036 checkpoint that bridges the operator's real spreadsheet to the Python pipeline. M3 stays `Planned`.

- **M2 C5 — Local run artifact basics** *(closed 2026-04-28)*
  - Implemented the selector stage (`HIGHEST_SCORE_WITH_CASCADE` per `docs/selector_contract.md` §11.1 + §12) and the `BEST_ONLY` / `FULL` retention surface with sidecar artifacts (`candidates_summary.csv`, `candidates_full.json`) under `FULL`. Closes the contract → implementation gap that `docs/selector_contract.md` has carried since M2 C2 closure on 2026-04-26.
  - Public entry: `select(scoredCandidateSet, *, retentionMode, runEnvelope, selectorStrategyId, selectorStrategyConfig=None, sidecarTargetDir=None) → FinalResultEnvelope`. Branch discipline (§10.3) — exactly one of `AllocationResult` (success) or `UnsatisfiedResultEnvelope` (failure) is in `result`. Strategy + retention-mode gating rejects unregistered identifiers before any §10 output construction begins (§11.1, §13).
  - `HIGHEST_SCORE_WITH_CASCADE` cascade per §12.2: max `totalScore`, then higher `pointBalanceGlobal`, then higher `crReward`, then lowest `candidateId`. Implemented as a single deterministic sort key — Python's tuple comparison applies the cascade in one pass.
  - Sidecar artifacts per §14 are byte-identical under repeated runs on a single platform per §18: `candidates_summary.csv` carries `schemaVersion: 1` (top-of-file comment), header `candidateId, totalScore, <9 components>, runId, seed, batchId` (with `batchId = ""` since first-release `SEEDED_RANDOM_BLIND` does not surface batches per `docs/solver_contract.md` §18.2); `candidates_full.json` carries `schemaVersion: 1`, `runId`, `generationTimestamp`, and per-candidate full `AssignmentUnit[]` + full `ScoreResult` indexed by `candidateId`. JSON serialization uses `sort_keys=True, indent=2` for deterministic output. Filesystem placement is execution-layer-owned per §14.3 — caller passes `sidecarTargetDir`.
  - Failure-branch (§15): selector forwards solver's `UnsatisfiedResult.unfilledDemand` / `reasons` / `diagnostics` unchanged into `UnsatisfiedResultEnvelope`. No sidecars regardless of `retentionMode`.
  - `runEnvelope` rides through unchanged on `FinalResultEnvelope.runEnvelope` per §16.4 — the selector synthesizes nothing about identity; `runId`, `generationTimestamp`, and the v2-required `sourceSpreadsheetId` / `sourceTabName` (D-0032) are passed through.
  - Architectural import-boundary check in tests: selector module MUST NOT import `score` from `rostermonster.scorer` nor `evaluate` from `rostermonster.rule_engine` per §9. Importing the *types* (`ScoreResult`, etc.) is permitted and necessary — the contract explicitly allows the selector to preserve the full component breakdown into `winnerScore` and sidecar columns.
  - `python/tests/test_integration_smoke.py` extended through the selector — the full M2 pipeline (parser → solver → scorer → selector) now runs end-to-end on the real ICU/HD May 2026 fixture with byte-identical re-runs verified and FULL-mode sidecar emission round-tripped (CSV row count matches `maxCandidates`, JSON candidates indexed by candidateId).
  - Tests: 21 parser + 3 real-data + 14 rule engine + 20 scorer + 29 solver + 20 selector + 8 integration smoke = **115/115 pass** via standalone runners; pytest-compatible.
  - Locked architectural decisions: none new — this checkpoint operationalizes the selector contract closed under M2 C2 (D-0030, D-0032) without touching contract semantics.
  - Delivered as the closure PR (single PR — module + tests + delivery-plan transition).
  - Main affected surfaces: `python/rostermonster/selector/` (new — `__init__.py`, `result.py`, `strategy.py`, `sidecars.py`, `selector.py`); `python/tests/test_selector.py` (new); `python/tests/test_integration_smoke.py` (extended through selector); `docs/delivery_plan.md` (this section + §7 + §8 + §12 + §15 transition).

### M2 C5 sign-off note
M2 C5 is complete. The selector stage from M2 C2's contract closure (2026-04-26) now has a working implementation, and the `BEST_ONLY` / `FULL` retention surface with sidecar artifacts is live. The full M2 compute pipeline — parser → solver → scorer → selector — is end-to-end runnable on real ICU/HD May 2026 operator data with byte-identical re-runs across the entire stack and FULL-mode audit artifacts on disk. No architectural decisions surfaced; the implementation faithfully tracks the selector contract closed under D-0030 / D-0032 (selector v1 → v2). The remaining M2 candidate per §7 is the late-M2 snapshot-extraction Apps Script implementation per D-0036, which closes the inbound Apps Script ↔ Python production seam before M3 ships writeback. Active milestone (M2) unchanged; active checkpoint set to "no active checkpoint" per §8 pending separate scoping of that final M2 checkpoint. M3 stays `Planned`.

- **M2 C4 — Minimal rule/scorer/solver integration** *(closed 2026-04-27)*
  - Shipped the three remaining M2 compute-pipeline stages — rule engine, scorer, solver — wired onto the parser/normalizer's CONSUMABLE `NormalizedModel` from M2 C3, with an integration smoke test exercising parser → solver → scorer end-to-end on the real ICU/HD May 2026 fixture.
  - **T1 — Rule engine implementation.** Stateless reference implementation of all 5 first-release hard rules per `docs/rule_engine_contract.md` §11 (`BASELINE_ELIGIBILITY_FAIL`, `SAME_DAY_HARD_BLOCK`, `SAME_DAY_ALREADY_HELD`, `UNIT_ALREADY_FILLED`, `BACK_TO_BACK_CALL`) with full-violation canonical ordering per §12, FixedAssignment scoped handling per §15, and the public surface `(normalizedModel, ruleState, proposedUnit) → Decision`. Lives at `python/rostermonster/rule_engine/`.
  - **T2 — Scorer implementation.** Pure-function `score(allocation, normalizedModel, scoringConfig) → ScoreResult` covering all 9 first-release components per `docs/scorer_contract.md` + `docs/domain_model.md` §11.2; `HIGHER_IS_BETTER` direction-guard property test per §13; harmonic `crReward` curve satisfying §12 strict-monotonic-decrease; scorer-owned soft-effect reading per §14; required component breakdown per §10. Architectural revision mid-checkpoint (D-0037) bumped scorer to v2 with operator-tuneable `pointRules` (per-day call-point weights) flowing through the parser overlay; D-0038 reversed D-0037 sub-decision 5's silent-fallback rule to a fail-loud per-key lookup with parser producer-coverage obligation. Lives at `python/rostermonster/scorer/`.
  - **T3 — Solver implementation.** Pure-function `solve(...)` with the `SEEDED_RANDOM_BLIND` first-release strategy per `docs/solver_contract.md` §11.1 + §12 (Phase 1 `CR_MINIMUM_PER_DOCTOR` preference seeding + Phase 2 `MOST_CONSTRAINED_FIRST` fill); `crFloor` `SMART_MEDIAN` / `MANUAL` modes per §13; whole-run `UnsatisfiedResult` per §14; byte-identical determinism per §16. Scoring-blind end-to-end (architectural import-boundary check in tests). Lives at `python/rostermonster/solver/`. Codex review surfaced 7 rounds of follow-ups across PR #85 (frozenset-iteration determinism leak under `PYTHONHASHSEED`; `crFloor` mode-equality + integer-type validation; fixed-assignment preservation + `ruleEngine` as input #2 instead of hard-wired import; integer `candidateId` per `docs/selector_contract.md` §16; `maxCandidates` integer guard; signed-seed RNG aliasing fix; per-unit doctor dedup against duplicated `eligibleGroups`); each fix is captured by a regression test in the merged PR.
  - **T4 — Integration smoke test.** End-to-end pipeline test (`python/tests/test_integration_smoke.py`) loads the real ICU/HD May 2026 snapshot fixture, parses to CONSUMABLE, runs the solver with the rule engine handle, scores every emitted candidate, asserts non-empty `CandidateSet`, byte-identical re-runs under fixed seed, zero rule-engine hard-rule violations across candidates, and complete `ScoreResult` shape. Sortable by `totalScore` per §10's `HIGHER_IS_BETTER` direction.
  - In-checkpoint architectural-discipline additions: PR #80 added a bidirectional contract-audit rule to §14 (forward + reverse seam tracing — recurrence prevention for the M2 C2 audit miss that D-0037 ultimately closed); PR #83 added FW-0024 (auto-repopulate empty call-point cells on the operator-facing sheet under D-0037's overlay backstop).
  - Locked architectural decisions: D-0037 (operator-tuneable scoring config surface — scorer v1 → v2 across 5 contracts), D-0038 (`pointRules` fail-loud on missing keys, supersedes D-0037 sub-decision 5).
  - Delivered as PR #78 (T1 — rule engine), PR #79 (T2 — scorer), PR #80 (bidirectional audit rule in §14), PR #81 (D-0037 — scorer v1 → v2 + 5-contract architectural revision), PR #82 (T2 patch — scorer consumes `pointRules`), PR #83 (FW-0024), PR #84 (D-0038 — fail-loud `pointRules`), PR #85 (T3 — solver `SEEDED_RANDOM_BLIND`), and the closure PR (T4 — integration smoke test + T5 delivery-plan transition).
  - Tests: 21 parser + 3 real-data + 14 rule engine + 20 scorer + 29 solver + 5 integration = **92/92 pass** via standalone runners; pytest-compatible.
  - Task closure status: T1 Done, T2 Done, T3 Done, T4 Done, T5 Done.
  - Main affected surfaces: `python/rostermonster/rule_engine/` (new — `__init__.py`, `evaluator.py`, `result.py`, `state.py`); `python/rostermonster/scorer/` (new — `__init__.py`, `scorer.py`, `components.py`, `result.py`); `python/rostermonster/solver/` (new — `__init__.py`, `solver.py`, `strategy.py`, `result.py`, `cr_floor.py`); `python/tests/test_rule_engine.py`, `python/tests/test_scorer.py`, `python/tests/test_solver.py`, `python/tests/test_integration_smoke.py` (new); `docs/scorer_contract.md` (v1 → v2 under D-0037; §11 fail-loud under D-0038); `docs/parser_normalizer_contract.md` (§9 producer-coverage rule + §14 admission cases under D-0037 / D-0038); `docs/snapshot_contract.md` (§5 + §11A scoring-config records); `docs/template_artifact_contract.md` (§11 + §12 weights + point-row scoring posture); `docs/sheet_generation_contract.md` (§11A Scorer Config tab); `docs/decision_log.md` (D-0037 + D-0038 appended); `docs/future_work.md` (FW-0024 appended); `docs/open_decisions.md` (OD-0002 closed by D-0037, removed); `docs/delivery_plan.md` §14 (bidirectional audit rule).

### M2 C4 sign-off note
M2 C4 is complete. The M2 compute pipeline is now functionally end-to-end: parser/normalizer (M2 C3) → rule engine + solver + scorer (M2 C4), exercised in milliseconds on the real ICU/HD May 2026 fixture (29 days, 22 doctors, 638 request records). The integration smoke test proves byte-identical determinism + zero rule-engine violations + complete score breakdowns over a non-empty `CandidateSet`. Selector implementation is intentionally NOT in C4 — its retention modes and sidecar artifacts will land in the next "local run artifact basics" candidate per §5. Two architectural revisions landed mid-checkpoint and improved the contract surface meaningfully: D-0037 unified component weights and per-day call-point cells under one operator-tuneable scoring-config flow (scorer v1 → v2 + 5 contracts touched), and D-0038 reversed D-0037 sub-decision 5 to make `pointRules` fail-loud on missing keys with a parser producer-coverage obligation. The bidirectional contract-audit rule added to §14 (PR #80) is the recurrence-prevention measure surfaced by the same audit miss D-0037 ultimately closed; FW-0024 (PR #83) parks the operator-facing UX gap that the parser overlay backstop creates. Active milestone (M2) unchanged; active checkpoint set to "no active checkpoint" per §8 pending separate scoping of the next M2 checkpoint (candidates per §5 / §7: local run artifact basics, snapshot-extraction Apps Script implementation per D-0036). M3 stays `Planned`.

- **M2 C3 — Parser/normalizer implementation closure** *(closed 2026-04-27)*
  - Shipped the **first executable code under M2** — the local-first Python parser/normalizer module per `docs/decision_log.md` D-0018 (local-first Python direction), against the closed M2 C1 + M2 C2 contract surfaces (rule engine, scorer, solver, selector). Greenfield package skeleton at `python/` (Python 3.11+, no runtime deps beyond stdlib), with `rostermonster.snapshot` (input shapes per `docs/snapshot_contract.md`), `rostermonster.template_artifact` (parser-consumable subset per `docs/template_artifact_contract.md`), `rostermonster.domain` (normalized output types per `docs/domain_model.md`), and `rostermonster.parser` exposing the public entry `parse(snapshot, template_artifact) -> ParserResult`.
  - **T1 — Parser side admission pipeline.** Implements parser_normalizer_contract.md §6 / §8 / §10 / §11 / §13 / §14 / §15. Stages 1-3 cover §13 structural validation (template-mismatch, doctor-key uniqueness, doctor-locator uniqueness, dayIndex contiguity, rawDateText non-empty / unique, request locator uniqueness + cross-references, prefilled-locator uniqueness + day cross-reference). Stage 4 covers §14 ICU/HD obligations (canonical doctor-group resolution from `inputSheetSections`, slot-demand instantiation, baseline eligibility, request-semantics binding match). Stage 5 covers per-cell ICU/HD grammar parsing per `docs/request_semantics_contract.md` §6 / §7 / §9 / §10 / §11 / §12 / §13 / §15. Stage 5c covers §14 prefilled-assignment cases including the FixedAssignment scoped admission exception. Issue codes are stable strings within this implementation (the contract defers issue-code standardization per §10).
  - **T2 — Normalizer side.** Adds `displayLabel` to `SlotTypeDefinition` (closes the T1 self-audit gap against `docs/domain_model.md` §7.6); wires `provenance` fields onto every snapshot-derived normalized entity (Doctor / RosterDay / Request / FixedAssignment / DailyEffectState / SlotDemand) per parser_normalizer §16 traceability obligation; adds `_verify_handoff_consistency` — a §17 explicit-handoff defense layer that validates the assembled `NormalizedModel` before emitting `CONSUMABLE` (catches Doctor/Request/FixedAssignment/SlotDemand/EligibilityRule/DailyEffectState orphan references plus SlotDemand cross-product completeness; fails closed by surfacing as NON_CONSUMABLE on parser-internal defect).
  - **T3 — Hand-test against ICU/HD May 2026 dev-copy source.** Ships a one-shot test-only xlsx → snapshot bridge (`python/tests/extract_icu_hd_may_2026.py`), a committed JSON fixture (`python/tests/data/icu_hd_may_2026_snapshot.json` — 22 doctors, 29 days, 638 request records, 0 prefilled assignments), and three hand-test cases exercising the parser end-to-end on real operator-supplied data. Confirms parser handles real ICU/HD volume cleanly in milliseconds.
  - Locked architectural decisions: D-0033 (snapshot adapter normalizes dates to ISO 8601; parser uses `rawDateText.strip()` directly as `dateKey`), D-0034 (doctor-name matching algorithm: trim + collapse internal whitespace + casefold on both sides; punctuation/diacritics not folded), D-0035 (provenance shape locked at locator-direct, closing OD-0001 from `docs/open_decisions.md`), D-0036 (snapshot-extraction Apps Script implementation pinned to a late-M2 checkpoint before M3 activation; transport jointly scoped with `docs/writeback_contract.md` §6.3).
  - Delivered as PR #70 (T1 — admission pipeline), PR #71 (D-0033 + D-0034), PR #72 (T2 — normalizer side), PR #73 (T2 codex follow-up — DailyEffectState orphan + SlotDemand completeness checks added to the §17 defense), PR #74 (T3 — real-data hand-test, codex round-trip on extractor contract conformance: `rawSectionText` raw header preservation per snapshot_contract.md §7, `rawRequestText` no-trim per §9), PR #75 (D-0035 + D-0036 — provenance lock + Apps Script extractor pinning + writeback §6.3 joint-transport addendum).
  - Tests: 21 synthetic admission cases + 3 real-data hand-test cases = **24/24 pass** via standalone `python3 python/tests/test_parser.py` and `python3 python/tests/test_real_icu_hd_may_2026.py` runners; pytest-compatible.
  - Task closure status: T1 Done, T2 Done, T3 Done, T4 Done.
  - Main affected surfaces: `python/rostermonster/` (greenfield package — `__init__.py`, `domain.py`, `snapshot.py`, `template_artifact.py`, `parser/__init__.py`, `parser/result.py`, `parser/request_semantics.py`, `parser/admission.py`); `python/tests/` (`fixtures.py`, `test_parser.py`, `extract_icu_hd_may_2026.py`, `data/icu_hd_may_2026_snapshot.json`, `test_real_icu_hd_may_2026.py`, `__init__.py`); `python/pyproject.toml`, `python/README.md`; `docs/decision_log.md` (D-0033..D-0036 appended); `docs/open_decisions.md` (new doc — empty after OD-0001 closure); `docs/writeback_contract.md` §6.3 (joint-transport addendum per D-0036); `.gitignore` (Python entries appended).

### M2 C3 sign-off note
M2 C3 is complete. The parser/normalizer module is now functionally complete for ICU/HD first-release inputs end-to-end — admission pipeline, normalizer-side polish, and real-data validation all shipped. The compute-pipeline contracts settled in M2 C1 + M2 C2 now have their first implementation living against them; downstream stages (rule engine, scorer, solver, selector, writeback) remain deferred per §10 and will be scoped individually as subsequent M2 checkpoints. The architectural seam between Apps Script (sheet-facing) and Python (compute-heavy) per D-0017 / D-0018 is now exercised on the inbound side: the test-only xlsx → JSON bridge proves the seam works against real ICU/HD May 2026 volume, and the production Apps Script extractor that supersedes it is pinned to a late-M2 checkpoint per D-0036, before M3 activation, so the inbound seam is exercised end-to-end before M3 ships writeback. `docs/open_decisions.md` is empty at C3 closure. The next M2 checkpoint candidates per §5 / §7 are minimal rule/scorer/solver integration, local run artifact basics, and the late-M2 Apps Script extractor; sequencing will be picked at the next checkpoint scoping.

- **M2 C2 — Selector contract closure** *(closed 2026-04-26)*
  - Closed the selector-stage contract boundary with `docs/selector_contract.md` covering: pure-function public surface with `(scoredCandidateSet, retentionMode, runEnvelope, selectorStrategyId, selectorStrategyConfig) → FinalResultEnvelope` shape; pluggable strategy interface mirroring the solver's `StrategyDescriptor` pattern; first-release `HIGHEST_SCORE_WITH_CASCADE` strategy with `pointBalanceGlobal` → `crReward` → lowest `candidateId` cascade; retention modes `BEST_ONLY` (default) and `FULL` (operator opt-in) with per-mode output behavior; sidecar artifacts `candidates_summary.csv` and `candidates_full.json` cross-referenced by `candidateId` and embedding `runId`; no-sidecar-on-failure rule; run envelope and `(runId, candidateId)` traceability identity with execution-layer-supplied `runId`; selector-owned retroactive population of per-batch best-candidate field plus per-batch score-distribution summary; byte-identical determinism within a single implementation on a single platform; sidecar `schemaVersion: 1`.
  - Locked architectural decisions: D-0030 (selector contract closure at v1) and D-0032 (selector contract bumped from v1 to v2 inside the still-active C2 window after PR #66 codex review surfaced a contract-seam consistency gap with the writeback scope-ahead contract drafted in the same change round; the upstream-patch resolution adopted in place of the §16.3 additivity-clause approach initially proposed in D-0031 sub-decision 11 added `runEnvelope.sourceSpreadsheetId` and `runEnvelope.sourceTabName` to selector §9 item 3's required-fields list so selector compliance under v2 implies writeback input compatibility on the source-sheet-identity surface).
  - Delivered as PR #65 (selector contract draft at v1 + D-0030 + FW-0013 cross-reference + FW-0018 + delivery-plan updates), PR #66's selector v1 → v2 portion (D-0032 + selector contract bumped to v2 + delivery-plan change-log entry; the writeback contract drafted in the same PR is M3 scope-ahead per D-0031 and is not part of C2's closure scope), and PR #67 (M2 contract polish sweep across rule-engine / scorer / selector / writeback surfaces with no contract semantics change).
  - Task closure status: T1 Done, T2 Done, T3 Done, T4 Done.
  - Main affected surfaces: `docs/selector_contract.md` (new at v1 in PR #65; bumped to v2 in PR #66), `docs/decision_log.md` (D-0030 appended in PR #65; D-0032 appended in PR #66), `docs/future_work.md` (FW-0013 cross-reference + FW-0018 in PR #65; FW-0019..FW-0023 added in PR #66 are M3 scope-ahead artifacts under D-0031, not C2 deliverables), `docs/rule_engine_contract.md` / `docs/scorer_contract.md` / `docs/writeback_contract.md` (PR #67 polish only), `docs/delivery_plan.md` (active-checkpoint and change-log updates).

### M2 C2 sign-off note
M2 C2 is complete. The selector-stage contract boundary is now normative and operationalizes D-0026 sub-decision 8 (selector retroactively populates `TrialBatchResult` best-candidate fields). The selector contract is at `contractVersion: 2` after the v1 → v2 bump that closed the contract-seam consistency gap with the writeback scope-ahead contract drafted in the same change round (D-0032). PR #67's polish sweep completed wording-only consistency follow-ons across the M2 contract surfaces without changing any contract semantics, leaving the M2 contract set self-consistent at C2 closure. With C1 and C2 both closed, the three-stage `solver → scorer → selector` pipeline has a complete normative contract surface, and the writeback boundary downstream is drafted scope-ahead per D-0031 (M3 territory, not M2). The next M2 checkpoint candidate per §5 / §7 ordering is parser/normalizer implementation closure; it will be scoped and activated separately as M2 C3.

- **M2 C1 — Rule engine + scorer + solver contract closure** *(closed 2026-04-25)*
  - Closed the three interlocking M2 compute-pipeline contract boundaries: `docs/rule_engine_contract.md` (stateless surface, full-violation canonical ordering, scoped fixed-assignment handling, equivalence-test discipline for future non-stateless implementations), `docs/scorer_contract.md` (pure-function surface, required component breakdown, `HIGHER_IS_BETTER` direction-guard invariant, `crReward` diminishing-marginal-utility property, scorer-owned soft-effect reading, operator-tuneable weight surface, streaming/delta scoring as permitted optimization), and `docs/solver_contract.md` (scoring-blind surface, strategy-pluggable interface with additive extension clause, `SEEDED_RANDOM_BLIND` first-release composite, `crFloor` computation, whole-run failure on any unfillable slot, `maxCandidates`-only termination, byte-identical determinism, retention moved downstream to the selector stage).
  - Locked architectural decisions: D-0024 (rule engine contract), D-0025 (scorer contract), D-0026 (solver contract), D-0027 (pipeline-stage separation `solver → scorer → selector`), D-0028 (operator-tuneable surface broader than blueprint §16), D-0029 (`unitIndex` operational-equivalence — same-`SlotType` units are equivalent in doctor admissibility and baseline workload weight, with per-unit occupancy via `UNIT_ALREADY_FILLED` explicitly carved out).
  - Delivered as PR #63 (rule-engine + scorer + solver contracts plus supporting future-work / decision-log / blueprint patches) and PR #64 (M2 C1 follow-ups closing post-merge audit flags: `unitIndex` equivalence narrowing to doctor-admissibility per D-0029 sub-decision 1, retention opt-in clarifications, byte-identical parity wording propagation, contract fixes into FW-0010 and FW-0012).
  - Task closure status: T1 Done, T2 Done, T3 Done, T4 Done.
  - Main affected surfaces: `docs/rule_engine_contract.md` (new), `docs/scorer_contract.md` (new), `docs/solver_contract.md` (new), `docs/decision_log.md` (D-0024..D-0029 appended), `docs/future_work.md` (FW-0003..FW-0017 appended), `docs/blueprint.md` §16 (clarifying patch), `docs/domain_model.md` §10.2 (D-0029 equivalence note), `docs/delivery_plan.md` (active-checkpoint and change-log updates).

### M2 C1 sign-off note
M2 C1 is complete. The three M2 compute-pipeline contract boundaries (rule engine, scorer, solver) are now normative and consistent with one another and with the surrounding domain-model / blueprint anchors. The pipeline-stage separation `solver → scorer → selector` (D-0027) is committed; D-0026 sub-decision 8 (selector retroactively populates `TrialBatchResult` best-candidate fields) is named but its operationalization is deferred to M2 C2 (selector contract closure), now active per §8. PR #64's post-merge audit follow-ups closed the consistency-propagation flags surfaced by Codex review across the M2 C1 surfaces, leaving the C1 contract set self-consistent at the time of M2 C2 activation.

- **M1.1 C1 — Implement operator launcher web app** *(closed 2026-04-22)*
  - Delivered the M1.1 operator-facing launcher: Apps Script web-app (`doGet()` HTML form + `submitLauncherForm` wiring) against the existing `generateIntoNewSpreadsheet` / `generateIntoExistingSpreadsheet` entrypoints, shared spreadsheet-reference normalization (URL or bare ID) per `docs/sheet_generation_contract.md` §12.5, deployed at a stable `/exec` behind the operator-facing `https://tinyurl.com/cghicuhdlauncherv1` redirect with GCP OAuth consent-screen Test Users curated for the pilot operators.
  - Auto-share of newly-created spreadsheets to "anyone with the link (Editor)" landed during C1 as additive operational polish on top of T1–T4; the directional decision, the three-attempt sequence (`DriveApp` under `drive.file` → `DriveApp` under full `drive` → Drive Advanced Service v3 under `drive.file`), and the additive response-field surface (`autoShared`, `autoShareError`) are recorded in **D-0023**. No contract structural change was required — the additive fields sit within the `docs/sheet_generation_contract.md` §12 launcher surface already described there.
  - Verified end-to-end through the non-maintainer Test-User consent + generation path on a separate Google account and device; both output modes (new spreadsheet file, new tab in existing spreadsheet) exercised successfully, with negative-case input handling confirmed via an out-of-range 2027 request date. Pilot operator self-report independently confirms the end-to-end flow; pilot operators were not pushed through a recorded demo to avoid forcing OAuth consent for a maintainer-authored Test-User app.
  - Task closure status: T1 Done, T2 Done, T3 Done, T4 Done.
  - Main affected surfaces: `apps_script/m1_sheet_generator/` (launcher module, HTML, README); `docs/sheet_generation_contract.md` §12 launcher surface; no authoritative contract changes beyond the §12 addendum already in place.

### M1.1 C1 sign-off note
M1.1 C1 is complete. The operator-facing launcher delivers pilot-usable empty-form ICU/HD request-sheet generation in both output modes, with the non-maintainer OAuth consent path validated hands-on on a separate Google account and device. Pilot operator self-report independently confirms the end-to-end flow works; pilot operators were not pushed through a recorded demo to avoid forcing consent for a maintainer-authored Test-User app, consistent with the first-release access model in D-0022. M1.1's stated exit criteria are met and the launcher is the current pilot-operator entrypoint to ICU/HD request-sheet generation.

- **C4 — Implement operator-ready sheet generation** *(closed 2026-04-21)*
  - Delivered the ICU/HD request sheet shell end-to-end through Google Apps Script in `apps_script/m1_sheet_generator/`, covering both output modes (new spreadsheet file, new tab in existing spreadsheet).
  - Applied whole-sheet protection restricted to the script owner with unprotected exceptions for operator-editable surfaces (doctor-name cells, request-entry cells, call-point cells, lower-shell assignment cells) and warning-only regex validation on request-entry cells.
  - Verified against the C3 acceptance checklist for an operator-owned May 2026 cycle (CGH ICU/HD Call, 2026-05-04 to 2026-06-01, 9/6/7 manpower) via `clasp run` execution against a user-managed GCP project.
  - Operator-facing operational lesson recorded as D-0020: `clasp run` requires both (a) GCP OAuth consent-screen scope allowlist and (b) `clasp login --use-project-scopes --include-clasp-scopes`.
  - Task closure status: T1 Done, T2 Done, T3 Done, T4 Done, T5 Done.
  - Main affected surfaces: `apps_script/m1_sheet_generator/` (code + README); no authoritative contract changes required.

### C4 sign-off note
C4 is complete. M1 is now delivered on operator terms, not just contract closure: the generator produces an operator-usable ICU/HD request sheet shell against settled M1 contracts, in both output modes, with intended editable/protected surfaces and practical validation in place, and has been exercised hands-on against an operator-owned May 2026 spreadsheet.

- **C3 — Define generation acceptance/handoff readiness** *(closed 2026-04-18)*
  - Locked M1 implementation-ready checklist for ICU/HD first-release generation handoff without reopening C1/C2.
  - Declared the first allowed implementation slice (template read + operator inputs + shell generation + output-mode support + practical locking/validation).
  - Declared explicit out-of-scope items to prevent parser/compute/writeback/orchestration/benchmark scope drift during generation-slice start.
  - Task closure status: T1 Done, T2 Done, T3 Done.
  - Main affected docs: `docs/delivery_plan.md`.

### C3 sign-off note
C3 is complete. M1 generation boundary is now implementation-handoff ready: acceptance assumptions, immediate generation-slice start scope, out-of-scope guardrails, and final contract-to-implementation notes are explicit and sufficient for execution without reopening milestone-level scope.

- **C2 — Align template artifact vs generation needs** *(closed 2026-04-18)*
  - Closed remaining cross-doc alignment between `docs/template_artifact_contract.md` and `docs/sheet_generation_contract.md`, including first-release visible title/header generated-surface alignment.
  - Confirmed ICU/HD first-release combined-shell generation declarations remain consistency-aligned and non-redesign.
  - Task closure status: T1 Done, T2 Done, T3 Done.
  - Main affected docs: `docs/template_artifact_contract.md`, `docs/sheet_generation_contract.md`, `docs/delivery_plan.md`.

### C2 sign-off note
C2 is complete. Template artifact and generation contract surfaces are now sufficiently aligned for ICU/HD first-release sheet-shell generation usage, with no material remaining ambiguity requiring checkpoint-level redesign before C3 handoff-readiness framing.

- **C1 — Close sheet-generation MVP boundary** *(closed 2026-04-17)*
  - Closed generation inputs, generated structural surfaces, allowed operator edits, editable/protected + validation boundary, explicit non-goals, and acceptance framing at contract/planning level.
  - Task closure status: T1 Done, T2 Done, T3 Done, T4 Done, T5 Done, T6 Done.
  - Main affected docs: `docs/sheet_generation_contract.md`, `docs/delivery_plan.md`.

### C1 sign-off note
C1 is complete. The sheet-generation MVP boundary is now closed for execution: generation inputs, structural surfaces, allowed operator edits, editable/protected + validation expectations, explicit non-goals, and checkpoint acceptance framing are sufficiently fixed to proceed to C2 without reopening milestone-level scope.

### Recently completed milestones
- **M1.1 — Operator-facing launcher** *(closed 2026-04-22)*
  - Closed on hands-on validation via M1.1 C1 against the M1.1 exit criteria. The non-maintainer Test-User consent + generation path was exercised on a separate Google account and device across both output modes, and pilot operator self-report independently confirms the end-to-end flow. Addendum-milestone framing preserved: M1 itself was not reopened; see D-0021 for the addendum convention and D-0022 for the launcher architecture. Auto-share additive operational polish captured in D-0023.
- **M1 — Operator-ready request sheet generation** *(closed 2026-04-21)*
  - Closed on operator delivery via C4, against settled contract surfaces fixed in C1/C2/C3. See D-0019 for the formal closure decision and D-0020 for the operator-facing clasp OAuth operational lesson surfaced during M1 execution.

## 12. Change log for this delivery plan
- **2026-04-16:** Document created as the living execution guide.
- **2026-04-16:** Activated Milestone 1 (`Operator-ready request sheet generation`).
- **2026-04-16:** Activated Checkpoint 1 (`Close sheet-generation MVP boundary`).
- **2026-04-17:** Closed Checkpoint 1 (`Close sheet-generation MVP boundary`) and recorded formal sign-off in this plan.
- **2026-04-17:** Activated Checkpoint 2 (`Align template artifact vs generation needs`) as current execution focus.
- **2026-04-18:** Closed Checkpoint 2 (`Align template artifact vs generation needs`) and recorded formal sign-off in this plan.
- **2026-04-18:** Activated Checkpoint 3 (`Define generation acceptance/handoff readiness`) as current execution focus.
- **2026-04-18:** Closed Checkpoint 3 (`Define generation acceptance/handoff readiness`) with explicit M1 implementation-ready checklist, allowed first implementation slice, out-of-scope guardrails, and contract-to-implementation handoff notes.
- **2026-04-18:** Closed Milestone 1 (`Operator-ready request sheet generation`) and activated Milestone 2 (`Minimal local compute pipeline`).
- **2026-04-18:** Seeded M2 active checkpoint (`Parser/normalizer implementation closure`) with narrow parser-first task framing.
- **2026-04-18:** Reopened Milestone 1 in place so milestone closure aligns with operator delivery rather than contract closure alone; returned Milestone 2 to Planned.
- **2026-04-18:** Activated Checkpoint 4 (`Implement operator-ready sheet generation`) as the current M1 implementation checkpoint; seeded compact task list T1–T5.
- **2026-04-21:** Closed Checkpoint 4 (`Implement operator-ready sheet generation`) with T1–T5 Done; verified the generated shell against the C3 acceptance checklist against an operator-owned May 2026 cycle.
- **2026-04-21:** Closed Milestone 1 (`Operator-ready request sheet generation`) on operator delivery; see D-0019.
- **2026-04-21:** Activated Milestone 2 (`Minimal local compute pipeline`) at milestone level; M2 checkpoints listed but none yet activated.
- **2026-04-21:** Activated Milestone 1.1 (`Operator-facing launcher`) as an addendum to closed Milestone 1; returned Milestone 2 to Planned under the one-active-milestone rule. See D-0021 (addendum-milestone convention) and D-0022 (launcher architecture).
- **2026-04-21:** Activated Checkpoint 1 (`Implement operator launcher web app`) as the M1.1 execution focus; seeded compact task list T1–T4.
- **2026-04-22:** Reconciled §9 C1 task status against shipped code (T1/T2/T3 Done, T4 remains the only pending gate) and captured the auto-share additive scope (D-0023) that landed during C1 without retroactively inserting new tasks. Reconciliation is documentation-only; C1 stays active pending T4 sign-off.
- **2026-04-22:** Closed Task T4 (`Verify end-to-end with a non-maintainer test operator`) on hands-on validation of the non-maintainer Test-User consent + generation path across both output modes on a separate Google account and device, with pilot operator self-report as independent confirmation and a negative-case 2027 request-date check.
- **2026-04-22:** Closed Checkpoint 1 (`Implement operator launcher web app`) under M1.1 with T1–T4 Done; sign-off recorded in §11.
- **2026-04-22:** Closed Milestone 1.1 (`Operator-facing launcher`) on hands-on validation; see §11.
- **2026-04-22:** Activated Milestone 2 (`Minimal local compute pipeline`) at milestone level; first checkpoint to be scoped separately (§8), mirroring the 2026-04-21 "activate at milestone level, checkpoint to follow" pattern.
- **2026-04-23:** Activated M2 Checkpoint 1 (`Rule engine + scorer + solver contract closure`) as the current execution focus; seeded compact task list T1–T4 (T1/T2/T3 already Done on-branch; T4 in progress). C1 is docs-only contract closure and does not itself ship executable code.
- **2026-04-25:** Closed M2 Checkpoint 1 (`Rule engine + scorer + solver contract closure`) on operator audit and merge of PR #63 + PR #64; sign-off recorded in §11. Locked architectural decisions span D-0024..D-0029.
- **2026-04-25:** Activated M2 Checkpoint 2 (`Selector contract closure`) as the current execution focus; seeded compact task list T1–T4. C2 is docs-only contract closure and does not itself ship executable code; it operationalizes D-0026 sub-decision 8 (selector retroactively populates `TrialBatchResult` best-candidate fields) and locks the selector-stage boundary deferred by D-0027 sub-decision 2.
- **2026-04-26:** Drafted writeback contract (`docs/writeback_contract.md`) scope-ahead of M3 activation; recorded D-0031 with all eleven sub-decisions; appended FW-0019..FW-0023 for deferred richer-mode surfaces. During PR #66 codex review, surfaced a contract-seam consistency gap between the writeback contract's required `runEnvelope.sourceSpreadsheetId` / `runEnvelope.sourceTabName` and the selector contract v1's required-fields list; resolved by bumping `docs/selector_contract.md` from v1 to v2 with both fields added to §9 item 3 (D-0032). Selector compliance under v2 implies writeback input compatibility on the source-sheet-identity surface. Active milestone (M2) and active checkpoint state (M2 C2 — still the active checkpoint per §8; the selector v2 bump lands inside the still-active checkpoint rather than after sign-off) unchanged; no new active checkpoint added; M3 stays `Planned`.
- **2026-04-26:** Polish sweep across the M2 contract surfaces and the writeback scope-ahead contract resolving wording-only consistency findings from a self-audit pass: stale `docs/solver_contract.md` §13 anchor in `docs/scorer_contract.md` §16 corrected to §6/§11.1 (the actual scoring-blind anchors); stale "(not yet created)" qualifier in FW-0016 dropped with selector anchors pinned (`docs/selector_contract.md` §13, §21); "additive" misnomer in `docs/writeback_contract.md` §9 item 1 corrected to "required per selector v2 §9 item 3" (matching the §4/§18/§20/§23 wording, since selector §16.3 reserves "additive" for optional-additive only); rule_engine §6 / scorer §6 boundary blocks tightened so scorer/selector are framed as consuming the solver's `CandidateSet` per the D-0027 three-stage separation rather than reading directly from rule engine or pairing with a separate retention-policy stage; selector §11 strategy-descriptor framing notes the deliberate `selectorStrategyId` (vs solver's `strategyId`) namespacing; writeback §6.2 acknowledges the §11.1 wall-clock carve-out named in §9 normative properties. No contract semantics change. Active milestone (M2) and active checkpoint state (M2 C2 — polish lands inside the still-active checkpoint per the same-day selector v2 precedent) unchanged; no new active checkpoint added; M3 stays `Planned`.
- **2026-04-26:** Closed M2 Checkpoint 2 (`Selector contract closure`) on operator audit and merge of PR #65 + PR #66's selector v1 → v2 portion + PR #67 polish sweep; sign-off recorded in §11. Locked architectural decisions span D-0030 and D-0032 (selector contract at v1 then v2). C2 closure is docs-only; the writeback contract drafted in PR #66 is M3 scope-ahead per D-0031 and is not part of C2's closure scope. Active milestone (M2) unchanged; active checkpoint set to "no active checkpoint" per §8 pending separate scoping of M2 C3 (`Parser/normalizer implementation closure`); M3 stays `Planned`.
- **2026-04-26:** Activated M2 Checkpoint 3 (`Parser/normalizer implementation closure`) as the current execution focus; seeded compact task list T1–T4. C3 is the **first M2 checkpoint that ships executable code** (C1 and C2 were both docs-only contract closure) and implements the local-first Python parser/normalizer module per `docs/parser_normalizer_contract.md`, `docs/snapshot_contract.md`, and `docs/domain_model.md` for ICU/HD first-release inputs only. Stack ownership is local-first Python per D-0018; Apps Script remains M1 / launcher / writeback territory. Downstream-stage implementation (rule engine / scorer / solver / selector / writeback) remains deferred per §10. M3 stays `Planned`.
- **2026-04-26:** Closed two open decisions surfaced by M2 C3 T1 implementation (PR #70). D-0033 pins `dateKey` adapter normalization: snapshot adapter emits ISO 8601 `rawDateText`; parser uses `rawDateText.strip()` directly as `dateKey` with no parser-side reformatting. D-0034 pins doctor-`displayName` matching for prefilled-assignment resolution: trim + collapse internal whitespace + casefold on both sides of the comparison; punctuation and diacritics not folded. Also lands the matching code change (`_normalize_doctor_name` helper) plus a positive test fixture (`with_prefill_using_messy_whitespace_and_case`) locking the D-0034 behavior. T1 parser implementation already conformed to D-0033; D-0034 widens the prior trim+casefold-only rule additively. Active milestone (M2) and active checkpoint (M2 C3) unchanged; no new active checkpoint added; M3 stays `Planned`.
- **2026-04-26:** Closed M2 C3 T2 (`Implement the normalizer side`) for the parser/normalizer module. Added `displayLabel` to `SlotTypeDefinition` (closing the T1-self-audit gap against `docs/domain_model.md` §7.6 first-release minimum required fields), wired `provenance` fields onto every snapshot-derived normalized entity (`Doctor`, `RosterDay`, `Request`, `FixedAssignment`, `DailyEffectState`, `SlotDemand`) per `docs/parser_normalizer_contract.md` §16 traceability obligation, and added a §17 explicit-handoff internal-consistency defense layer (`_verify_handoff_consistency`) that validates the assembled `NormalizedModel` before emitting `CONSUMABLE`. New tests (`test_slot_type_definition_carries_display_label`, `test_provenance_traces_back_to_snapshot_locators`, `test_handoff_consistency_check_catches_internal_defects`) lock all three. Created `docs/open_decisions.md` as the new home for pending-decision tracking distinct from `decision_log.md` (closed) and `future_work.md` (ideas); seeded with OD-0001 (provenance field-shape standard, surfaced during T2 implementation, low-priority since the §16 obligation is satisfied by the current locator-direct shape). T2 task closure: T1 Done, T2 Done; T3 (hand-test against ICU/HD May 2026 source) and T4 (delivery-plan sign-off transition) remain. Active milestone (M2) and active checkpoint (M2 C3) unchanged; no new active checkpoint added; M3 stays `Planned`.
- **2026-04-26:** Closed Codex P2 follow-ups on PR #72's §17 handoff-defense layer (PR #73). Extended `_verify_handoff_consistency` with `DailyEffectState` orphan checks and `SlotDemand` cross-product completeness check; tests 19/19 → 21/21. No contract semantics change, no open decisions surfaced. Active milestone (M2) and active checkpoint (M2 C3) unchanged; M3 stays `Planned`.
- **2026-04-27:** Closed M2 C3 T3 (`Hand-test against ICU/HD first-release inputs`) on real-data validation of the parser/normalizer module against the operator-supplied ICU/HD May 2026 dev-copy source (PR #74). Added one-shot test-only xlsx → snapshot bridge (`python/tests/extract_icu_hd_may_2026.py`), committed JSON snapshot fixture (`python/tests/data/icu_hd_may_2026_snapshot.json` — 22 doctors, 29 days, 638 request records, 0 prefilled assignments), and three hand-test cases (one positive end-to-end, two negative — section-unknown and day-axis-broken). Codex round-trip on PR #74 surfaced two P2 contract-conformance flags on the test-only extractor (`rawSectionText` should preserve raw header text per snapshot_contract.md §7; `rawRequestText` should not be trimmed at extraction time per §9), both addressed in the same PR; Codex re-reviewed and returned the clean signal ("Didn't find any major issues. Bravo."). Tests 21 + 3 = 24/24 pass. No new open decisions surfaced — production Apps Script extractor remains its own future checkpoint (now scoped to late-M2 per D-0036, see next entry). Active milestone (M2) and active checkpoint (M2 C3) unchanged; T4 remains; M3 stays `Planned`.
- **2026-04-27:** Closed two decisions surfaced after T3 completion. D-0035 closes OD-0001 by locking the locator-direct provenance shape as the first-release standard — `Doctor.provenance: DoctorLocator`, `RosterDay.provenance: DayLocator`, `Request.provenance: RequestLocator`, `FixedAssignment.provenance: PrefilledAssignmentLocator`, `DailyEffectState.provenance: RequestLocator` (forwarded from originating Request), `SlotDemand.provenance: DayLocator` (snapshot side of template×day product). No code change required; T1+T2+T3 already conformed. OD-0001 removed from `docs/open_decisions.md` per its maintenance rule (closed entries promote to decision_log and are deleted from the open-decisions doc). D-0036 pins the production Apps Script snapshot-extraction adapter as a **late-M2 checkpoint, before M3 activation**, so a real ICU/HD operator sheet can feed the Python pipeline end-to-end at M2 close. M3 stays writeback-only; the inbound Apps Script ↔ Python seam exercised under M2 first. §5 M2 likely-checkpoints list updated to include this new entry. A `docs/snapshot_adapter_contract.md` is now known-forthcoming work (drafted alongside or just before the implementation slice); transport (paste / upload / Drive Picker / alternative) settled jointly with writeback's `docs/writeback_contract.md` §6.3 deferral in the same change round. Active milestone (M2) and active checkpoint (M2 C3) unchanged; no new active checkpoint added; M3 stays `Planned`.
- **2026-04-27:** Closed M2 Checkpoint 3 (`Parser/normalizer implementation closure`) on operator audit and merge of PR #70 + PR #71 + PR #72 + PR #73 + PR #74 + PR #75; sign-off recorded in §11. The first executable code under M2 is shipped end-to-end: parser side admission pipeline (T1, PR #70), normalizer side with provenance + `displayLabel` + §17 internal-consistency defense (T2, PR #72; PR #73 codex follow-up), and real-data hand-test against the ICU/HD May 2026 dev-copy source (T3, PR #74). 24/24 tests pass. Locked architectural decisions span D-0033..D-0036 (dateKey adapter normalization, doctor-name matching, provenance shape locked, Apps Script extractor pinned to late-M2). Active milestone (M2) unchanged; active checkpoint set to "no active checkpoint" per §8 pending separate scoping of the next M2 checkpoint (candidates per §5 / §7: minimal rule/scorer/solver integration, local run artifact basics, snapshot-extraction Apps Script implementation per D-0036); M3 stays `Planned`.
- **2026-04-27:** Activated M2 Checkpoint 4 (`Minimal rule/scorer/solver integration`) as the current execution focus; seeded compact task list T1–T5. C4 implements the three remaining M2 compute-pipeline stages whose contracts settled in M2 C1 (rule engine per `docs/rule_engine_contract.md`, scorer per `docs/scorer_contract.md`, solver per `docs/solver_contract.md`) for ICU/HD first-release inputs, wired onto the parser/normalizer's CONSUMABLE `NormalizedModel` from M2 C3. Output of C4 is a sorted `CandidateSet` with full `ScoreResult` per candidate. Selector implementation is intentionally NOT in C4 — selector retention modes and sidecar artifacts are output-layer concerns that fit naturally into the next "local run artifact basics" candidate per §5. Stack ownership remains local-first Python per D-0018; M3 stays `Planned`.
- **2026-04-27:** Added a bidirectional contract-audit rule to §14 (Maintenance rules) — "Cross-contract consistency audits MUST trace every producer-consumer seam in BOTH directions. Forward: consumer expects Y → producer declares Y. Reverse: producer references downstream output Y → consumer's input shape includes Y." Recurrence-prevention measure surfaced during M2 C4 T2 implementation discussion: the M2 C2 contract-surface audit window did not catch `scorer_contract.md` §15 (which declared operator-tuneable `scoringConfig` flows from parser boundary) ↔ `parser_normalizer_contract.md` §9 (which did not declare `scoringConfig` as a `ParserResult` output) inconsistency. The gap surfaced four contract-touches later. Adding this rule normatively going forward closes the audit-coverage gap. Active milestone (M2) and active checkpoint (M2 C4) unchanged; M3 stays `Planned`.
- **2026-04-27:** Closed the architectural revision the §14 bidirectional-audit rule was added to fix. D-0037 records the operator-tuneable scoring config surface architecture (sheet → snapshot → parser → scorer flow) covering BOTH component weights AND per-day call-point cells under uniform extraction. Five contracts touched: `docs/scorer_contract.md` (v1 → v2 — `ScoringConfig` shape grows required `pointRules` field; §15 extended; §22 v2 follow-on subsection added), `docs/parser_normalizer_contract.md` (§9 `ParserResult` outputs grow `scoringConfig`; §14 gains scoring-config admission cases), `docs/snapshot_contract.md` (top-level §5 grows `scoringConfigRecords` component; new §11A declares `componentWeightRecords` + `callPointRecords` shapes; §10 sourceLocator surfaces extended), `docs/template_artifact_contract.md` (§11 scoring posture extended with `componentWeights` defaults; §12 hard validity expectations updated), `docs/sheet_generation_contract.md` (new §11A declares the launcher-generated Scorer Config tab). OD-0002 (point-row weighting) closed and removed from `docs/open_decisions.md` per its maintenance rule (promoted to D-0037). The architectural revision was self-audited against the new bidirectional-audit rule (the dogfooding is recorded inline in D-0037). Implementation half lands as an in-checkpoint T2 patch within M2 C4 — `python/rostermonster/scorer/` `ScoringConfig` shape grows `pointRules`; `pointBalance*` components consume it with `1.0`-per-call fallback. Active milestone (M2) and active checkpoint (M2 C4) unchanged; M3 stays `Planned`.
- **2026-04-27:** Appended FW-0024 to `docs/future_work.md` — auto-repopulate empty call-point cells on the operator-facing sheet with template defaults so blank cells don't visually read as "no point / zero / inapplicable" while architecturally being backstopped to template defaults at parse time per D-0037 / parser_normalizer §9 overlay rule. Captured ahead of the parser overlay implementation (deferred to late-M2 per D-0036) so the UX gap is addressed alongside or shortly after the overlay ships. Active milestone (M2) and active checkpoint (M2 C4) unchanged; M3 stays `Planned`.
- **2026-04-27:** Recorded D-0038 in `docs/decision_log.md` — `pointRules` fail-loud on missing keys, revising D-0037 sub-decision 5. Per-key lookup raises rather than falling back silently to `1.0`; parser overlay carries a producer-coverage obligation (`(call-slot, dateKey)` cross-product MUST be total) declared in `docs/parser_normalizer_contract.md` §9 and admission-blocking per §14. Implementation half lands in the same change round: `python/rostermonster/scorer/` `_call_points_per_doctor` drops the `.get((...), 1.0)` fallback; `score()` adds a one-pass completeness validation against the model's call-slot × day cross-product; `ScoringConfig.first_release_defaults` now takes a model arg and builds complete `pointRules` via the new `uniform_point_rules(model)` helper. No `contractVersion` bump — scorer stays v2; only behavioral semantics tighten. Active milestone (M2) and active checkpoint (M2 C4) unchanged; M3 stays `Planned`.
- **2026-04-27:** Closed M2 C4 T1 (`Rule engine implementation`) on PR #78 — stateless reference implementation of all 5 first-release hard rules per `docs/rule_engine_contract.md` §11 with full-violation canonical ordering per §12, FixedAssignment scoped handling per §15, and the public surface `(normalizedModel, ruleState, proposedUnit) → Decision`. Lives at `python/rostermonster/rule_engine/`. Closed M2 C4 T2 (`Scorer implementation`) on PR #79 + PR #82 — pure-function `score(allocation, normalizedModel, scoringConfig) → ScoreResult` covering all 9 first-release components; harmonic `crReward` per §12; direction-guard property test per §13; required component breakdown per §10. PR #82 is the in-checkpoint patch wiring the D-0037 `pointRules` consumption into `pointBalance*` components. Closed M2 C4 T3 (`Solver implementation`) on PR #85 — `SEEDED_RANDOM_BLIND` first-release strategy per `docs/solver_contract.md` §11.1 + §12; `crFloor` `SMART_MEDIAN` / `MANUAL` per §13; whole-run `UnsatisfiedResult` per §14; byte-identical determinism per §16; scoring-blind end-to-end. Codex review surfaced 7 rounds of follow-ups (frozenset-iteration determinism leak under `PYTHONHASHSEED`, mode-equality + integer-type validation, fixed-assignment preservation + `ruleEngine` as input #2 instead of hard-wired import, integer `candidateId` per `docs/selector_contract.md` §16, `maxCandidates` integer guard, signed-seed RNG aliasing, per-unit doctor dedup against duplicated `eligibleGroups`); each fix is captured by a regression test in the merged PR. Closed M2 C4 T4 (`Integration smoke test`) on the closure PR — `python/tests/test_integration_smoke.py` exercises parser → solver → scorer end-to-end on the real ICU/HD May 2026 fixture; asserts non-empty `CandidateSet`, byte-identical re-runs under fixed seed, zero rule-engine violations across emitted candidates, complete `ScoreResult` shape per candidate. 5/5 integration tests pass; 92/92 across the python suite (21 parser + 3 real-data + 14 rule engine + 20 scorer + 29 solver + 5 integration). Active milestone (M2) and active checkpoint (M2 C4) unchanged at this point; closure transition follows in the next change-log entry.
- **2026-04-27:** Closed M2 Checkpoint 4 (`Minimal rule/scorer/solver integration`) on operator audit and merge of PR #78 + PR #79 + PR #80 + PR #81 + PR #82 + PR #83 + PR #84 + PR #85 + PR #86; sign-off recorded in §11. The M2 compute pipeline is now functionally end-to-end: parser/normalizer (M2 C3) → rule engine + solver + scorer (M2 C4), exercised in milliseconds on the real ICU/HD May 2026 fixture. Locked architectural decisions span D-0037 (operator-tuneable scoring config surface — scorer v1 → v2 across 5 contracts) and D-0038 (`pointRules` fail-loud on missing keys, supersedes D-0037 sub-decision 5). Active milestone (M2) unchanged; active checkpoint set to "no active checkpoint" per §8 pending separate scoping of the next M2 checkpoint (candidates per §5 / §7: local run artifact basics, snapshot-extraction Apps Script implementation per D-0036); M3 stays `Planned`.
- **2026-04-28:** Activated and closed M2 Checkpoint 5 (`Local run artifact basics`) in a single change round. Implemented the selector stage (`HIGHEST_SCORE_WITH_CASCADE` per `docs/selector_contract.md` §11.1 + §12) and the `BEST_ONLY` / `FULL` retention surface with sidecar artifacts (`candidates_summary.csv`, `candidates_full.json`) under `FULL`. Closes the contract → implementation gap that `docs/selector_contract.md` has carried since M2 C2 closure on 2026-04-26. Public entry: `select(scoredCandidateSet, *, retentionMode, runEnvelope, selectorStrategyId, selectorStrategyConfig=None, sidecarTargetDir=None) → FinalResultEnvelope` with branch discipline (§10.3), strategy/retention-mode gating (§11.1, §13), failure-branch forwarding (§15), envelope passthrough (§16.4), and byte-identical determinism (§18). Architectural import-boundary check in tests confirms the selector module does not consume `score(...)` or `evaluate(...)` per §9 — the contract permits importing result *types* (e.g. `ScoreResult`) but not the active scorer/rule-engine *interfaces*. Lives at `python/rostermonster/selector/` (5 files: `__init__.py`, `result.py`, `strategy.py`, `sidecars.py`, `selector.py`). `python/tests/test_integration_smoke.py` extended through the selector — full M2 pipeline (parser → solver → scorer → selector) now runs end-to-end on real ICU/HD May 2026 data with byte-identical re-runs across the entire stack and FULL-mode sidecar emission verified. Tests: 21 parser + 3 real-data + 14 rule engine + 20 scorer + 29 solver + 20 selector + 8 integration smoke = 115/115 pass. No new architectural decisions — implementation faithfully tracks the selector contract closed under D-0030 / D-0032 (selector v1 → v2). Sign-off recorded in §11. Active milestone (M2) unchanged; active checkpoint set back to "no active checkpoint" per §8 pending separate scoping of the remaining M2 candidate (snapshot-extraction Apps Script implementation per D-0036, pinned to late-M2 before M3 activation); M3 stays `Planned`.
- **2026-04-28:** Activated and closed M2 Checkpoint 6 (`Parser overlay for scoring config`) in a single change round. Wires the Python consumer-side of D-0037 (operator-tuneable scoring config surface) — `Snapshot.scoringConfigRecords` field per snapshot_contract §5 + §11A; `TemplateArtifact.pointRows` + `componentWeights` per template_artifact §9 + §11; `ParserResult.scoringConfig` per parser_normalizer §9; new `parser/scoring_overlay.py` implements the §9 sheet-wins / template-defaults-backstop overlay with §14 admission cases (mis-signed weight, malformed numeric, duplicate / unknown record references, incomplete pointRules cross-product per D-0038). Per-day call-point defaults applied via today/tomorrow weekday-vs-weekend classification of the calendar `dateKey` (no public-holiday calendar in first release). The producer-side — Apps Script sheet generator emitting the Scorer Config tab + production snapshot extractor reading it — remains scoped to D-0036's late-M2 Apps Script checkpoint; the test-only xlsx → snapshot bridge was extended to emit empty `scoringConfigRecords` so the parser overlay falls back to template defaults end-to-end on the dev-copy fixture. Tests: 29 parser (8 new) + 3 real-data (extended) + 14 rule engine + 20 scorer + 29 solver + 20 selector + 9 integration smoke (1 new) = 124/124 pass. Codex review surfaced 3 rounds of follow-ups (P1: ISO date validation at admission to prevent downstream crash on malformed `rawDateText`; P1×2: non-finite numeric guard on operator-edited weight + call-point cells; P2: duplicate `pointRows[].rowKey` reject; P1×3: non-finite template default weight, pointRow.slotType invalid/non-CALL, non-finite defaultRule field — total 8 fixes, each paired with a regression test, final test count 38 parser + supporting). No new architectural decisions — implementation faithfully tracks D-0037 + D-0038 contract semantics. Sign-off recorded in §11. Active milestone (M2) unchanged; active checkpoint set back to "no active checkpoint" per §8 pending separate scoping of the remaining M2 candidate (snapshot-extraction Apps Script implementation per D-0036); M3 stays `Planned`.
- **2026-04-28:** Activated and closed M2 Checkpoint 7 (`Scorer Config tab — D-0037 sheet-generator half`) in a single change round. Closes the D-0037 producer-side gap M2 C6 left open: the Apps Script launcher now generates the Scorer Config tab declared in `docs/sheet_generation_contract.md` §11A, with the 9 first-release component weights pre-populated from `template.scoring.componentWeights` and operator-editable. Each data row carries `DeveloperMetadata` keyed by canonical componentId for stable extractor lookup. `TemplateArtifact.gs` extended with `pointRows[].slotType` bindings (D-0037) and the `componentWeights` defaults map. `ScorerConfigTab.gs` (new) builds the tab; both `generateIntoNewSpreadsheet` and `generateIntoExistingSpreadsheet` entrypoints call it after the request-entry tab finishes. Tests: 13 existing + 6 new = 19 Apps Script unit tests via `runAllTests_`; manual end-to-end verification via clasp deploy + live launcher run. **FW-0024 attempted but reverted on Codex P1 review (PR #89)** — a `CallPointAutoFill.gs` module with simple `onEdit(e)` was drafted but Codex flagged that simple triggers don't fire on launcher-generated standalone-Web-App spreadsheets. The proper fix needs installable triggers with quota / ownership / cleanup discipline; deferred to a dedicated checkpoint. The `docs/future_work.md` FW-0024 entry was updated to capture the trigger-architecture finding so the next attempt has a clearer starting point. **Hotfix landed as PR #90** — initial implementation attached `DeveloperMetadata` to a 3-cell range, which the Sheets API rejects as "arbitrary range"; switched to A1 row notation so the range is recognised as row-scoped. **UX polish landed as PR #91** — expanded the Scorer Config tab from 3 cols to 4 (Component / Weight / Description / Sign), added per-row data validation rejecting wrong-sign weight values at type-time, wrote per-component descriptions matching operator vocabulary (R3, registrar calls, CRs). No new architectural decisions — implementation faithfully tracks `docs/sheet_generation_contract.md` §11A + `docs/template_artifact_contract.md` §9 + §11. Sign-off recorded in §11. Active milestone (M2) unchanged; active checkpoint set back to "no active checkpoint" per §8 pending separate scoping of the remaining M2 candidate (snapshot-extraction Apps Script implementation per D-0036); M3 stays `Planned`.
- **2026-04-28:** Activated M2 Checkpoint 8 (`spacingPenalty geometric-decay shape (port v1)`) per `docs/decision_log.md` D-0039. Replaces v2's binary `gap < 3` count with the v1 reference implementation's geometric decay (`weight / 2^(gap - 2)` across gaps `[2, 6]`, zero at gap `≥ 7`). Restores the continuous gradient so the solver's `SEEDED_RANDOM_BLIND` Phase 2 tie-break can distinguish "tightly-spaced but legal" from "comfortably-spaced" pairs. Hard-pins the curve shape in `docs/scorer_contract.md` §12A (new normative subsection parallel to §12's `crReward` strict-decrease pattern) — operator-tuneable curve parameters remain `docs/future_work.md` FW-0007 deferred. The 7-day cutoff embeds the once-per-week call cadence as the natural rhythm we neither encourage nor discourage. **Scorer `contractVersion` bumped from `2` to `3`** per §2 (scoring-semantics change for valid configs — same `ScoringConfig` produces different `spacingPenalty.score` under v3 vs v2). Public `ScoringConfig` shape itself is unchanged. C8 is pure-Python scorer territory + tests; no Apps Script changes. Implementation phased docs-first per the contract-first discipline: Phase 1 docs (this entry + D-0039 + §12A); Phase 2 code (`spacing_penalty()` rewrite + curve-shape property tests); Phase 3 closure (sign-off + §11 entry). Active milestone (M2) unchanged; M3 stays `Planned`.
- **2026-04-28:** Closed M2 Checkpoint 8 (`spacingPenalty geometric-decay shape (port v1)`) on operator audit and merge of PR #92 (Phase 1 docs) + PR #93 (Phase 2 code) + the closure PR; sign-off recorded in §11. The scorer's `spacingPenalty` component now matches v1's geometric-decay reference shape (`weight / 2^(gap - 2)` across gaps `[2, 6]`, zero past gap = 7). Phase 1 codex review surfaced 3 findings across 2 rounds — P1 contractVersion bump (initial draft said no bump) + P2 strict-decrease wording self-contradictory at `weights[spacingPenalty] = 0` + follow-on P2 stale `v2` cross-refs in 4 downstream contracts (`docs/snapshot_contract.md` §11A, `docs/sheet_generation_contract.md` §11A, `docs/parser_normalizer_contract.md` §9, `docs/future_work.md` FW-0024 context) flagged by the §14 bidirectional-audit rule; each addressed in the same PR. Phase 2 codex review returned the clean signal on first auto-pass (no findings). Test fixture model widened from 5 days to 14 days so spacing tests can probe gaps up to + past `MAX_SOFT_GAP_DAYS`; 4 new spacing tests added (exact decay progression, strict-monotonic-decrease property test across multiple negative weights, zero past cutoff, weight-zero disable path); 23 scorer + 38 parser + 14 rule_engine + 29 solver + 20 selector + 9 integration smoke + 3 real-data = **136/136 pass**. Locked architectural decisions: D-0039 (spacingPenalty geometric-decay shape pinned + scorer v2 → v3 bump). FW-0007 (operator-tuneable curve parameters: alternative `MAX_SOFT_GAP_DAYS`, alternative decay shapes such as linear or exponential) remains deferred. Active milestone (M2) unchanged; active checkpoint set back to "no active checkpoint" per §8 pending separate scoping of the remaining M2 candidate (snapshot-extraction Apps Script implementation per D-0036, pinned to late-M2 before M3 activation); M3 stays `Planned`.
- **2026-04-29:** Activated M2 Checkpoint 9 (`Snapshot extraction (Apps Script) — D-0036 implementation`) — the final M2 checkpoint. Phase 1 (docs + decisions) lands four new decision-log entries — D-0040 (transport = browser-download of operator-saved JSON file; symmetric inbound + outbound mechanic resolving D-0036 sub-decision 4 + writeback §6.3 joint scoping), D-0041 (extractor placement = bound template spreadsheet + central Apps Script library at HEAD; replaces launcher's `SpreadsheetApp.create()` with `makeCopy(template)`; resolves the FW-0024 architectural blocker as a side effect), D-0042 (snapshot identity = `snapshot_<spreadsheetId>_<extractionTimestamp>`; matches launcher's `runId` parallel pattern), D-0043 (layout drift = DeveloperMetadata-anchored extraction with hard-fail on missing anchors; no visible-text fallback because pre-D-0041 sheets cannot run the extractor anyway). New contract `docs/snapshot_adapter_contract.md` drafted covering adapter responsibilities (extends snapshot_contract §3), source surfaces (request-entry tab + Scorer Config tab with the new metadata anchor set), extraction algorithm (metadata-finder + column-offset reads), output transport (browser-download mechanic), snapshot identity, migration story (pre-D-0041 sheets must regenerate), error surface, and Python ingestion CLI. `docs/writeback_contract.md` §6.3 receives the resolved-transport addendum citing D-0040; the §6.3 deferral language is retained for forward-compat (writeback launcher form UX remains M3 implementation-slice). `docs/sheet_generation_contract.md` gains §11B declaring the launcher-attached DeveloperMetadata posture (sheet level + per-row anchors), making the extractor-side anchor dependencies normative on the launcher side. `docs/future_work.md` FW-0024 updated: D-0041 supersedes the PR #89 finding; FW-0024 is no longer architecturally blocked, deferred only because pilot operator confusion has not yet been observed. C9 is the final M2 checkpoint; M2 closes when C9 closes. Phased docs-first delivery per the M2 C8 cadence: Phase 1 docs (this entry + D-0040..D-0043 + new adapter contract); Phase 2 code (bound template + library + extractor + Python ingestion CLI + end-to-end demo); Phase 3 closure (sign-off + §11 entry). Active milestone (M2) unchanged; M3 stays `Planned`.
- **2026-04-29:** Closed M2 Checkpoint 9 (`Snapshot extraction (Apps Script) — D-0036 implementation`) on operator audit and merge of PR #95 (Phase 1 docs) + PR #96 (Phase 2 code) + the closure PR; sign-off recorded in §11. The production Apps Script extractor is now in place: launcher generates operator sheets via `DriveApp.makeCopy(template)`, each operator sheet inherits a bound shim from the template, the bound shim's `onOpen` installs a "Roster Monster → Extract Snapshot" menu, the menu delegates to a central library at HEAD, the library reads the sheet via sheet-scoped DeveloperMetadata finders with runId-paired tab discovery + per-anchor cardinality validation, and serves a downloadable JSON blob the operator saves locally. The Python CLI (`python -m rostermonster.run --snapshot path/to/file.json`) ingests the JSON and runs the full M2 pipeline (parser → solver → scorer → selector). Codex review surfaced ten rounds of findings across the two implementation PRs; five real architectural P1s were addressed (HEAD-vs-Editor library access, link-wide-vs-per-email sharing, OAuth scope expansion, template Drive ACL, workbook-vs-sheet-scoped finder), three real silent-data-loss / wrong-config bugs were addressed (per-anchor completeness validation for doctor rows, P0 templateVersion path, P1 assignment-row coverage), two internal-consistency P2s were addressed (§4/§6 finder consistency, §1/§3 auth-surface wording), and three sharing-related P1s were deliberately deferred to FW-0025 with the trade-off explicitly documented. JSON shape correction surfaced during Phase 2: the contract's `sourceLocator.path = {...}` typed-shape framing didn't match the live flattened shape the parser actually consumes; SnapshotBuilder.gs locators flattened to match. ICU/HD template promoted from `python/tests/fixtures.py` to `python/rostermonster/templates/icu_hd.py` so the CLI doesn't import test code; existing fixtures.py re-exports for test compatibility. Tests: 141/141 pass (was 136; +5 CLI tests covering end-to-end on the May 2026 fixture, byte-identical determinism, missing-snapshot exit-code, deserializer round-trip, recursive serializer). End-to-end CLI run: 116/116 assignments filled, score -21.534. Apps Script tests for the two new projects deferred until operator-side clasp-clone setup is complete. Locked architectural decisions span D-0040..D-0043 (transport, placement, identity, layout-drift). FW-0025 (per-email Drive sharing tightening) deferred with four explicit revisit triggers. **M2 (`Minimal local compute pipeline`) closes at this checkpoint per §6** — C9 was the final M2 checkpoint per §5. Active milestone moves to *none*; active checkpoint moves to *none*. M3 (`Safe result/output and writeback`) stays `Planned` until the maintainer activates it. The `m1_template_bound_script/` and `m1_extractor_library/` directories created in Phase 2 were renamed to `m2_*` in this closure round to match the milestone-prefix-by-introduction convention applied to `m1_sheet_generator/` (the prefix means "introduced in M<N>", not "owned by M<N>"); all cross-references updated.
- **2026-04-30:** Activated M3 (`Safe result/output and writeback`) at milestone level + activated M3 Checkpoint 1 (`Writeback library implementation + launcher route + Python CLI extension`) immediately, mirroring the same-day "activate-milestone-and-first-checkpoint" pattern used at M2 activation on 2026-04-22. Phase 1 (docs + decisions) lands four new decision-log entries — D-0044 (writeback transport mechanic = file upload via the launcher web form, refining D-0040's "drag/paste" wording for the writeback direction; no new OAuth scopes), D-0045 (writeback envelope shape = single JSON wrapper carrying `FinalResultEnvelope` + snapshot subset + `doctorIdMap`, so the operator uploads ONE file rather than three), D-0046 (writeback placement = launcher Web App route, NOT in-sheet bound shim menu — keeps the M2 C9 bound-template architecture clean and reuses the launcher's existing form infrastructure), D-0047 (Python CLI's standard output IS the writeback envelope, controlled by a new `--writeback-ready` flag defaulting to `true`; the existing bare-`FinalResultEnvelope` mode remains available behind `--writeback-ready=false` for callers who don't intend to writeback). `docs/writeback_contract.md` §6.3 receives the resolved-transport addendum citing D-0044 (parallel to the snapshot extractor's D-0040 resolution); the §22 deferral language for drag-and-drop and Drive Picker is retained for forward-compat. `docs/snapshot_adapter_contract.md` §11 grows a forward-pointer to D-0047 noting the CLI's outbound extension under M3 C1. The writeback contract was drafted scope-ahead during the M2 C2 round (`docs/writeback_contract.md` `contractVersion: 1`, D-0031 with eleven sub-decisions), so M3 C1 is implementation-heavy rather than contract-heavy — Phase 1 only settles the four concrete-implementation choices the contract deferred. Phased docs-first delivery per the M2 C8 / M2 C9 cadence: Phase 1 docs (this entry + D-0044..D-0047 + cross-contract updates); Phase 2 code (Apps Script writeback library + launcher route + Python CLI extension + tests); Phase 3 closure (sign-off + §11 entry + on-real-data demo).
- **2026-04-30:** Closed M3 Checkpoint 1 (`Writeback library implementation + launcher route + Python CLI extension`) on operator audit and merge of PR #100 (Phase 1 docs) + PR #101 (Phase 2 code) + the closure PR; sign-off recorded in §11. The full sheet → snapshot → Python compute → writeback round-trip now runs end-to-end: Python CLI emits a wrapper envelope (`FinalResultEnvelope` + snapshot subset + `doctorIdMap`); the launcher Web App's `?action=writeback` route accepts the upload via a file-input form with `FileReader` + `google.script.run.applyWriteback`; the Apps Script writeback library writes a new tab in the source spreadsheet with the winner allocation, prefilled cells at their source-tab `(surfaceId, rowOffset)` coordinates, traceability footer, hidden DeveloperMetadata, and whole-tab read-only protection. The 3-state launcher diagnostic per `docs/writeback_contract.md` §17 surfaces SUCCESS / FAILED / RUNTIME_ERROR with `#gid=`-anchored spreadsheet URL on success. **Phase 2 contract amendment surfaced during Codex round 2**: `docs/writeback_contract.md` §9 grew a 6th category `outputAssignmentRows` (`[{surfaceId, slotType, rowOffset}, ...]` from the template's `outputMapping.surfaces[].assignmentRows[]`) so the writeback library can place prefilled cells at their source-tab cell positions per §10.1 — the binding cannot be derived from `AssignmentUnit` alone (which carries `slotType` but no `rowOffset`). D-0045's Follow-up #2 had foreseen this Phase 2 pinning, so the addition closes that follow-up rather than introducing a new decision. **Live operator-facing demo** executed on the M2 C9 dev-copy snapshot (real Drive ID `1XXfA3AyNtZ1dqp74aX1Qx6KDVnzms2T19LUUmduLnyg`, source tab `v0429214357`): 100 candidates, 116/116 filled, score -2199.97; writeback tab written + verified with traceability + protection live. Codex review on PR #101 surfaced 5 findings across 3 rounds (round 1: P1 prefilled-cell positioning + P2 tab-anchor URL; round 2: P1 surfaceId-as-slot-row-key wrong + P2 protection-editor preservation; round 3: P2 tab-creation try/catch coverage), all addressed inside the same PR before merge. Tests: 143/143 Python pass; 17 Apps Script writeback unit tests via `runAllTests_`. Locked architectural decisions span D-0044..D-0047. Active milestone (M3) unchanged; active checkpoint set back to "no active checkpoint" per §8 pending separate scoping of M3 C2 (`End-to-end demo on a real ICU/HD operator cycle + M3 closure`); M2 stays Completed.
- **2026-04-30:** **Closed M3 (`Safe result/output and writeback`)** at the milestone level on M3 C1 closure; M3 C2 was dropped per `docs/decision_log.md` D-0048 (live-operator demo no longer the right next investment; round-trip already proven on the dev-copy round-trip exercised under M3 C1). M3's exit criteria were materially met by C1 closure — the writeback round-trip with traceability + 3-state diagnostic + no silent partial-state was live-verified on the dev-copy snapshot. **Activated M4 (`Cloud end-to-end pipeline + dual-track preservation`)** at the milestone level same-day per D-0049, mirroring the M2 → M3 same-day activation pattern. M4 was reframed from `Parallel operational search and orchestration` to the new goal because the maintainer's near-term priority shifted to a boss-clickable cloud demo (political/visibility goal), with solver-strategy work flagged as the next post-M4-C1 priority and parallel orchestration de-prioritized. The old M4 framing (parallel) and the old M5 framing (observability) were parked as **future-work entries FW-0027 + FW-0028** — re-promotable to future milestones when concrete drivers surface. **Activated M4 Checkpoint 1 (`Cloud end-to-end pipeline (minimum demo)`)** alongside the milestone activation, mirroring the same-day "activate at milestone level + first checkpoint" pattern. M4 C1's quality bar is **minimum demo** (sync wait, basic toast, click-through ready) per the maintainer call recorded in D-0049. Phase 1 (this entry + five new D-NNNN entries D-0048..D-0052 + new contract `docs/cloud_compute_contract.md` + cross-contract additive updates to `docs/snapshot_adapter_contract.md` §7 + §11 and `docs/writeback_contract.md` §6.3 + FW-0027 / FW-0028) settles the architectural commitments around the Cloud Run + IdentityToken + dual-track-Python architecture. Phase 2 (code) lands the cloud HTTP wrapper, Cloud Run deploy in `RosterMonsterV2`, Apps Script library reorganization (writeback library moves from launcher project into the central library so bound shim can invoke it inline), and the bound shim's "Solve Roster" menu. Phase 3 closure ties to a boss-as-non-maintainer demo. Active milestone is M4; active checkpoint is M4 C1; M3 stays `Completed`; M2 stays `Completed`.
- **2026-05-01:** Closed M4 Checkpoint 1 (`Cloud end-to-end pipeline (minimum demo)`) on operator audit and merge of PR #103 (Phase 1 docs) + PR #104 (Phase 1 amendment for D-0053 random-seed default) + PR #105 (Phase 2 code) + the closure PR; sign-off recorded in §11. The full **operator-facing one-click cloud-mode pipeline** runs end-to-end: bound shim's `Roster Monster → Solve Roster` menu orchestrates in-memory snapshot extraction → POST to Cloud Run → writeback into a new tab — within one Apps Script invocation. Cloud Run service `roster-monster-compute` deployed at `https://roster-monster-compute-693837275969.asia-southeast1.run.app` (asia-southeast1, scale-to-zero, max-5, `--allow-unauthenticated` + Flask-side ALLOWED_EMAILS allowlist per D-0054 amendment). The original D-0051 IAM-based auth design didn't survive live testing (`ScriptApp.getIdentityToken()`'s `aud` claim doesn't match Cloud Run's expected service URL); D-0054 amends to a public-service + app-level allowlist via custom `X-Auth-Token` header (Cloud Run strips the standard `Authorization` header on public services). Bound shim manifest scope `spreadsheets.currentonly` was insufficient for `SpreadsheetApp.openById()` even on the bound spreadsheet's own ID; broadened to full `spreadsheets`. Path 2-Lite visual polish aligns the writeback tab's appearance with M1.1's empty-shell aesthetic (FW-0029 captures the template-aware label propagation path for when a second template enters scope). Codex review on PR #105 surfaced 7 findings across 4 rounds (3 P1s + 4 P2s), all addressed inside the same PR. Tests: 164/164 Python pass (was 143; +14 service + +7 pipeline). Locked architectural decisions span D-0048..D-0054. **Closed M4 (`Cloud end-to-end pipeline + dual-track preservation`) at the milestone level** at this checkpoint — C1 was the only declared M4 checkpoint, and its exit criteria materially met M4's stated goal. Solver-strategy optimization (the maintainer's stated post-cloud priority per D-0049's forward-pointer) is materially different in character from M4's "delivery surface" framing (changes core compute semantics) and lands as its own forthcoming milestone slot rather than an M4 C2. Active milestone moves to *none*; activation of the next milestone is deferred to the maintainer's call. M3 stays `Completed`; M2 stays `Completed`.

## 13. Relationship to other repo docs
- `README.md` = front door orientation.
- `docs/blueprint.md` = stable architecture truth.
- `docs/roadmap.md` = milestone-level delivery order.
- `docs/delivery_plan.md` = active execution guide.
- `docs/decision_log.md` = accepted directional decisions.
- `docs/open_decisions.md` = pending decisions surfaced by implementation, awaiting closure (reviewed at checkpoint sign-off).
- `docs/future_work.md` = non-normative parking lot for ideas.
- Contract docs = normative technical boundary definitions.

## 14. Maintenance rules for this document
- Keep exactly one active milestone unless there is a deliberate exception.
- Keep exactly one active checkpoint unless there is a deliberate exception.
- Update this document whenever execution focus changes.
- Do not duplicate normative contract wording here.
- Do not turn this document into a full issue tracker.
- If a task does not support the active checkpoint, it likely does not belong here.
- Cross-contract consistency audits MUST trace every producer-consumer seam in BOTH directions. Forward: when contract X says it consumes Y, verify Y's producer contract declares Y as a normative output. Reverse: when contract X references producing Y for a downstream consumer, verify the consumer's contract declares Y in its input shape. The reverse direction is the harder one to remember (the M2 C2 contract-surface audit missed `scorer_contract.md` §15 ↔ `parser_normalizer_contract.md` §9 because of it — `scorer §15` declared the operator-tuneable scoring config flows from the parser boundary, but `parser_normalizer §9` did not declare `scoringConfig` as part of `ParserResult`; the gap surfaced four contract-touches later, during M2 C4 T2 implementation, instead of at the C2 audit window).

## 15. Initial seed content
This version reflects the 2026-05-01 **closure of M4 (`Cloud end-to-end pipeline + dual-track preservation`)** at milestone level on M4 C1 closure, on top of the 2026-04-30 closure of M3 (`Safe result/output and writeback`) at milestone level. Current state:
- active milestone **none** — M4 closed 2026-05-01 with M4 C1 as its only delivered checkpoint. Solver-strategy optimization (the maintainer's stated post-cloud priority per D-0049's forward-pointer) lands as its own forthcoming milestone slot rather than an M4 C2 because it changes core compute semantics rather than adding a delivery vehicle.
- active checkpoint **none** — M4 C1 closed 2026-05-01.
- closed-milestone trail: M1 (C1–C4) + M1.1 (C1) + **M2 (C1–C9)** + **M3 (C1)** + **M4 (C1)**, anchored by **D-0019..D-0054** with sign-off notes in §11.
- closed-checkpoint trail for **M4 C1 + M3 C1 + M2 C9 + M2 C8 + M2 C7 + M2 C6 + M2 C5 + M2 C4 + M2 C3 + M2 C2 + M2 C1** in §11 with sign-off notes.
- M4 architectural surface settled across the milestone: **D-0048** (close M3 early — drop M3 C2), **D-0049** (reframe M4 to cloud-end-to-end + dual-track preservation; old M4/M5 → FW-0027 + FW-0028), **D-0050** (dual-track Python architecture — shared compute core + thin CLI wrapper + thin HTTP wrapper), **D-0051** (Cloud Run platform + consolidated `RosterMonsterV2` GCP project), **D-0052** (Apps Script library reorganization — writeback moves into central library so bound shim can invoke it inline), **D-0053** (solver seed default = random per invocation), **D-0054** (Cloud Run auth = public service + Flask-side `X-Auth-Token` validation + `ALLOWED_EMAILS` allowlist; amends D-0051 sub-decision 2 after live testing surfaced the `aud` mismatch). New contract `docs/cloud_compute_contract.md` (`contractVersion: 1`) pins the HTTP boundary shape, auth model, structured-error envelope, determinism / idempotency stance, and Cloud Run deployment posture. Cross-contract additive updates: `docs/snapshot_adapter_contract.md` §7 + §11 (in-memory extraction + cloud-mode dual-track addenda); `docs/writeback_contract.md` §6.3 (bound-shim direct-invocation path).
- the **full sheet → snapshot → Python compute → writeback** round-trip runs end-to-end on real ICU/HD data in **both modes**:
  - **Local mode**: launcher generates a request-entry tab + paired Scorer Config tab via `DriveApp.makeCopy(template)`; the bound shim's "Extract Snapshot" menu downloads a Snapshot JSON; the operator saves the JSON locally; the Python CLI (`python -m rostermonster.run --snapshot path/to/file.json`) ingests it and produces a wrapper-envelope JSON; the operator uploads the envelope via the launcher's `?action=writeback` route; the writeback library writes a new tab.
  - **Cloud mode (new under M4 C1)**: bound shim's `Roster Monster → Solve Roster` menu orchestrates extract → POST to Cloud Run (`roster-monster-compute` in `RosterMonsterV2` GCP project, asia-southeast1) → in-memory writeback within one Apps Script invocation. ~30-90s synchronous wait. M1.1-style visual fidelity on the writeback tab.
  - Both modes share the same Python compute core per D-0050; both produce byte-identical wrapper envelopes at the same `(snapshot, optionalConfig)` input when seed is explicitly set per D-0053's parity precondition.
- **164/164 Python tests pass** via standalone runners (38 parser + 3 real-data + 14 rule_engine + 23 scorer + 29 solver + 20 selector + 9 integration smoke + 7 CLI + 7 pipeline + 14 service). Apps Script tests run via `clasp run runAllTests_` on the launcher project (17 writeback-helper unit tests added under M3 C1).
- scorer contract is at `contractVersion: 3` per D-0039; `ScoringConfig` public shape unchanged from v2.
- contracts settled across M2 + M3 + M4: `docs/snapshot_contract.md`, `docs/snapshot_adapter_contract.md`, `docs/parser_normalizer_contract.md`, `docs/template_artifact_contract.md`, `docs/sheet_generation_contract.md`, `docs/rule_engine_contract.md`, `docs/scorer_contract.md` (v3), `docs/solver_contract.md`, `docs/selector_contract.md` (v2), `docs/writeback_contract.md` (`contractVersion: 1`, §9 6 categories), `docs/cloud_compute_contract.md` (`contractVersion: 1`).
- transport decisions for the Apps Script ↔ Python boundary span two modes: **local mode**: inbound = browser-download per D-0040; outbound = file upload via launcher form per D-0044 — no new OAuth scopes for this mode. **Cloud mode**: inbound = in-memory snapshot via central library entrypoint; outbound = HTTP request body + structured-response envelope per `docs/cloud_compute_contract.md`. Cloud mode required four manifest-scope additions on the bound shim per D-0051 sub-decision 3a + D-0054: `script.external_request` (`UrlFetchApp.fetch`), `openid` (`ScriptApp.getIdentityToken`), `userinfo.email` (token email claim), and `spreadsheets` (broadened from `currentonly` per the live-testing finding for `SpreadsheetApp.openById`). Cloud Run runs `--allow-unauthenticated`; operator-identity gating is via Flask-side `ALLOWED_EMAILS` env var per D-0054.
- `docs/open_decisions.md` is empty. M4 C1 surfaced two architectural amendments (D-0053 random-seed default, D-0054 auth model after live testing) — both settled in-flight as accepted decisions rather than open-decision queue entries.
- explicit deferrals retained from the M1.1 scope (public signup, in-app allowlist, operator-editable template, persisted per-operator state, alternative launcher platforms) and M4's parked frameworks (parallel orchestration → FW-0027; observability → FW-0028; template-aware label propagation in writeback library → FW-0029; service-account-based Cloud Run auth → D-0054 §7.6 future-work paragraph).
