// Layout.gs
// Helpers for building the roster sheet layout (headers, frozen rows/cols, column widths).
// TODO(M1): add layout builders used by GenerateSheet.gs.

function buildLayout_() {
  // TODO(M1): construct layout descriptors.
}
