// ProtectionAndValidation.gs
// Helpers for editable/protected ranges and data validation rules on the roster sheet.
// TODO(M1): add protection + validation helpers used by GenerateSheet.gs.

function applyProtections_() {
  // TODO(M1): mark header/metadata ranges as protected.
}

function applyValidations_() {
  // TODO(M1): attach shift-code dropdowns to editable cells.
}
