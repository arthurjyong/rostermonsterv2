# Runtime dependencies for the M4 C1 Cloud Run service per
# `docs/cloud_compute_contract.md` §8.5. The rostermonster package
# itself is pure-Python with no third-party deps, so this list stays
# narrow on purpose — Cloud Run cold-start latency benefits.
flask>=3.1,<4.0
gunicorn>=23.0,<24.0
