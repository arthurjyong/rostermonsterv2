# Roster Monster v2

Roster Monster v2 is a reusable roster-allocation core with department-specific templates, keeping **Google Sheets** as the operational front end. The first concrete implementation target is **CGH ICU/HD**.

## Current state
- **Live end-to-end in two modes.** Operator-facing one-click cloud (`Roster Monster → Solve Roster` menu in the bound-template spreadsheet → Cloud Run service `roster-monster-compute` → writeback into a new tab) and maintainer-facing local CLI (Apps Script extracts a Snapshot JSON → `python -m rostermonster.run --snapshot file.json` → upload via the launcher's writeback form). Both modes share one Python compute core per D-0050 and produce byte-identical envelopes at the same input when seed is explicitly set per D-0053.
- **All milestones M1 / M1.1 / M2 / M3 / M4 / M5 / M6 closed** (M6 closed 2026-05-09 per D-0069). 249 Python tests pass (244 baseline at M6 C3 close + 5 swapProbability / audit-surface tests added in M6 C4 PR-A; pipeline 10/10, selector 23/23, run-CLI 12/12, solver 41/41 at M6 closure); 17 Apps Script writeback unit tests.
- **Active milestone: M7 — Parallel solver (Cloud Batch + single-VM dense pack, async UX)** *(activated 2026-05-10 per D-0070; UX pivoted sync → async on 2026-05-11 per D-0071 driven by M7 C2 perftest evidence; architecture amended to single-VM 2026-05-11 per Codex P1.7 fix)*. M7 parallelizes K-trajectory LAHC via Cloud Batch on a single large VM (`c3-highcpu-88` running `multiprocessing.Pool(88)` for K=88 trajectories; the same single Cloud Batch task does the inline finalize step after the Pool joins). **Architecture pivoted twice during M7 C3:** (1) D-0070's original 13-VM dense pack (`c3-highcpu-8` × 13 = K=104) inside sync UX with Cloud Run timeout 250s; (2) D-0071's async pivot kept 13-VM dense pack but moved to async via Cloud Batch finalizer task group (separate 2nd task group with `SUCCEEDED_OR_FAILED` dependency on the worker group); (3) **Codex P1.7 amendment** caught that Cloud Batch v1 supports only ONE task group per job — multi-task-group + cross-group dependencies don't exist — forcing the pivot to single-VM with inline finalize. K reduces 104 → 88 (statistically equivalent — both 4-9× past best-of-K plateau at K=10-20 per FW-0036). M7 C2 perftest evidence (5/13 tasks unfinished at 240s deadline on real production roster at K=104) drove the sync→async pivot. The architecture escapes the GIL-bound single-thread bottleneck identified during M6 C4 cloud benchmarking. FW-0037 elbow tuple locked: `L=50` / `idleThreshold=3500` / `swapProbability=0.5`. K_target=2,500 stays as design center; K=88 reflects current C3_CPUS=108 quota — future C3_CPUS ≥176 unlocks `c3-highcpu-176` for K=176 (parameterized FW-0040 dial). On-demand pricing chosen over Spot for predictable wall-time. **Active checkpoint: M7 C4 — Async UX implementation (per D-0071 + Codex P1/P2 fixes)** *(activated 2026-05-12 on M7 C3 Task 1 closure — PR #147 landed after 17 rounds of Codex iteration)*. M7 C3 closed 2026-05-12 — D-0071 architecture locked + ~38 Codex-driven fixes consolidated into the contract. M7 C2 closed 2026-05-11 — full Cloud Batch parallel-LAHC chain delivered (worker + orchestrator + Batch job spec + GCS adapter + attempt-id race closure) with the §12A.4 byte-identity exit criterion proven (5/5 audit tests; local-CLI ↔ Cloud-Batch produce byte-identical writebackEnvelope at the same `(masterSeed, K)` including the negative-seed `_UINT64_MASK` case). M7 C1 closed 2026-05-10 with GCP quota approvals (asia-southeast1): CPUS=300 / INSTANCES=350 / C3_CPUS=108 (binding). M7 closure trail: D-0070 (M7 framing) → D-0071 (M7 C3 async pivot + Codex P1.7 single-VM amendment). M6 closure trail: D-0066 → D-0067 → D-0068 → D-0069. M7 C4 implementation per-PR cadence (per `docs/delivery_plan.md` §9 — reordered at Codex P2 round 1 finding 3 to eliminate broken-window risk between bound shim + Cloud Run cutover): T2A (Cloud Batch single-task job spec + worker.py inline finalize step) → T2B (launcher SECOND Web App deployment with `executeAs: USER_DEPLOYING` + `AsyncRenderCallback.gs` route + `script.external_request` + `script.send_mail` OAuth scopes + version-pinned) → T2C (bound shim `Menu.gs` defensive async-state handler — accepts SUBMITTED + retains OK/UNSATISFIED/INPUT_ERROR/COMPUTE_ERROR back-compat) → T2D (Cloud Run `/compute` thin front-door refactor: validate + concurrent-rejection + submit Batch + return SUBMITTED — safe because T2C bound shim already handles SUBMITTED). M7 C5 live-operator validates + closes M7. Cloud Run per-instance concurrency, threading, and Spot pricing explicitly excluded per D-0070 sub-decisions. FW-0035 (cloud-mode LAHC integration) + FW-0030 (cloud-side FULL retention support) co-promote with M7 closure (closure path now async per D-0071); FW-0027 absorbed into M7 C3+C4 per D-0071; new FW-0039 (silent-operator-outcome watchdog) added per D-0071 sub-decision 17 + Codex P1 fix as accepted-gap-for-v1; new FW-0040 (K=88 → K=176 dial) per Codex P1.7 amendment. End-to-end operator workflow live in cloud (currently `SEEDED_RANDOM_BLIND` strategy in v1; LAHC arrives via cloud at M7 C4 wiring + maintainer-only `/compute-lahc-test` route already live for end-to-end testing) and local (CLI with `--strategy LAHC` + analyzer + launcher upload form).

## Repo posture
- **Architecture-first and contract-first.** Core boundaries are pinned in explicit contract docs before broad implementation work. The docs-first cadence (Task 1 docs → Task 2 code, with optional Task 2A/2B/2C sub-letters when work has logically distinct chunks → Task 3 closure; per D-0064) held across M2..M5. Historical "Phase 1/2/3" labels in past PR titles and closed-milestone closure entries are preserved as-is — the cadence vocabulary applies forward from M5 closure onward.

## Planning vocabulary
**Product** → **Milestone** → **Checkpoint** → **Task**. Normally one active milestone and one active checkpoint at a time.

## Repo navigation
- `docs/blueprint.md` — Stable architecture truth (what the system is, boundary invariants).
- `docs/roadmap.md` — Milestone-level delivery order + closed-milestone trail.
- `docs/delivery_plan.md` — Active execution guide (active milestone/checkpoint/tasks, recently completed checkpoints).
- `docs/decision_log.md` — Accepted directional decisions (D-0001..D-0070).
- `docs/future_work.md` — Non-normative parking lot for ideas (FW-0001..FW-0038).
- `docs/open_decisions.md` — Pending decisions awaiting closure (empty as of 2026-05-10).
- `docs/*_contract.md` — Normative technical boundary definitions.

## Code layout
- `apps_script/launcher/` — Operator-facing launcher Web App (sheet generation + writeback form).
- `apps_script/bound_shim/` — Bound shim attached to the template spreadsheet (Extract Snapshot + Solve Roster menus).
- `apps_script/central_library/` — Central Apps Script library (snapshot extractor + writeback library + cloud-mode orchestration).
- `python/rostermonster/` — Shared Python compute core (parser, normalizer, rule engine, scorer, solver, selector, pipeline entrypoint).
- `python/rostermonster_service/` — Flask HTTP wrapper for Cloud Run (D-0050 dual-track).
- `cloud_compute_service/` — Dockerfile + deployment bundle for Cloud Run.

## How to use this repo
1. Read this README for orientation.
2. Read `docs/blueprint.md` for architecture and boundary invariants.
3. Use `docs/delivery_plan.md` for current execution truth.
4. Check `docs/decision_log.md` if a proposed change is direction-changing.
5. Read the narrowest relevant contract doc for technical boundary detail.
